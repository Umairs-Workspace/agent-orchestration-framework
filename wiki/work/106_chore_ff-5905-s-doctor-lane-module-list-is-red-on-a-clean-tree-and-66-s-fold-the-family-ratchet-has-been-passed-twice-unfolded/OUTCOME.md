# 106 · Ff 5905 S Doctor Lane Module List Is Red On A Clean Tree And 66 S Fold The Family Ratchet Has Been Passed Twice Unfolded — Outcome

## Delivered

### 66's fold-the-family ratchet is answered, and the answer is a refusal with a price
[wiki/work/TECH_DEBT.md](wiki/work/TECH_DEBT.md#L516) item 10 records the ratchet as SUPERSEDED with
the measurement behind it — the fold buys 8 of 158 root modules (5.1%) and strands 214
`src/work-doctor-*.mjs` citations across 87 `wiki/` documents that no gate in this tree resolves —
so an eighth doctor lane arrives at a decided gate instead of an unanswered one.

### The doctor family's growth is governed by FF-5905's named roster, not by its placement
The three rules that replace the ratchet are recorded in the same item 10 amendment — placement is
decided at birth, this family's growth is governed by the named roster in
[test/arch/acd-controls-never-execute.test.mjs](test/arch/acd-controls-never-execute.test.mjs#L95),
and the root count stays item 10's own `src/mesh/` + `src/work/` partition — so the control that
actually caught the seventh lane is the one the tree relies on.

### Every in-source citation of the ratchet resolves to the refusal
[src/work-doctor-controls.mjs](src/work-doctor-controls.mjs#L1),
[src/work-doctor.mjs](src/work-doctor.mjs#L668) (the `controlsLane` entry in `CHECK_GROUPS`) and
[src/commands/audit.mjs](src/commands/audit.mjs#L10) each state the ratchet as superseded and name
where the refusal lives, and each records that 66's "4 → 5" was mismeasured at birth — 54/04's
rubric lane already made the roster six — so no reader of the doctor family meets a live gate that
was never untripped.
