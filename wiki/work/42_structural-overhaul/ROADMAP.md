# 42 · Structural overhaul — execution roadmap

The checklist view of the inline implementation (operator direction 2026-07-26: implement directly,
outside the aof agent ceremony). [STATE.md](STATE.md) is the running narrative with the full
rationale per landing; [TECH_DEBT.md](../TECH_DEBT.md) is the evidence base. Statuses here are
honest: **done** means committed + unit-verified; live-soak caveats are listed at the bottom.

---

## Wave (a) — stop the bleeding (the verification substrate)

| Leg | Status | Commits | What's left |
|---|---|---|---|
| **Item 5 — the gate gates** | ✅ DONE | `cec520b` | Arch suite at 694/0 across 219 files; dead guards retired with their subjects; bundle counts derived. *Caveat: the FULL suite still can't run on the control node while daemons hold :4182 — focused runs remain the discipline.* |
| **Item 2 — daemon log sink** | ✅ DONE | `b72da09`, `9a9cfe2` + remote commit | JSONL sink (rotating) + warning tee + build-stamped startup for both daemons; `aof mesh logs [proc] [--tail N]` locally. **Remote read DONE:** a worker's log events ride its existing stream into the control's `node_logs` ring (schema v6, T6-attributed, ring-bounded at 500/node); `aof mesh logs --node <id>` answers from the store — no SSH archaeology. `--follow` DESCOPED by decision → the deferred backlog (polling `--tail` covers the need; a follow mode is a CLI nicety, not debt). |
| **Item 3 — silent catches** | ✅ DONE | `cde6407` + sweep commit | Ratchet armed, then **the sweep landed the same day**: all 97 silent sites (the 94 baseline + 3 added since) across 28 files now report coded degrade events via `degrade.mjs` (`reportDegrade` — throttled per code, never-throwing, into the mesh-log sink family as `degrade.log`). The gate's baseline is the sanctioned floor — the reporter + the sink themselves (2 sites) — making it an outright ban everywhere else. 698/0 arch, 159/0 behavioral after the sweep. |
| **m38-F26 — temp-file leak** | ✅ DONE | `dcf04e6` | `writeText` reclaims its temp on rename failure; age-gated startup sweep over presence/ + nodes/; 48 live orphans reclaimed on the control node. |

## Wave (b) — one home, one door

| Leg | Status | Commits | What's left |
|---|---|---|---|
| **One door per act** | ✅ DONE | `42d76e2`, `44fa328`, `a23c50f` + doors commit | `work:continue`, `work:refine`, `work:verify` — ONE factory, one pure decision, one scope rule, three registered doors. Every face is transport: CLI (`aof work <phase> <ref> [--node]`), board POSTs (`/api/work/<phase>`, same-origin-guarded, explicit-allowlisted), the DetailPanel button (refine/verify no longer spawn the local dock on their own authority — the server decides running/local/remote). All registry-derived guards (route bijection, CLI bijection, write-isolation allowlist, known-ids) extended and green. |
| **Item 7 — liveness loop** | ✅ DONE (all 3 legs) | `f4c9689` + leg-2/3 commit | Leg 1: PTY liveness probe (`failed/agent_died` ≤15s after a child dies). Leg 2: worker STARTUP reclaim — persisted worktree dirs (assignment-id-named) are reported `failed/daemon-restarted` before new work; safe by the new apply-seam invariant **a terminal assignment never regresses** (stale/late worker frames refused, reportably). Leg 3: the dual-staleness reclaim was structurally dead for cross-machine runs (local-only run-record read — the control checkout has no `runs/`); it now reads the local record, else the STREAMED v5 `work_item_runs` record, else the assignment's own frozen `updatedAt` as the staleness clock — a worker dead before ever streaming is no longer un-reclaimable. *Live-drill verification still pending (see caveats).* |
| **Item 6 — board bridge** | ✅ DONE | `0f8719e` + mirror commit | Doc bodies + run records ride the projection (schema v5); drill-downs answer from the worker stream — and streamed content PERSISTS after worktree cleanup (the stretch was already covered by the upsert design). **Console leg DONE (reworked at the operator's insistence):** the board has ONE terminal surface — the dock — and a session is either LOCAL (pty, read-write) or REMOTE (the fleet's read-only mirror on fixed :4181). A running item with a captured session offers **View terminal — <node>**, which opens THE DOCK in mirror mode (read-only badge, fixed 80×24, no input path). The first attempt bolted a second terminal widget into the detail panel — the same several-surfaces disease in the UI — and was deleted. |
| **Item 4 — workspace identity** | 🟡 ONE HOME DONE | `ac361f8` + sweep commit | Gate: a launch dir can't publish itself (`mesh-workspace-unconfigured`); `prune-projection.mjs` recovers landed rows. **One home DONE:** `workspace-identity.mjs` owns the derivation + the one precedence (override → pinned config id → path); all 14 hand-spelled `?? workspaceIdFor(...)` fallbacks retired; `acd-workspace-identity-single-home` forbids the pattern re-appearing (raw derivation callable only from the home + the store's compat re-export). **Clone-time pin DONE:** a fresh checkout gets the ASSIGNMENT's canonical id written into its own config (merge-preserving; end-to-end pinned: `resolveWorkspaceId` on the checkout answers the fleet's id on every machine — kills the per-machine divergence class at the source for every FUTURE clone). **cwd-independence DONE:** every mesh verb accepts `--workspace <path|id>` (bare id resolves through the descriptor store; unknown refuses loudly) — verified live from a foreign cwd. **Migration DONE:** the Mac's pre-fix checkout repinned live (`scripts/pin-checkout-id.mjs`: measured `14d86b2b… → 1f164bd0…` — the exact recorded divergence) and its projection's 63 stale old-id rows pruned with the committed tool. **Item 4 complete** pending the Mac daemon restart (it publishes under the pinned id from then on — the `workspace-workdir-unresolvable` spam should end with it). |
| **m38-F23 + F24 — presence/descriptor truth** | ✅ DONE | F23/F24 commit | F23: `fabricLivenessFor` no longer rebuilds the record (the accidental four-key whitelist that destroyed `sessions` for every ONLINE node) — liveness now contributes exactly one fact (alive NOW) and the disk record rides through whole; regression pins sessions surviving the merge for an online node. The shape now has exactly ONE authority (`assemblePresenceRecord`) plus the deliberate wire gate (`applyPresenceFrame`); the accidental third whitelist is gone. F24: a node card's `workspaces` is projected from the MEMBERSHIP table (resolved through workspace descriptors), never the last publisher's descriptor-file stamp — the system32 advertisement class is dead. |

## Wave (c) — the honest build

| Leg | Status | Commits | What's left |
|---|---|---|---|
| **Item 1 — launcher decouple** | ✅ DONE (core) | `ad72943` | `aof.exe` is a payload-first launcher; deploy = file copy + restart; SEA only for bootstrap/release; BUILD_ID stamped + surfaced (`--version`, daemon startup, log sink); `.bak` pruning. **Remote build-id DONE:** the presence record carries `buildId` as its sixth additive key (publisher → wire gate → store → `aof mesh status` node lines show `· build payload <stamp>`) — surviving the liveness merge by construction, F23's fix proving itself on the first new key. **Left (optional):** daemons self-restart when the installed stamp changes — install becomes the whole deploy. |
| **Item 0 — the umbrella closes** | ✅ DONE (measured; convention recorded) | deletion commit | The four disease forms each have their structural cure + a gate that keeps it cured: **one home per fact** (identity: `workspace-identity.mjs` + gate; presence shape: one authority + F23 merge fix; doc set: shared const), **one door per act** (3 phase doors, one factory, faces are transport, 4 gates), **errors are events** (97-site sweep, silent-catch BAN at the 2-site sanctioned floor, sink + remote ring), **the build is honest** (payload launcher, stamps everywhere incl. remote presence). **Deletion tranche:** dead lease wire-kind surface removed (`LEASE_SIGNAL_KIND`/`leaseRelayEnvelope`/`pushLeaseSignal`); dead guards + duplicate derivations already retired in-wave. **Measured vs the baseline:** standing arch failures 10 → **0** (698 green); silent catches 43 (honest count 97) → **2** sanctioned; identity derivations 17 sites → **1 home**; scar markers 1,670 → 1,563. Lines grew (41.3k → 43.3k) — the overhaul added verification surface, and that is the point. **Recorded decision:** the remaining comment-mass reduction is an ongoing convention (scar comments retire as files are touched, with the gates preventing new debt classes), not a blocking work item — grinding 1,500 historical comments in one pass would churn every file for cosmetic gain. |

## Wave (d) — command spine & effects ledger (the cascade seams)

> Scoped 2026-07-27 from the operator's command-layer review ("side effects not happening is the
> biggest problem") + two full codebase maps. The settled design is
> [PRD-command-spine-effects-ledger.md](../../planning/PRD-command-spine-effects-ledger.md); this
> table is its milestone cut. The disease is m42's own class one level deeper: write seams have one
> home (waves (a)–(c)'s win), but **cascade seams have none** — "what must happen after X" lives
> inline at whichever call site needed it first (`completeRun`: 8 sites, exactly 1 does the status
> rollback; global publish is a per-command import decision; reindex renumbers refs that key six
> stores and tells none). Rule three: **one ledger per consequence** — effects are declared, not
> remembered.

| Leg | Status | Commits | What |
|---|---|---|---|
| **d1 — command-spine-faces** | ✅ DONE (2026-07-31) | on branch | **Landed:** the ONE generic face (`src/spine/face.mjs`: spec-driven flag parsing, registry-derived route table, one `--json` error envelope + exit-code policy, `cli.exit` findings-gate adapter, ASYNC `cli.argv` for interactive completion, post-invoke effects sweep) + **22 verbs migrated**: the full assets/packages/project families (`assets list/show/add/remove/use/unuse/validate/clean/apply`, `packages list/show/add/remove/validate/install`, `project show/validate/doctor/migrate`) plus `work:doc`/`work:tasks`/`work:run-complete`. The two big flows (`assets apply`, `packages install`) follow the pure-outcome write-verb idiom — run() executes, render reproduces the transcript byte-for-byte; install's failure summary moved stderr→stdout (pinned in `packages.feature`). Helper homes: `spine/flags.mjs`, validate-shared.mjs, formatApplyAction + friendly-action/marker/display→render-plan, packageDiagnostics/packageSummaries→packages.mjs, frameworkInstall machinery→commands/packages-install.mjs. Gates registry-derived (`acd-command-route-derived`); BDD `config-family.feature` + `command-spine.feature` + `packages.feature`. cli.mjs 3,419 → 2,472. **WAVE 2 (same day): the whole work family + top-level faces** — 21 more routed verbs (`work list/validate/next/doctor/feedback`, run verbs, `continue/refine/verify`, the insert family + `promote-gap`, `work upgrade` with the top-level spelling delegating, `import milestone`, `migrate`, the four-word notion routes); `runVerbCli`/`workInsertCli` + every work face copy deleted; faceCtx now reaches `cli.json`/`cli.exit` (doctor --strict, validate findings gate); the ONE error envelope carries the insert family's `shifted`. 43 routes derive collision-free. cli.mjs → 1,990. **WAVE 3 + ITS TAIL (2026-07-30):** graph + every registered mesh verb onto the route table; the **launcher seam** (`cli.launch`, with `--json` NEVER launching — a launcher verb's machine face is its non-blocking probe, by face policy) carrying mesh serve/ui, graph serve, work ui, assets ui; the flag-bearing nested verbs (assign / recover-push / repo publish) as Commands; the CLI-only batch. **THE d1 END-STATE (2026-07-30):** `parseOptions` + its global boolean allow-list and `printJson` DELETED — every verb's flag vocabulary is its own `cli.spec` — and `helpText()` derives its listing from the registry. **THE LAST THREE PRD ITEMS (2026-07-31):** the four upward imports inverted (`command-error.mjs`, `mesh-assignment.mjs`, `aofVersion` deleted), the one confirmed cycle broken (`mesh-repo-marker.mjs` owns the `mesh.repo` subtree both sides shared), and `console.log` confined — the printing defaults retired for `NO_PRINT`, the printer set closed and ratcheted. Gates: `acd-command-layer-imports-downward`, `acd-console-log-confined`. cli.mjs **3,419 → 647**. Full ledger: [WAVE-D-MIGRATION.md](WAVE-D-MIGRATION.md). (`work memory` + `session` stay laddered by design — declared, and their gate rows are one-way doors.) **No behaviour change; 714/714 arch, 124/124 BDD.** |
| **d2 — effects-ledger-foundation** | 🟢 CODE COMPLETE (2026-07-28) — live kill-drill owed | on branch | **Landed:** the per-node journal (`src/effects/journal.mjs` — events + per-reactor steps, one-transaction append, busy_timeout from birth), `src/effects/table.mjs` (closed vocabulary; `run.completed` → rollback-status@checkout + publish-projection@local), the dispatcher (`src/effects/dispatch.mjs`: locus-routed drain, failures → degrade + retry, per-reactor outcomes in the envelope; ephemeral fallback), `transitionRunComplete` (`src/effects/run-transitions.mjs` — the ONLY event-raiser; write-then-append) and the `work:run-complete` port (inline rollback+publish deleted; `failureReason` structural). **THE SWEEP (same day):** all 7 worker-side `completeRun` sites in `mesh-worker-execution.mjs` (withdraw ×3, the bracket settle, startup ghost reclaim, terminal-resume ×2) settle through the seam — a FAILED worker run now rolls the checkout item back via the declared reactor; `completeRun` is gate-pinned reachable only from the store + the seam (`acd-effects-ledger`, incl. THE CRASH-WINDOW PIN: transition-without-drain leaves pending steps a later drain pays — unit AND BDD). Worker suites green post-sweep (bracketing, withdraw, retention, reclaim, push, liveness, completion-detection). **Remaining: ONLY the live kill-drill on the soak** (deploy + restart, kill a worker between transition and settle → the settle lands on the next drain). |
| **d3 — bridge-facts-and-outbox** | ✅ DONE (2026-07-31) | on branch | **The guard move:** `effects/assignment-transitions.mjs` — the holder (T6) and terminal-never-regresses invariants ran at ONE of `updateAssignmentState`'s three call sites, so withdraw re-derived a weaker copy and the reclaim tick had none; they now run in front of every write, and the writer is gate-pinned reachable only from its store + the seam. `assignment.settled` carries the branch record that was inline at the frame door. **The outbox:** `effects/outbox.mjs` — a remote-locus step is already a durable journal row, so delivery is its second half: shipped as an `effect-step` frame, **delivery is not completion** (the step stays owed until the control node acks it by `(eventId, reactorKey)` — the async-RPC receipt), an offline send is not an attempt (it never burns the retry budget), and a DECIDED refusal ends the step. The bridge door is guard + append-into-control's-own-journal + drain + receipt, re-deciding nothing: a bridged fact settles through the same transition, so a non-holder is refused identically at both doors. **The measured defect dies:** the worker's nine terminal report sites + the startup-reclaim broadcast (STATE's fire-once `failed/daemon-restarted`) now report durably; posture frames stay best-effort by design. 8 outbox lanes incl. the offline→online redelivery as a test. |
| **d4 — cascade-ports** | ✅ DONE (2026-07-31, all four ports) | on branch | **PAID: the `writeLock` bypasses** — `aof init` / `project migrate` wrote the WHOLE lock document, silently deleting the `work`/`planning` sections of a workspace that had them; `mergeLock` is the read-merge home and `acd-lock-read-merged` pins it structurally + behaviourally. **PAID: publish-on-mutate is a `local`-locus reactor** — `withGlobalWorkPropagation` DELETED, so whether a mutation propagates is the ledger's decision and not a per-command import the next author must remember. Three declared events feed the ONE publish reactor: `run.started` (raised by the new `transitionRunStart`, which ALL FOUR mint sites now route through — run-start ×2, run-retry, the worker's two — so the event never lies about a mint that raised nothing), `feedback.recorded` (raised by the THIRD transition seam, `effects/doc-transitions.mjs`, which `appendFeedbackBullet` moved into so the record-doc write is unreachable without its event — the board's feedback POST inherits it), and `run.completed` (d2, unchanged). **The blocking design question, settled: the face THREADS the warnings back** — `threadPropagationWarnings(result, effects)` is the one home, run-complete's inline copy died into it, and the port is invisible on the wire (byte-identical CLI smoke, mesh-off and mesh-on; the two ported verbs deliberately did not grow an `effects` key). The sites that never published still don't (no `workspace` ⇒ null `workspaceRoot` ⇒ the reactor skips — d2's worker precedent). Gates: `acd-publish-on-mutate-ledgered` (wrapper-gone ratchet, one publish door, one reactor per propagating event, threading proven through `invoke()` with the publish failing); `acd-effects-ledger` + the mint-reachability proof; `acd-board-write-isolation` followed the writer and got stronger (face AND command now write nothing). **PAID: the two reclaim halves on ONE edge** — a reclaim IS a run completion, but neither path went through the completion seam, so the restart scan's caller looped `rollbackItemStatus` inline while the CONTROL tick kept its own copy of the reclaim edge and rolled **nothing** back (measured both ways with the src stashed: at HEAD the item stayed `in-progress`). `reclaimRun` is the one edge, `transitionRunReclaimed` the one door, and the rollback is the declared cascade of the `run.completed` a reclaim now raises. `reclaimStaleRuns` keeps its m20 signature (composed of the new pure `staleRunningRuns` + the edge). Documented behaviour changes: a control-side reclaim rolls the item back; a failing rollback degrades-and-retries instead of aborting `work:run-start`. Gates: `acd-reclaim-one-edge` (both halves behavioural + the one-home ratchet); `acd-effects-ledger` reclaim-reachability; `acd-status-rollback-bounded`, `acd-fleet-reclaim-guarded` and `acd-run-reclaim-stale-only` all FOLLOWED the shape (no run-* command calls the frontmatter writer any more). **PAID: insert/reindex emits `stream.reindexed`** — the renumber renames folders and rewrites depends/parent and stops, but a REF keys six other stores and the renumber told none: run records kept the old ref, and the Notion sidecar kept binding `03` to a page while `03` had become a DIFFERENT item, so the next sync PATCHed that page with another item's content. `effects/stream-transitions.mjs` is the FOURTH seam; the event carries the OLD → NEW map as evidence (computed PRE-rename by `buildRefRemap`, including the cascade a call-site copy would miss — a story's ref changes when its milestone moves — ordered descending so nothing writes onto a ref another has yet to vacate). Three reactors: `remap-run-refs`@checkout, `remap-notion-map`@checkout, `remap-projection`@local. Two recorded deviations from the PRD sketch: the sidecar remap is CHECKOUT not `integration:notion` (it is a local JSON file; at the integration locus the step would sit deferred forever on an ordinary CLI process and the mis-binding would outlive the port), and `publish-projection` is deliberately NOT on this event (`work_items` is rebuilt wholesale by any publish, and publishing here would publish an intermediate stream). The remap is a permutation, so it is EVENT-ID DEDUPED — the reactor contract's other sanctioned option, first needed here. Gate: `acd-stream-reindex-cascade` (4 proofs incl. the mis-binding dying end-to-end and the redelivery no-op). **PAID: Notion status sync rides the ledger, gated by applicability (port 4, `4def8d0`)** — a completion in a Notion-configured workspace owes a durable `notion-status-sync` step at `integration:notion` (forgetting to sync stops being invisible). Both blocking decisions SETTLED: **(a)** the operator's call (2026-07-31) — automatic IF the integration opts in: `work.integrations.notion.autoSync: true` lets the completion's own drain reach the integration locus (`reachableLoci`) and sync in place; without it the step stays deferred and the `sync-work` verb drains this workspace's owed steps after its own sync (additive `drained` on the envelope). Egress is sidecar-deduped (`lastStatus`/`lastContentHash`) so redelivery and repeats cost zero calls. **(b)** the APPLICABILITY PREDICATE is the new table machinery — `applies(payload, ctx)` evaluated ONCE by every transition seam at append time (`applicableReactors`); a consequence that can never apply (no Notion config; the worker's no-workspace sites) is not owed at all, closing the steps-owed-to-nobody leak. One body one door: the sync core moved to `src/notion/sync-work.mjs`, shared by verb + reactor. Two containment fixes landed with it: `integration:*` never rides the outbox (workspace-scoped, not the control bridge's business), and the unscoped crash-recovery sweep fetches only loci it can run (a deferred backlog can no longer starve its limit window). Gate: `acd-notion-sync-ledgered` (5 lanes); two BDD scenarios; `autoSync` in both schema arms. Full ledger: [WAVE-D-MIGRATION.md](WAVE-D-MIGRATION.md) §d4. |
| **d5 — fact-projection-split** | ✅ DONE (2026-07-31) | on branch (`76ffa35`) | **The classification is executable** (`src/effects/stores.mjs`): every table declared `fact \| projection \| meta`, TOTAL over the real schema by a two-way ratchet; the row publisher's sweeps route through the one class-checking guard (a fact-table sweep throws coded, before the statement); fact-table SQL gate-pinned to each table's declared writers. **The ref-remap split** (port 3's deferral): the mirrors rewrite at `local` in the CLI's own drain; the dispatch facts (`global_assignments`/`global_item_branches`) at `control-store` — predicated (mesh workspaces only, the port-4 machinery) and deduped on their OWN watermark; the `stream.reindexed` payload grew `workspaceId` so a bridged step never dereferences a foreign checkout path. **The reconciler** (`src/effects/reconcile.mjs`) closes the write-vs-append window: the eaten event is re-derived from the record (runId join, json_extract), bounded to each item's LATEST record at/after the ledger's birth. **The doors**: `aof work doctor --explain <event>` (declared cascade + journal state) and `--converge` (reconcile, drain reachable loci in bounded passes, report what's left with whose-job-it-is hints); findings lane byte-identical. Gates: `acd-fact-projection-split` (6 lanes), `acd-stream-reindex-cascade` followed the shape, `acd-effects-ledger` admits the reconciler as the deliberate second append door; two BDD scenarios. Full ledger: [WAVE-D-MIGRATION.md](WAVE-D-MIGRATION.md) §d5. |

---

## Status: WAVES (a)–(c) DONE (2026-07-26) · LIVE-SOAK FIXES ON BRANCH (2026-07-27) · WAVE (d): d1 + d3 + d4 + d5 DONE, d2 CODE-COMPLETE (live kill-drill owed — the wave's one open item) (2026-07-31)

> 2026-07-27: the first full live-soak day surfaced a family of post-overhaul defects (run
> truncation, wrong-base dispatch, ghost run records, unlogged decisions, invisible stops). All
> fixed + verified on branch `fix/worker-completion-and-milestone-cascade` (10 commits, pushed,
> UNMERGED — operator merges). Full ledger, missing tests, and residuals: [STATE.md](STATE.md)
> §"2026-07-27 — the live-soak day".

## Next work (operator-ordered, not yet built)

| Item | Status | What |
|---|---|---|
| **Interactive worker terminals** | ✅ BUILT (`5c03269`) — live drill pending | Shipped exactly to the mapped design: dock keystrokes ride the tuple-bound `/ws/terminal-view` socket → mesh-ui wraps them with THE SOCKET'S OWN tuple (content-blind, bounded) → loopback relay → serve self-subscribed router (`mesh-terminal-input.mjs`) → `terminal-input` DOWN-frame → the worker writes ONLY the exactly-matched live session (`liveSessionInputs`). Gate rewritten as `acd-fleet-terminal-input-constrained` (pins the constrained shape, incl. content-routed and first-live-PTY-fallback plants). **Plus the recorded scope addition that makes it useful:** the pending-AskUserQuestion lane now reports `code: needs-input` immediately (persisted — schema v7), keeps the session ALIVE for the answer, and parks only after the 15-min window; the board renders `Answer on <node>` + "waiting for your input". Focused 89/0, arch 698/0. **Live verification owed:** restart both machines, then answer a real pending question from the dock. |
| **Per-item branch (the brittleness cure)** | 🟡 BRANCH + BASE-COMMIT PIN DONE (`87a7f39`, `50c2c82`) — docs-to-default half awaiting the operator's mechanism decision | **DONE:** one derivable branch per item (`meshItemBranchName(ref)` → `aof/mesh/<ref>`; the per-assignment mint retired by ratchet), `global_item_branches` demoted to a CACHE that wins on hit (continuity for pre-cure suffixed names + reindexed items); the worker's fallback CONVERGES (`baseBranch ?? derivation`, existing branch takes the reuse door — a re-dispatch releases the stale holder and continues the line); control dispatch cache-only, recovery-push cache-first — the wrong-base class dies structurally even on a forgotten lookup. Gate `acd-item-branch-derivable`; the branch-not-detached suite rewritten to the convergence invariant. **PLUS the base-commit pin (operator, 2026-08-01, `50c2c82`):** the dispatch stamps the assigning checkout's HEAD onto the directive; a fresh worker worktree builds from EXACTLY that commit (fetching origin once when the never-refreshed clone lacks it), and an unbuildable base refuses coded (`assignment-base-commit-unavailable`) instead of silently building from a stale HEAD. **REMAINING half — SUPERSEDED (operator, 2026-08-01) by [43 · Mesh artifact authority](../43_milestone_mesh-artifact-authority/SPEC.md).** The open half was run-settled DOC changes landing on the default branch, so main-based reads (local continue, `work next`, validate, the board's local rows) stop lying about refined items. Put up for its mechanism decision, the investigation instead measured *why* those reads lie: `work_items` is a projection of the CONTROL node's disk, wholesale-deleted and rebuilt on every propagation tick ([global-work-store.mjs:431](../../../src/global-work-store.mjs#L431) ← [mesh-launcher.mjs:732](../../../src/mesh-launcher.mjs#L732)), so the worker's streamed rows are merged in and then rebuilt out again — and after settle the worker stops ticking forever (its worktree is deleted, [mesh-worker-execution.mjs:2664](../../../src/mesh-worker-execution.mjs#L2664)) and the stale side wins permanently. The operator's answer: **do not make the control's disk current — stop reading it.** m43 makes the SQLite cache the one read surface, fed by whichever node authored the change, and locks an assigned item against every other writer. The branch cure's landed halves (derivable branch + base-commit pin) stand and are m43's dependencies — the pin IS its control→worker propagation mechanism. |
| **Missing tests for the soak-day fixes** | ✅ PAID (2026-07-31, `4496dad`) | Eight of the nine lanes pinned (new `mesh-worker-withdraw-settle` suite + three suites extended in place); the ninth (board-UI banner/re-poll) resolved by recorded decision — a component harness deferred to the next board-UI touch (ui/ has no test infra at all). Ledger: [STATE.md](STATE.md) §MISSING TESTS. |

Waves (a)–(c)'s legs are landed, unit-verified, committed to `main`, synced to both machines and
staged in the control node's payload; wave (d) is scoped, designed, and unbuilt. Two items carry recorded scope decisions (`--follow` and the
self-restart leg → the deferred backlog in [../ROADMAP.md](../ROADMAP.md); the comment-mass
convention in item 0's row). What remains below is LIVE verification, which requires operator
restarts and a real dead-run drill — tracked as caveats, not as open work.

## Live-soak caveats (unit-green ≠ running)

- Everything above is staged in the control node's payload (`aof --version` shows the stamp); the
  **desktop-app restart** picks up sink + sweep + all wave-(a) behaviour on this machine.
- The **Mac worker restart** is the one that arms the PTY liveness probe (leg 1 runs there).
- The whole liveness loop gets its real verification the next time a run dies — the sink now records
  what happened, and the probe should flip the assignment within ~15s. Until a real (or provoked)
  dead-run drill passes on the soak, item 7 stays "unproven live".

## Suggested order for what's left

1. Merge the soak-day branch + land the nine owed test lanes ([STATE.md](STATE.md) §MISSING TESTS) —
   pin what shipped under fire before anything moves again.
2. Per-item branch (the brittleness cure, ~half-day) — kills the wrong-base dispatch class
   structurally, independent of wave (d).
3. Wave (d) in leg order d1 → d5: d1 is pure mechanics (safe to interleave with soak operation);
   d2's live kill-drill is the gate the later legs build on; d3–d5 each leave the soak running.
4. The interactive-terminal + liveness live drills (the standing caveats) whenever both machines
   next restart — unchanged by wave (d).
