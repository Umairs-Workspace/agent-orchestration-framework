# Notes (a non-PRD decoy)

> A plain `*.md` file that is **not** a PRD and does not carry the `PRD-` prefix. It exists so the
> discovery scenarios can assert that discovery does NOT auto-select an arbitrary markdown file when
> no `PRD-*.md` is present — it asks instead (ADR-005: never guess among other `*.md`). Not a product
> artifact; do not shatter it.

Random working notes. No objective, no scope, no milestone chunks here.
