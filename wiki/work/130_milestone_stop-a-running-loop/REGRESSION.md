<!-- The regression gate's evidence (96/ADR-008). Appended by the gate command; never hand-written. -->
# Regression gate

## Gate runs

One row per gate run, appended by `aof work regression-gate <ref>`. A rerun APPENDS: the newest row is the one
the accept door reads, and the earlier rows are the milestone's history. A row whose commit,
instant, scope or result is missing makes this document UNREADABLE rather than green — repair it
by hand rather than deleting the row, because a row nobody can read and a gate nobody ran are the
same fact. A `override` row is a recorded reason for accepting WITHOUT a green gate
(ADR-008 §4), never a gate result.

| commit | instant | scope | result | detail |
|---|---|---|---|---|
| 03a271a86feed357d42cd7c779f7b9712abec84e | 2026-09-24T00:56:17.245Z | all | red | arch/FF-5407: the deterministic engines stay pure and gain no runner — no spawn, no dynamic import(), and no edge to the grade modules, arch/53 FF-5312: source, installed dogfood and retired work-stream home are exact, arch/53 FF-5313: every delivered loop record is installed in aof's own registry, byte-identical, FF-6302/7 the eol RATCHET: every .aof/** asset the bundle declares is pinned eol=lf, and its two copies agree byte for byte, arch/124/02 FF-12405 leg 9 (task 03): the manifest is a true content address, and it agrees with the lock on every path they both name, arch/96/02 FF-9603 (2) THE BAN IS A PROPERTY OF THE STREAM — every PLAN.md under wiki/work is admitted, not only the template, 69/04 task 00 wiring: the production work:dispatch command sends a multi-ref ready set through the bounded pool, verification-template: `src/bundle/templates/milestone/` holds exactly the templates shipped before, plus VERIFICATION.md (OUTCOME.md left for the type-agnostic `shared` member at story 80), verification-template (install): `aof work init` on a repo with no ACD install creates the template alongside the other milestone templates, 96/02-00 the ban is a property of the stream, not of one file — every PLAN.md in the work tree is admitted, work/archive-is-a-move: 00 the verb is registered with its own flags, and every hand-kept ledger learned it consciously, work/this-tree-holds-what-is-live: 00 validate and doctor from the repository root are no redder than before the write — no finding outside the pre-existing stale-reads class, no error-level finding, work/this-tree-holds-what-is-live: 02 the root of wiki/work holds only live items, the archive holds only done drivers, list --all carries every archived row last with archived: true, and 42_structural-overhaul is not an item |
| d5f5d4400316362b69090da97feb6b5aa6ccfbdb | 2026-09-24T09:27:40.757Z | all | red | arch/96/02 FF-9603 (2) THE BAN IS A PROPERTY OF THE STREAM — every PLAN.md under wiki/work is admitted, not only the template, verification-template: `src/bundle/templates/milestone/` holds exactly the templates shipped before, plus VERIFICATION.md (OUTCOME.md left for the type-agnostic `shared` member at story 80), verification-template (install): `aof work init` on a repo with no ACD install creates the template alongside the other milestone templates, 96/02-00 the ban is a property of the stream, not of one file — every PLAN.md in the work tree is admitted, work/archive-is-a-move: 00 the verb is registered with its own flags, and every hand-kept ledger learned it consciously, work/this-tree-holds-what-is-live: 00 validate and doctor from the repository root are no redder than before the write — no finding outside the pre-existing stale-reads class, no error-level finding, work/this-tree-holds-what-is-live: 02 the root of wiki/work holds only live items, the archive holds only done drivers, list --all carries every archived row last with archived: true, and 42_structural-overhaul is not an item |
