<!-- The regression gate's evidence (96/ADR-008). Appended by the gate command; never hand-written. -->
# Regression gate

## Gate runs

One row per gate run, appended by `aof work regression-gate <ref>`. A rerun APPENDS: the newest row is the one
the accept door reads, and the earlier rows are the milestone's history. A row whose commit,
instant, scope or result is missing makes this document UNREADABLE rather than green — repair it
by hand rather than deleting the row, because a row nobody can read and a gate nobody ran are the
same fact. A `override` row is a recorded reason for accepting WITHOUT a green gate
(ADR-008 §4), never a gate result.

| commit | instant | scope | result | detail |
|---|---|---|---|---|
| ea7092052777b8a92020d01d47af15dc80a51ce2 | 2026-09-16T20:19:05.834Z | all | red | arch/119 FF-11901 · 121: NO control under test/arch/ spells an import-specifier extractor of its own — the one home is test/support/module-family.mjs, and the remainder is a named, shrink-only baseline, arch/119 FF-11902: NO control narrows a repository walk by a filename PREDICATE without a non-vacuity leg — a move REDS a control, it never empties one, arch/119 FF-11902: NO control retypes a member census — a derived set is never asserted equal to a string-array literal; a home is named AMONG the set, under a floor and a declared ceiling, arch/119 FF-11903: one resolver answers every citation — at HEAD, or through a rename this repository recorded, arch/119 FF-11903: the map is DERIVED and is asserted to be non-empty — an empty map must not read as a pass, arch/119 FF-11903: the citation sweep is bounded by a shrink-only ceiling pinned to a measured count, arch/53 FF-5307 (acd-loop-state-rides-the-run-record): frozen store and board read surfaces remain byte-identical to the milestone base, arch/96/02 FF-9603 (2) THE BAN IS A PROPERTY OF THE STREAM — every PLAN.md under wiki/work is admitted, not only the template, arch/53 FF-5301 (acd-session-driver-mesh-blind): frozen direct imports, lifecycle denylist, named admissions, and reach ceilings stay exact, arch/53 FF-5302 (acd-session-driver-single-home): the driver export set is exactly the frozen seventeen and the sink re-exports every binding by identity, arch/53 FF-7002 (acd-session-driver-single-home, extended): the driver imports the pure phase-brief leaf WITHOUT re-exporting it, the leaf is pure, and the driver's export set stays the frozen seventeen, arch/FF-6607b: LEG B is live — every control of this milestone that HAS landed is named by a runner, arch/FF-6607b: ADR-011/E — NO `done` item's register declares an UNRESOLVED control, marker or no marker (the accept-transition refusal), arch/FF-6607b: the THREE controls this story lands resolve under both legs, here and now, 119/00 task02 — one resolver answers every citation: two ways to resolve, and four ways not to, 119/00 task02 — the map is DERIVED from history: the command edge spawns, the resolver stays pure, and a repository with no history yields an empty map rather than an error, 81/01 the record itself is unchanged, and the machine face still carries the whole truth, loops-ledger/05 leg 9 the records name suites that exist: VERIFICATION.md and STATE.md, 129/04 task01 REFINE drives every unrefined story first, in stream order, then commits the loop's own writes, 53/00 task00 — a door does not invent names: a name neither module owns reads undefined at both, in the same check that reads a frozen one defined at both, 53/00 task01 — only the closed named set under test/ may name agent-session-driver; the 48 census members split as 44 zero-name importers plus four named re-aimed gates, shell/12 a caller that does NOT ask for host nodes gets exactly today's behaviour — a `{ current }` the renderer does not assign, an effect that early-returns, and no socket — and the diff adds no call site that asks on behalf of a suite that did not, 70/05 task02 the declared-ADR path is exercised by a real story, not by emptiness — at least one real story in the stream declares its ADRs, that story's brief carries those rather than the milestone's register, and the guard fails if no story in the stream declares any, 70/05 task02 outline what every real item's brief must satisfy (6 rows: a story with contracts at continue and at verify; a story whose milestone records architecture; a story that declares its ADRs at refine; a milestone at refine; any item at every phase), 70/05 task02 outline the cases fixtures were never shaped to cover (7 rows: contracts many times the ceiling; contracts past the ceiling even condensed; contracts absent; declared architecture absent; the register absent; a milestone's own spec many times over; every section within but the sum far past), 96/02-00 the ban is a property of the stream, not of one file — every PLAN.md in the work tree is admitted, 66/00 refuse: over the whole real stream the ONLY new finding is the one live file, and no file that parses gains one |
| 049c7fd0cacf6ca08173a5c284ac6a23dd3731a8 | 2026-09-17T10:32:50.746Z | all | red | arch/126/01 FF-12603 leg 6: 53/FF-5307's pin MOVED for this file and for no other, and it carries its reason, arch/47 F-47-04-ARCH-2 (acd-test-suite-registration): no fitness function grows a NEW positional slice over source text — a fixed character window, or a slice whose end is a second indexOf sentinel; the surviving instances are ledgered and may only shrink, 70/05 task02 the declared-ADR path is exercised by a real story, not by emptiness — at least one real story in the stream declares its ADRs, that story's brief carries those rather than the milestone's register, and the guard fails if no story in the stream declares any, 70/05 task02 outline what every real item's brief must satisfy (6 rows: a story with contracts at continue and at verify; a story whose milestone records architecture; a story that declares its ADRs at refine; a milestone at refine; any item at every phase) |
| 9cea4746ff1e75f0f59ba40db572d821cd4894e6 | 2026-09-17T11:30:01.597Z | all | red | work/this-tree-holds-what-is-live: 02 the root of wiki/work holds only live items, the archive holds only done drivers, list --all carries every archived row last with archived: true, and 42_structural-overhaul is not an item |
| c801091b0e173c6531d9876ff320e0304be82a89 | 2026-09-17T12:19:02.808Z | all | red | the runner exited with no verdict and enumerated no failure |
| f27d7d669e00b94d790cb6182d985a7ab2c8325e | 2026-09-17T13:10:03.279Z | all | green | — |
