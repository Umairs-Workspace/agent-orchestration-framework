---
doc: verification
---
<!--
  Milestone VERIFICATION.md — answers ONE question: what evidence do we have that this milestone
  works? Owner: aof:verify (the product owner is the SINGLE WRITER of the findings register).
  Scaffolded at refine so the declared controls have a home for their red probes; the evidence rows
  are filled as each story lands, and the accept decision at the close.
-->
# 72 · The inner loop — Verification

## Fitness functions

<!-- CITING register (66/ADR-008 ruling 4): every row resolves to a declaration in the sibling
     ARCHITECTURE.md `## Fitness functions` register and declares nothing itself. The red-probe cell
     records WHAT WAS CHANGED to make the control fail and THE MESSAGE OBSERVED.

     A control must fail when the invariant it guards is broken, so the probe is that assertion's
     positive control, and this row is where a reviewer reads that it was observed failing. An
     assertion nobody has ever seen red cannot be falsified by its own green.

     An untouched placeholder cell is a MISSING red probe, not a recorded one. Every cell below is
     `pending` at refine and must be FILLED before accept, each probe applied to the working tree,
     run, REVERTED, and the restore confirmed byte-identical before the next probe begins.

     ONE ROW CARRIES MORE WEIGHT THAN THE OTHERS AND ITS PROBE IS THE ONLY EVIDENCE IT IS ARMED:
       · FF-7207 is GREEN ON ARRIVAL over a tree that has never held the defect (ADR-007's own
         consequence — it is a RATCHET, not a fix). Its probe is therefore a PLANTED link-creating
         call under `src/`, not a repaired defect, exactly as 63/ADR-015 §2's ratchets are probed.
         Its subject is the hazard TECH_DEBT item 36 measured twice in four hours.

     TWO ROWS ARE NEGATIVE-SPACE CENSUSES and their probes must plant the thing they forbid, not
     remove a thing they require: FF-7201 (no program name spelled in `src/`, no second spawn seam)
     and FF-7204 (no door consumes a selection). A census green over a tree that never held the
     literal proves nothing until the literal has been planted and seen to red.

     DELIBERATELY NOT RESTATED HERE, because a guard already in service walks the whole subject and
     would already fail on the breach: 59/FF-5903 (a suite imported and never spread is not
     registered), 59/FF-5904 (the audit family's own closure), 59/FF-5911 (no eighth core command
     declares --strict), 70/ADR-007's budget controls. Each is named in the ADR whose invariant it
     discharges; none is a gap. -->

| id | enforced by | result | red probe |
|---|---|---|---|
| FF-7201 | `test/arch/acd-declared-program-single-speller.test.mjs` | **green** — 9/9, 2026-09-03 (census scope resolved to `src/work-toolchain.mjs` + `src/work-test-select.mjs`; `src/commands/test.mjs` not yet on disk, non-vacuity floor of ≥1 cleared). **Milestone gate, 2026-09-03:** `MILESTONE_MODULES` re-derived once, over every module 72 adds — `src/work-test-changed.mjs` added as the fourth member (`:64-69`, F-72-AK) — and re-run with all four on disk: **9/9 green**. | THREE probes, one per census clause, each applied to the working tree, run in a fresh `node` process, reverted, and the restore confirmed byte-identical (`src/work-toolchain.mjs` sha256 `20bf9d8f…71c91c` before and after every probe). **(a) The program-name census** — changed `command: step.program` to `command: "npm"` at `src/work-toolchain.mjs:483`, the command handed to the seam. RED: *"src/work-toolchain.mjs spells the program name \"npm\" in an executable position — the declaration is the only speller of the program, and a default that lives as text is a default waiting for a caller. Read it from work.test instead."* **(b) The one-seam census** — planted `import { execSync } from "node:child_process";` at `src/work-toolchain.mjs:59`. RED: *"src/work-toolchain.mjs imports node:child_process directly — every child process in this family comes from src/work-audit/spawn.mjs, because a bound one caller can route around is not a bound."* **(c) The one-reader census** — added `src/probe-second-reader.mjs` returning `config.work.test.command`. RED: *"src/probe-second-reader.mjs reads work.test.command, and src/work-toolchain.mjs is the one home for it — two readers of one key are two answers that agree until they do not."* Each probe reddened its own clause and left the other two green, so the three clauses are separately armed rather than one assertion wearing three names. |
| FF-7202 | `test/arch/acd-test-selection-widens-never-narrows.test.mjs` | **green** — 6/6, 2026-09-03 (censused subject resolved to `src/work-test-select.mjs` + `src/graph-impact.mjs`, module floor 2 cleared; the clock clause additionally walks `src/work-test-changed.mjs` and `src/commands/graph-impact.mjs`, and the one-seam clause walks the git reader so the split is a placement and not an exemption) | FIVE probes, one per clause, each applied to the working tree, run in a fresh `node` process, reverted, and the restore confirmed byte-identical (`src/work-test-select.mjs` sha256 `74872e5c…34688f` before and after every probe). **(a) The route census** — planted `const ARTIFACT = "graphify-out/graph.json";` at `src/work-test-select.mjs:50`. RED: *"the graph is READ, never built, and only through the shared reader: src/work-test-select.mjs holds a second read of the graph artifact file — the graph artifact is reached ONLY through the shared normalisation and impact functions, and a build is minutes even when nothing changed."* The same plant ALSO reported *"holds an invocation of the graph build"*, because the build detector's `\bgraphify\b` token matches the artifact directory `graphify-out/`; the red is correct and the second line's wording is not, recorded as **F-72-AL**. **(b) The four reasons, driven positively** — dropped the presence half of presence-outranks-the-union at `:183`, `if (entry == null || !entry.present)` → `if (entry == null)`. RED: *"not-in-graph: …carrying this reason — actual `'no-registered-dependent'`, expected `'not-in-graph'`"* — precisely the silent mis-classification ADR-002 §2a exists to forbid, and the one an implementation that checks the suite predicate first would ship. **(c) `builtAt` is the artifact's instant or exactly `null`** — returned `new Date().toISOString()` in place of `null` on the `graph-unreadable` path at `:161`. RED: *"graph-unreadable: exactly null, the mtime deliberately discarded — actual `'2026-09-03T02:33:56.706Z'`, expected `null`"*. **(d) The suppressor clause** — added `noWiden = false` to `selectSuites`'s destructured parameter list at `:125`. RED: *"the selector accepts no option that suppresses, narrows or overrides a widening — it accepts \"noWiden\""*. **(e) Purity** — hoisted the edge walk behind a cross-call cache keyed on the changed set at `:169`, which is the optimisation ADR-002 prices and forbids by name. RED: *"graph A answers A — actual `['test/a.test.mjs','test/arch/c.test.mjs','test/b.test.mjs']`"*. Probes (a)–(d) each reddened their own clause and left the other four green; (e) reddened purity AND the positive-drive row, because a cache that survives a call also poisons the answers the other rows read — a cross-call cache is not separable from what it corrupts, and that is the finding rather than a defect in the probe. |
| FF-7203 | `test/arch/acd-suite-registration-single-decider.test.mjs` | **green** — 4/4, 2026-09-03 (the story's module set `src/work-test-select.mjs` + `src/graph-impact.mjs` + `src/work-test-changed.mjs`, all three read; the decider's own answer obtained directly from `registrationDecision` and compared against the report over identical inputs) | THREE probes, one per clause, the same discipline and the same `src/work-test-select.mjs` sha256 `74872e5c…34688f` before and after each. **(a) The import clause** — replaced the `registrationDecision` import at `:58` with a local re-derivation over `suiteNames`/`assembled`. RED: *"the decision comes from the shared census module, by import"* — and the vocabulary leg went red with it (*"with the census's own code — actual `'unregistered'`, expected `'audit-suite-unregistered'`"*), which is exactly what a second decider costs the moment it exists. **(b) The no-second-derivation census** — planted `const UNREGISTERED_BASELINE = [{ suite: "test/work-observe.test.mjs", reason: "carried" }];` at `:81`. RED: *"src/work-test-select.mjs holds a second baseline of unregistered suites, which duplicates UNREGISTERED_BASELINE (src/work-audit/census.mjs), the shrink-only ledger with one home. There is one decider and 72 does not author a second."* **(c) The verdict vocabulary** — KEPT the import and re-phrased the carried message at `:277` to `` `${file} is not registered — running it tells you nothing about CI.` ``. RED: *"…and the census's own message, verbatim"*, the diff showing the re-phrasing against the census's own *"is registered by no runner — 1 of the 1 tests it exports are absent from the assembled suite, so it is green, red or deleted with identical effect on CI."* Probe (c) is deliberately probe (a) minus the removal of the import, so a re-phrasing that keeps the reuse is armed SEPARATELY from a re-derivation that drops it — the two-assertion split this control was written around. |
| FF-7204 | `test/arch/acd-test-command-reports-not-decides.test.mjs` | **green** — 7/7, 2026-09-03 (all nine probed modules on disk, floor 4 cleared; the door census read `src/commands/item-status.mjs`, `src/work-doctor.mjs`, `src/work-loop.mjs`, `src/work-audit/**` and `src/bundle/**`; re-run over the restored tree after the five red probes and green again) | FIVE probes, one per clause, each applied to the working tree, the control run in a fresh `node` process, reverted, and the restore confirmed byte-identical (`src/commands/test.mjs` sha256 `189220cf…f6f3b9` before and after (b)–(e); `src/work-loop.mjs` `76b5a107…02873d` for (a)). **(a) The door census** — appended `export async function plantedGate(ctx) { return await invoke("test", { scope: "all" }, ctx); }` to `src/work-loop.mjs`. RED: *"no status, accept, merge, loop or audit door reads a test selection as a verdict: src/work-loop.mjs carries invoke("test") — a transition door that can read a test selection is a gate consuming a report, and 09/ADR-004 is held structurally here rather than by convention."* **(b) The fresh-process probe** — planted a STATIC `import "../../test/support/source-slice.mjs";` at `src/commands/test.mjs:1`. RED: *"src/commands/test.mjs resolved 1 specifier(s) under a declared test root — importing project test code EXECUTES it: file:///C:/Source/umami/aof/test/support/source-slice.mjs"* — seen by the fresh process and invisible to the text census (which matches `import(`/`require(` only), which is the two-sided proof's whole point. **(c) The gate rule** — `gate: scope === "all" && widened.length === 0` → `gate: scope === "all"`. RED: *"…and still may NOT stand as a verdict (no-graph) — true !== false"*. **(d) Both streams** — the report text reduced to stdout alone. RED: *"the failure was read at all — 0 !== 1"*: the `not ok - beta` fixture on stderr vanished, the confident "no failures" over a red run that ADR-003 §1 forbids. **(e) The session launch** — added `launch: async () => {}` to the registered command's `cli`. RED: *"the test command declares NO session launch — it runs a bounded child and returns — 'function' !== 'undefined'"*. Each probe reddened its own row and left the other six green. |
| FF-7205 | `test/arch/acd-session-verb-boots-no-registry.test.mjs` | **green** — 4/4, 2026-09-03 (`src/cli.mjs`'s and `src/commands/mesh-session.mjs`'s closures both walked through the shipped `importClosure`, floors of 5 cleared with the named members present; re-run over the restored tree after the five red probes and green again) | FIVE probes, the same discipline: `src/commands/mesh-session.mjs` sha256 `9f49fdb7…69f5e0`, `src/cli.mjs` `d2edd492…1bbaec` and the control `4619d138…33690b` each confirmed before and after. **(a) THE RATCHET, session half** — planted `import { getCommand as plantedGetCommand } from "../command-core.mjs";` at the top of `src/commands/mesh-session.mjs`. RED, two rows: *"src/command-core.mjs is absent from src/cli.mjs's static closure — importing it pulls all 88 command modules onto a path that touches none of them — true !== false"* (the CLI assertion runs first, and `src/cli.mjs` statically imports the session module, so one plant reached both closures) and the lazy-path row — *"no module on the session path defers the registry rather than dropping it: src/commands/loop.mjs … doctor.mjs … tune.mjs … trigger.mjs reaches the command registry through a dynamic import"* — four registry-reaching command modules pulled onto the session path by the plant, the walk showing its reach rather than a defect in the probe. **(b) The CLI closure** — the same static import planted into `src/cli.mjs`. RED: *"src/command-core.mjs is absent from src/cli.mjs's static closure …"*. **(c) The session-arm position** — `const { listCommands: plantedList } = await import("./command-core.mjs");` inserted as the FIRST statement of `run()`. RED: *"src/cli.mjs reaches the registry (an import of the registry) before the session arm dispatches — a presence ping that fires on every prompt would pay the whole command surface for a code path that touches none of it."* **(d) The lazy path** — a `plantedLazy()` holding `await import("../command-core.mjs")` appended to the session module. RED: *"src/commands/mesh-session.mjs reaches the command registry through a dynamic import — a lazy path that awaits the registry on the session hot path costs the same 88 modules as a static one, and satisfies a closure walk while doing it."* **(e) No wall clock** — a `Date.now()` reading appended to the control's own source. RED: *"a clock reading (`Date.now`) appears — this control asserts no wall-clock duration, because a timing leg reds on a slow machine and proves nothing structural."* |
| FF-7206 | `test/arch/acd-managed-hook-not-duplicated.test.mjs` | **green** — 4/4, 2026-09-03 (six entries read, managed and unmanaged both present; the `Bash\|PowerShell` guard admitted with a managed neighbour planted beside it; re-run over the restored tree after the four red probes and green again) | FOUR probes: `.claude/settings.json` sha256 `56294620…ab27fd` before and after (a)–(c), the control `1a21e325…2ba343` for (d). **(a) The HEAD state restored** — the hand-authored `SessionStart` block put back beside the managed one, exactly what this story removed. RED, three rows: *"SessionStart (matcher ""): an unmanaged entry resolves to the same invocation as the managed `claude-session-start` — "aof session start". It is the HAND-AUTHORED block that goes: delete the marked one instead and the next `aof work update` restores it beside the survivor, and the duplication is back."*, the converse row re-running the same census, and *"exactly one SessionStart entry invokes `aof session start` — it fired twice on every prompt before this story — 2 !== 1"*. **(b) The wrong survivor** — `aofManaged` stripped from the sole `SessionStart` entry. RED: *"…and the survivor is the one aof manages (`claude-session-start`): deleting the marked one instead would let the next `aof work update` restore it beside the survivor — null !== 'claude-session-start'"* (the planted-duplicate self-check, which needs a managed entry to pair against, went red with it). **(c) The converse** — the operator's guard entry replaced by a different unmanaged hook under the same matcher. RED: *"the file carries an UNMANAGED entry invoking guard-test-isolation.mjs — the hook aof does not manage, and the reason the merge's escape hatch exists"*. **(d) The merge reach** — `import * as plantedMerge from "../../src/claude-settings.mjs"` added to the control. RED: *"this control imports nothing from claude-settings.mjs — true !== false"*. |
| FF-7207 | `test/arch/acd-worktree-never-linked.test.mjs` | **green** — 5/5, 2026-09-03 (`src/` walked, the 80-module floor cleared; zero link-creating calls and no worktree-derived recursive delete over the shipped tree — the ratchet's green, believed only after probe (a); re-run over the restored tree after the five red probes and green again) | FIVE probes: `src/mesh-worktree.mjs` sha256 `b98c4345…8d357c` before and after (c) and (d), the control `999544c9…4250c6` for (e); (a) and (b) created files, each confirmed ABSENT after. **(a) THE `@manual` RATCHET PROBE (72/04 `tasks/01`)** — planted `src/planted-ratchet-probe.mjs` calling `symlink(shared, meshWorktreePath(projectRoot, assignmentId) + "/node_modules", "junction")`, TECH_DEBT item 36's exact shape. RED: *"aof creates no link into a worktree: src/planted-ratchet-probe.mjs calls `symlink(` with a path or target inside a worktree — a link into a worktree is what a forced removal follows out of it, and TECH_DEBT item 36 is what that costs."* The plant removed, the control passed again 5/5. **(b) The delete census** — planted `src/planted-delete-probe.mjs` with `rm(meshWorktreePath(root, id), { recursive: true, force: true })`. RED: *"no aof path recursively deletes a worktree: src/planted-delete-probe.mjs calls `rm(` recursively over a path derived from the worktree seam — removal is git's (`git worktree remove`), never a filesystem delete: a recursive delete is what follows a junction, and git's own verb does not."* **(c) The module's own reach** — an `rmSync(` call appended to `src/mesh-worktree.mjs`. RED: *"the worktree module reaches no filesystem delete at all: rmSync"*. **(d) The bare root** — the `!== path.resolve(root(projectRoot))` half dropped from `isInsideMeshWorktree`. RED: *"the worktrees root itself, with no keyed child beneath it, is not a worktree — true !== false"* — the row F-72-Y was written for. **(e) Root derived, not literal** — the literal `".aof/mesh/worktrees"` planted in the control. RED: *"this control spells no worktree path literal of its own: .aof/mesh/worktrees"*. Each probe reddened its own row and left the other four green. |

**Probe procedure.** Each probe is applied to the working tree, the control run in a FRESH `node`
process, the message recorded verbatim with its source line, then REVERTED and the restore confirmed
byte-identical (sha256 before and after) before the next probe begins.

## Evidence

<!-- One row per verification claim. Filled as each story lands; the milestone gate rows at accept. -->

| subject | kind | how | result | traces to |
|---|---|---|---|---|
| 72/00 `tasks/00` + `tasks/01` | `@executable` | both task features, run through `test/work-toolchain-declaration.test.mjs`'s exported array in a fresh `node` process, each test under its own `AOF_GLOBAL_HOME` (the runner's own isolation) — never `node --test`, which reports a silent false pass over these arrays | 2026-09-03: **12/12 green**, 0 failures. Covers every Examples row: the four resolution forms, the four absence shapes, the ten `work.test` field faults, the six `work.worktree.prepare` field faults, the three unresolvable shapes, the three-distinct-codes claim, and the whole of `tasks/01` — the one seam, the `{file}` rule over zero/one/three files, and the four seam outcomes. | `verifies →` `tasks/00`, `tasks/01` |
| 72/00 (FF-7201) | fitness | `test/arch/acd-declared-program-single-speller.test.mjs`, run as its own focused lane in a fresh process | 2026-09-03: **9/9 green**, 0 failures — re-run over the restored tree after the three red probes and green again. | `verifies →` FF-7201 |
| 72/00 | registration | the two suites imported AND spread in `scripts/test.mjs`'s registry | 2026-09-03: imported at `scripts/test.mjs:3219-3220` under the milestone-72/story-00 labelled block, spread at `:3438-3439`. Both halves present, so neither suite is a file on disk the assembled runner never reaches. | `verifies →` 59/FF-5903 |
| 72/00 | regression | the tree-wide guards this story's new files fall under — `acd-roundtrip-registration`, `acd-test-suite-registration`, `acd-loop-suite-registration` (REG-MUT-11's frozen runner region included) and `acd-audit-never-imports-project-code` (59/FF-5904) — run as one focused lane | 2026-09-03: **23/23 green**, 0 failures. This is the narrowest lane containing the guards a new `src/` module and a new labelled runner block can red; the whole-repo sweep is the MILESTONE gate's, not this story's. | — |
| 72/00 | non-vacuity | this repository's own `work.test` declaration, compiled by the shipped resolver against the real `.aof/aof.config.json` | 2026-09-03: compiles. `command: "node"`, `args: ["scripts/test.mjs"]`, `selectArgs: ["--only", "{file}"]`, `roots: ["test"]`, `deadlineMs: 900000`, `report.format: "tap"`. The declaration is an **interpreter plus a script**, never a `.cmd` shim — which is the shape ADR-001's no-shell rule leaves available on the Windows control node, and the shape the refusal message tells an operator to use. `--only` is not parsed by `scripts/test.mjs` until 72/02; that is the expected gap this story's Notes name, not a defect. | `verifies →` `tasks/00` |
| 72/00 | gate | `aof work validate 72/00` | 2026-09-03: **PASS — 72/00 is well-formed.** | — |
| 72/00 | gate | `aof work doctor 72/00` | 2026-09-03: **no `control-unresolved` finding at either severity** — FF-7201's file resolves on disk, so the accept is not standing on a `pending` marker. Two warns, neither a 72/00 acceptance gate: `numbering-gap` (stream-wide top-level driver numbering) and `rubric-join-unchecked` (no `work.rubric.report` configured). Loop-Ready 80% (8/10), clears L1. | — |

| 72/01 `tasks/00` + `tasks/01` | `@executable` | both task features, run through `test/work-test-select.test.mjs`'s exported array in a fresh `node` process, each test under its own `AOF_GLOBAL_HOME` — never `node --test`, which reports a silent false pass over these arrays | 2026-09-03: **16/16 green**, 0 failures. Covers every Examples row of both features: the four widening reasons driven positively, the four-reason vocabulary, the two-sided rule, the four suite-selection rows of the union-before-predicate outline, presence-outranks-the-union as its own row, the four `builtAt` paths, purity in both orders, the three `--since` bases, the empty-changed-set refusal, the porcelain reader's untracked/rename handling, and the whole of `tasks/01` — the five registration states, the three census codes, and a changed non-suite file reported as neither. | `verifies →` `tasks/00`, `tasks/01` |
| 72/01 (FF-7202) | fitness | `test/arch/acd-test-selection-widens-never-narrows.test.mjs`, run as its own focused lane in a fresh process | 2026-09-03: **6/6 green**, 0 failures — re-run over the restored tree after the five red probes and green again. | `verifies →` FF-7202 |
| 72/01 (FF-7203) | fitness | `test/arch/acd-suite-registration-single-decider.test.mjs`, run as its own focused lane in a fresh process | 2026-09-03: **4/4 green**, 0 failures — re-run over the restored tree after the three red probes and green again. | `verifies →` FF-7203 |
| 72/01 | registration | the three suites imported AND spread in `scripts/test.mjs`'s registry | 2026-09-03: imported at `scripts/test.mjs:3231-3233` under the milestone-72/story-01 labelled block, spread at `:3441-3443`. Both halves present for all three, so none is a file on disk the assembled runner never reaches. | `verifies →` 59/FF-5903 |
| 72/01 | regression | the guards this story's changes fall under, run as one focused lane: the layer gate `acd-command-layer-imports-downward` (the control the `computeImpact` move exists to keep green), both frozen graph-reader allowlists `acd-codebase-grounding-no-parse` and `acd-codebase-grounding-via-commands` (edited by this story), the three registration guards `acd-roundtrip-registration` / `acd-test-suite-registration` / `acd-loop-suite-registration` (REG-MUT-11's frozen runner region included), `acd-audit-never-imports-project-code` (59/FF-5904), and `test/graph-impact.test.mjs` — the behavioural suite that proves the re-export left the shipped `graph:impact` byte-equivalent | 2026-09-03: **40/40 green**, 0 failures. This is the narrowest lane containing every control F-72-U named as red-on-arrival; the whole-repo sweep is the milestone gate's, not this story's. | `verifies →` F-72-U, ADR-002 §1 |
| 72/01 | cross-lane | `test/arch/acd-declared-program-single-speller.test.mjs` (72/00's FF-7201) re-run with 72/01's modules on disk — the story's own stated risk, that a stage-1 story reds a sibling's control from a parallel lane | 2026-09-03: **9/9 green**, 0 failures. `src/work-test-select.mjs` reads no `work.test.*` key (`roots` is a parameter, ADR-002 §1b), and `src/work-test-changed.mjs` spells only `git`, which is not one of the five names FF-7201 freezes. | `verifies →` FF-7201, ADR-002 §1b |
| 72/01 | gate | `aof work validate 72/01` | 2026-09-03: **PASS — 72/01 is well-formed.** | — |
| 72/01 | gate | `aof work doctor 72/01` | 2026-09-03: **no `control-unresolved` finding at either severity** — both FF-7202's and FF-7203's files resolve on disk, so the accept is not standing on a `pending` marker. Two warns, neither a 72/01 acceptance gate and both identical to 72/00's: `numbering-gap` (stream-wide top-level driver numbering) and `rubric-join-unchecked` (no `work.rubric.report` configured). Loop-Ready 80% (8/10), clears L1. | — |

| 72/02 `tasks/00` + `tasks/01` + `tasks/02` | `@executable` | all three task features, run through the runner's own `--only` path — `node scripts/test.mjs --only test/test-command-contract.test.mjs test/arch/acd-test-command-reports-not-decides.test.mjs test/command-core-contract.test.mjs test/arch/acd-loop-suite-registration.test.mjs`, a fresh process under an isolated `AOF_GLOBAL_HOME`, each test under its own global home through the ONE execution loop — the selection path this story ships, exercised as its own evidence | 2026-09-03: **57/57 green**, 0 failures across the lane (the contract suite, FF-7204, the command-core contract, and REG-MUT-11 over the runner as left). Covers every Examples row: the three closed scope forms and the fourth refused, the four refusal inputs, the six outcome/face mixes, verbose containment, the six one-object fields, the five gate rows, the door census with its planted invocation, the fresh-process leak with its warmed-cache contrast, the session-launch contrast, and the whole of `tasks/02` — the seven `--only` shapes, the array-mutation fixture, one loop, isolation driven, and the importer's argv. | `verifies →` `tasks/00`, `tasks/01`, `tasks/02` |
| 72/02 | live face | `aof test` run from this tree (`aof --version` → `0.1.0 (source deb34a58+dirty)`, the npm-linked source, so the uncommitted command is what ran): `--scope file` over two suites; the same with `--json`; `--scope sweep`; `--scope file` with no file; `--scope file … --verbose` | 2026-09-03: `--scope file` printed no passing row and one summary line — `2/964 suites · scope file · graph built unknown · no widening · may not stand as a verdict`, exit 0. `--json` carried `askedScope: file`, `scope: file`, `gate: false`, `launched: true`, `total: 964`, `widened: []`, `builtAt: null`, the runner envelope (`outcome: exited`, exit 0, `deadlineMs: 900000`, attempted `"…\node.EXE" scripts/test.mjs --only …` — the PATHEXT-resolved program), 4 cases, 0 failures, `contradiction: false`. `--scope sweep` → `not ok - aof test refused: test-scope-unrecognised`, the message naming the three forms, `0/0 suites · scope none`, exit 1. A file scope with no file → `test-file-scope-names-no-file`, exit 1. `--verbose` printed the four `ok -` rows plus the identical summary line. **`--scope impacted` and `--scope all` were deliberately NOT run live**: on this working tree (untracked files) `impacted` widens to `all`, and `all` launches the whole suite including the `:4182` binder this machine cannot run — the widening and gate paths are driven by the contract suite and FF-7204 against a stub seam. | `verifies →` `tasks/00`, ADR-003 |
| 72/02 (FF-7204) | fitness | `test/arch/acd-test-command-reports-not-decides.test.mjs`, in the lane above and re-run alone over the restored tree after the five red probes | 2026-09-03: **7/7 green**, 0 failures, before the probes and after the restore. | `verifies →` FF-7204 |
| 72/02 | ADR-004 §5 discriminator | `git diff -U0 -- test/arch/acd-loop-suite-registration.test.mjs` | 2026-09-03: **exactly one changed line, `:269`** — `RUNNER_RESIDUE` `8cd5945f…` → `d7c50c16…`. No `from:` closure, `RUNNER_LOOP_HEADER`, plant or `syntheticSuite()` touched, so the re-derivation lane was never left and no cut-body census is owed. The control is green over the runner as left (lane above). | `verifies →` ADR-004 §5, F-72-AD |
| 72/02 | registration | the two suites imported AND spread in `scripts/test.mjs` | 2026-09-03: imported at `scripts/test.mjs:3247-3248` under the milestone-72/story-02 labelled block, spread at `:3488-3489`. | `verifies →` 59/FF-5903 |
| 72/02 | gate | `aof work validate 72/02` | 2026-09-03: **PASS — 72/02 is well-formed.** | — |
| 72/02 | gate | `aof work doctor 72/02` | 2026-09-03: **no `control-unresolved` finding at either severity** — FF-7204's file is on disk. Two warns, `numbering-gap` and `rubric-join-unchecked`, neither a 72/02 gate. Loop-Ready 80% (8/10), clears L1. | — |

| 72/03 `tasks/00` + `tasks/01` | `@executable` | both task features through `--only`: `test/cli-session-boot-closure.test.mjs`, the two controls, and the delivered `test/bundle-claude-session-hooks.test.mjs` this story repairs (ADR-005 §5) — fresh process, isolated | 2026-09-03: **23/23 green**, 0 failures. Covers the closure matrix (four absences, three presences), transitivity against the planted fixture, the five lazy-import placements, the session-arm position, the three verbs' behaviour and their six refusals, the three timing spellings; and the duplicate census with its planted duplicate, the ten equivalence/distinct rows, the operator's guard planted-and-required-green with a managed neighbour, the merge-reach self-check, and one session entry per event with the managed survivor. | `verifies →` `tasks/00`, `tasks/01` |
| 72/03 (FF-7205) | fitness | its control, in the lane and re-run alone over the restored tree after the five red probes | 2026-09-03: **4/4 green** both times. | `verifies →` FF-7205 |
| 72/03 (FF-7206) | fitness | its control, in the lane and re-run alone over the restored tree after the four red probes | 2026-09-03: **4/4 green** both times. | `verifies →` FF-7206 |
| 72/03 | at source | `git show HEAD:.claude/settings.json` against the working tree, counting `"command": "aof session …"` entries | 2026-09-03: **6 at HEAD → 3 now**; each survivor carries its `aofManaged` marker; the unmanaged `Bash\|PowerShell` guard entry is byte-unchanged. | `verifies →` ADR-005 §3 |
| 72/03 | registration | the three suites imported AND spread in `scripts/test.mjs` | 2026-09-03: imported at `scripts/test.mjs:3260-3262` under the milestone-72/story-03 labelled block, spread at `:3491-3493`. | `verifies →` 59/FF-5903 |
| 72/03 | gate | `aof work validate 72/03` | 2026-09-03: **PASS — 72/03 is well-formed.** | — |
| 72/03 | gate | `aof work doctor 72/03` | 2026-09-03: **no `control-unresolved` finding at either severity** — both controls' files are on disk. The same two warns as every story. Loop-Ready 80% (8/10), clears L1. | — |

| 72/04 `tasks/00` + `tasks/01` (`@executable`) | `@executable` | `node scripts/test.mjs --only test/mesh-worktree-prepare.test.mjs test/arch/acd-worktree-never-linked.test.mjs`, fresh process, isolated | 2026-09-03: **10/10 green**, 0 failures. Covers the prepare step inside the tree after creation, through the seam with the declared deadline, the six declaration shapes, the four run outcomes with three distinct codes; every link-creating form with its path or target inside a tree plus the admitted call, the root derived not literal, the four classification rows, the seven delete rows, and forced and unforced removal through git. | `verifies →` `tasks/00`, `tasks/01` |
| 72/04 `tasks/01` — "the ratchet is seen red before its green is believed" | `@manual` | run inline at this gate, not delegated: the control green over the tree as it stands (5/5); `src/planted-ratchet-probe.mjs` planted with `symlink(shared, meshWorktreePath(projectRoot, assignmentId) + "/node_modules", "junction")`; the control run in a fresh process; the plant removed and confirmed absent; the control run again | 2026-09-03: RED — *"aof creates no link into a worktree: src/planted-ratchet-probe.mjs calls `symlink(` with a path or target inside a worktree — a link into a worktree is what a forced removal follows out of it, and TECH_DEBT item 36 is what that costs."* — naming the planted module and the call; then **5/5 green** again with the plant gone. Recorded against FF-7207 in the register above. The developer ran the same probe at build (`STATE.md` feedback); this row is verify's own run. | `verifies →` `tasks/01` `@manual`, FF-7207 |
| 72/04 (FF-7207) | fitness | its control, in the lane and re-run alone over the restored tree after the five red probes | 2026-09-03: **5/5 green** both times. | `verifies →` FF-7207 |
| 72/04 | frozen counters | the two pins the story's review found ADR-007 had not priced — `acd-session-driver-mesh-blind` (assignment-sink reach, exact 68) and `acd-no-new-silent-catch` (`mesh-worktree.mjs` baseline 0) — in the gate's guard lane | 2026-09-03: green (in the 95/95 lane below): the compiler is reached by dynamic import and the teardown reports on the degrade channel, so neither counter moved. | `verifies →` ADR-007, ADR-006 §5 |
| 72/04 | registration | the two suites imported AND spread in `scripts/test.mjs` | 2026-09-03: imported at `scripts/test.mjs:3275-3276` under the milestone-72/story-04 labelled block, spread at `:3495-3496`. | `verifies →` 59/FF-5903 |
| 72/04 | gate | `aof work validate 72/04` | 2026-09-03: **PASS — 72/04 is well-formed.** | — |
| 72/04 | gate | `aof work doctor 72/04` | 2026-09-03: **no `control-unresolved` finding at either severity** — FF-7207's file is on disk. The same two warns. Loop-Ready 80% (8/10), clears L1. | — |

| 72 (gate) | regression | the guards every 72 change falls under, as one `--only` lane in a fresh process: FF-7201 (re-derived), FF-7202, FF-7203, the 72/00 and 72/01 behavioural suites, the layer gate `acd-command-layer-imports-downward`, the three registration guards `acd-test-suite-registration` / `acd-roundtrip-registration` / `acd-loop-suite-registration`, 59/FF-5904 `acd-audit-never-imports-project-code`, `acd-no-new-silent-catch`, `acd-session-driver-mesh-blind`, `acd-observation-census-filtered`, `acd-cli-entry-executes`, both frozen graph-reader allowlists, `test/graph-impact.test.mjs`, `test/repo-test-isolation-guard.test.mjs` | 2026-09-03: **95/95 green**, 0 failures, with F-72-AK's re-derivation applied. | `verifies →` F-72-AK, ADR-002 §1, ADR-007 |
| 72 (gate) | whole-repo sweep | the runner's REGISTERED array (`scripts/test.mjs` `tests`, the exact assembled set) minus the one suite this machine cannot run — `test/global-work-propagation.test.mjs` binds `:4182`, which the live control daemon holds — run once, in a fresh process, through a scratchpad driver mirroring the runner's own per-test isolation loop (the runner exposes no exclusion argv, and `--only` over 949 paths exceeds the Windows argv limit); the integration, cargo and shell lanes are the whole-suite lanes and were not run | 2026-09-03: **8,489 ok / 7 not ok** of 8,496 registered cases (6 excluded), 1,696 s. **Every failure named and attributed, none of them 72's code:** (1) `arch/58 FF-5809` — `test/arch/acd-prompt-bounds-name-their-home.test.mjs` unclassified in the registry-reach lane, a file committed at 71's close (`0d8d1005`) and unmodified here — **chore 93** (and chore 88's first item); (2–4) `70/05 task02` ×3 — the brief compiler over 72's records, **F-72-AS → chore 95**; (5) `61/03 task 02` (`test/acceptor-admissibility.test.mjs:589`) — the `harnessRefusal` lift, **chore 88**'s second item (71/F-71-H); (6) `arch/61 FF-6109` (`test/arch/acd-progress-ledger-consumed.test.mjs:451`) — **chore 92**; (7) `arch/43 ADR-005 acd-cache-read-surface-boundary` — *"src\commands\promote-gap-to-chore.mjs no longer declares defaultAt() — the structural read this pin protects has MOVED; re-point the pin at its new home"*, a milestone-43 control unmodified here — **chore 91** (and chore 88's first item). Six of the seven were red at 71's gate on the same day (71's STATE closure record attributes "three to 72's refine" — those are (2–4)); the seventh is the same inherited class. Every 72 suite in the array is green, and the 95/95 guard lane above is the per-item reading 71/R7 requires beside this count. | `verifies →` the milestone gate; 71/R7 |

## Findings

<!-- The product owner is the SINGLE WRITER of this register. One row per finding, id `F-72-<letter>`.

     Every finding below was raised at REFINE, by the QA and developer passes of the Three Amigos,
     against the ARCHITECTURE rather than against code — there is no code yet. They are recorded
     because a design gap caught before a build is the cheapest one this project ever catches, and
     because SEVEN of them would have produced a story that could not go green by any conforming
     implementation. The disposition column is the amendment that closed each.

     ALL findings are CLOSED as of 2026-09-02, across three architect amendment rounds
     (`ARCHITECTURE.md` 587 → 974 lines against the 1400 budget, `aof work doctor 72` errors: 0).
     Nothing here is deferred into the build except the two items the architect names as the ones to
     watch, both now written into the document rather than left to judgment:

       · **ADR-004 §5 is the one place a green control can hide a disarmed one.** Re-anchoring the
         cut after extracting the loop silently mis-cuts one of four regions. The discriminator is
         mechanical: THE DIFF OF `test/arch/acd-loop-suite-registration.test.mjs` MUST BE EXACTLY ONE
         CHANGED LINE, `:269`. Anything else demands a printed cut-body census.
       · **ADR-003's stderr clause is the correctness item to verify FIRST in 72/02's evidence.** A
         filter that reads only stdout prints a confident "no failures" over a red run, and every
         other test in that story would still pass. -->

| id | raised by | severity | finding | disposition |
|---|---|---|---|---|
| **F-72-A** | QA (72/02) | **Blocker** | No conforming implementation of ADR-004 §1 or §2 leaves `REG-MUT-11` green. `runnerPins` (`test/arch/acd-loop-suite-registration.test.mjs:286-307`) digests every runner line that is not a registration import, not a spread inside the array, and not a comment or blank, so **the argv reader alone moves `RUNNER_RESIDUE`**; and the loop region is cut at `suite.indexOf(RUNNER_LOOP_HEADER)` inside `functionBody(text, "async function runSuite(")`, so extracting the loop makes that cut miss. The control was in no story's `files:` and unnamed in ADR-008 §2. | **Closed** — ADR-004 gains §5: 72/02 re-derives the four region pins and `RUNNER_RESIDUE`, **weakening no leg** (region set, `residueFloor = 40`, integration-lane count, both registration patterns, resolve-on-disk). The control is on 72/02's `files:` and sole-writer row. The review owes an explicit before/after **leg census**, because re-deriving a pin and deleting a leg look identical in a diff. |
| **F-72-B** | QA (72/04) | **Blocker** | ADR-007 §1 said *"after `addWorktree` succeeds"* and named one of four worktree-materialisation doors — not the common one. `src/mesh-worker-execution.mjs:1592` branches on `reuseDoor = baseBranch != null \|\| branchExists`, so a **continuing** assignment takes `reuseWorktreeOnBranch`, which resolves a new `meshWorktreePath` for the new `assignmentId` and materialises a fresh EMPTY tree. No prepare would have run on it — the agent pays the install on every dispatch after the first, which is the story's whole subject. | **Closed** — §1 rewritten around a four-door table (`addWorktree :490`, `reuseWorktreeOnBranch :552`, `addSessionWorktree :528`, `addDispatchWorktree :263`), stating the rule **over the path each door returns** rather than over four call sites, so a fifth door inherits it or fails FF-7207. All four are in `src/mesh-worktree.mjs`; 72/04's write set is unchanged and ADR-006 §5 still holds. |
| **F-72-C** | QA (72/01) | Important | ADR-002 §3's selected set `dependents ∩ suites` defeated the story's own value proposition. Measured: `aof graph impact test/doctor-context-budget.test.mjs` → `imported/called by ← (1) scripts/test.mjs` — the runner, which is not a registered suite. So a changed test file had an EMPTY intersection, reason `no-registered-dependent` fired, and **editing one test file — the most common inner-loop action there is — widened to all 950 suites.** The invariant would have survived; the tool would not. | **Closed** — ADR-002 gains §2a: the selected set is `({changed file} ∪ dependents) ∩ suites`, union FIRST and intersection LAST, so a changed file that is itself a registered suite is in its own selection. Encoded as a four-row outline in `tasks/00`. |
| **F-72-D** | QA (72/01) | Important | An unresolvable `--since <rev>` had no stated outcome. It cannot be a fifth widening reason without contradicting "four, exhaustively", and must not read as an empty changed set — the maximal silent narrowing §3 forbids. | **Closed** — ADR-002 §5: a coded refusal (`since-rev-unresolvable`). The four widening reasons are aof failing to know something about the tree; this is the operator naming something that does not exist. |
| **F-72-E** | QA (72/01) | Important | FF-7202's suppressor clause was worded over a surface 72/01 does not have — *"asserted over the command's declared flags"*, but the story ships no command. ADR-008 §4 exists precisely so no stage-1 story declares a control it cannot clear (62/ADR-013 §7). | **Closed** — re-worded to the selection function's accepted option keys, driven by passing an unknown suppressing key; the flag half explicitly rides FF-7204 on 72/02. |
| **F-72-F** | QA (72/01) | Nit | FF-7202's *"`builtAt` present on every result"* is unachievable on two of the four widening paths: with `no-graph` or `graph-unreadable` there is no artifact to take an instant from, and the row's own "a `new Date()` anywhere in the family fails" correctly blocks filling the hole with a clock. | **Closed** — the row now reads "the artifact's instant **or exactly `null`**", with the reason carried in `widened[]`. The no-clock clause is what makes `null` the only honest value rather than a hole. |
| **F-72-G** | QA (72/03) | Important | FF-7206 paired candidate duplicates by EVENT only; matcher appeared nowhere. In an installed repo where aof manages an entry whose command matches an operator's hand-authored one under a **different** matcher, the event-only rule pairs them and reds on the operator's own hook — exactly the outcome ADR-005 §3 refuses. Does not bite this repo (all three local pairs share `matcher: ""`), which is why it would have shipped. | **Closed** — pairing is now *same event **and** same matcher*, with the reasoning written from the installed case since the local one cannot exercise the difference. |
| **F-72-H** | QA (72/03) | Important | Half of FF-7205 is GREEN ON ARRIVAL and was not declared a ratchet. Measured: `src/commands/mesh-session.mjs`'s static closure is **27 modules** and already contains neither `src/command-core.mjs` nor `src/spine/face.mjs`; `src/cli.mjs`'s is **277** and contains both. Two of the four closure rows pass with no change to the tree, and ADR-007 sets this milestone's own precedent that such a control owes a planted probe. | **Closed** — the session-module half is declared a ratchet and owes a planted red probe in this register. |
| **F-72-I** | QA (72/03) | Important | FF-7205 stated only ABSENCES, and an absence over an empty walk is vacuous: a closure walker with a broken resolver — wrong path join, unhandled `export … from`, a Windows separator — returns an empty set and passes all four rows. | **Closed** — each closure must positively contain a known member and clear a size floor, plus a planted fixture with a known-exact closure. QA encoded the two-sided `present in` rows in `tasks/00`. |
| **F-72-J** | QA (72/04) | Important | FF-7207's census was scoped to one of three worktree roots. `meshSessionWorktreesRoot` (`src/mesh-worktree.mjs:149`) and `meshDispatchWorktreesRoot` (`:228`) hold real worktrees and carry the identical TECH_DEBT-36 hazard — and the original incident was in a scratch worktree under **none** of the three, which is the argument for deriving rather than enumerating. | **Closed** — FF-7207 covers all three roots through their own exported `isUnder…` predicates, with the incident's own location recorded as the reason. |
| **F-72-K** | QA (72/04) | Important | ADR-007 covered *absent* (silent no-op, §1) and *failed* (loud, §4) but not **present-but-malformed** — the gap where a typo becomes an invisible non-install. | **Closed** — ADR-007 §5: a present declaration that does not compile is ADR-001 §2's coded refusal, raised at COMPILE time in `src/work-toolchain.mjs`, distinct from absent. Absent and malformed must not render identically (63/ADR-010 §7). |
| **F-72-L** | QA (72/02) | Correction | The digest hazard ADR-004's Context and the story narratives were built on is **stale**. `REGISTRATION_SPREAD` (`test/arch/acd-loop-suite-registration.test.mjs:271`) is `/^\s*\.\.\.[A-Za-z_$][\w$]*,?$/u` — the trailing comma is **optional** — and measured, appending after the terminal comma-less row leaves the residue and all four region pins unchanged. `scripts/test.mjs:4497` still asserts a trailing comma is required. **F-54-00-4 looks landed and its record is stale.** | **Recorded, not fixed here.** ADR-004's Context now states that the RULE (no story appends to, reorders or restructures the array) outlives its rationale, and that what actually binds is §5's pins. The stale comment at `scripts/test.mjs:4497` belongs in 72/02's own diff and was deliberately not edited at refine. |
| **F-72-M** | QA (72/04) | Important | A pre-existing defect outside 72's scope. `src/mesh-worker-execution.mjs:696` passes `meshWorktreesRoot` to `buildAskpassShim`; `:610` creates `<worktreesRoot>/.askpass/<uuid>`; `:635` cleans only the uuid child, so `<worktreesRoot>/.askpass/` persists. `listStrandedWorktreeAssignments` (`:317-338`) has no dotfile filter, so `src/mesh-launcher.mjs:1833-1852` emits a `startup-reclaim` warning and `reportAssignmentSettled({ assignmentId: ".askpass", state: "failed", code: "daemon-restarted" })` **through the durable outbox, redelivered until acked, on every worker restart.** | **Routed out** — `TECH_DEBT.md` item 88, framed on the finding that makes it worth writing: **TECH_DEBT 47 is CLOSED (2026-08-15, 52/05) and this is its species returning through a producer that fix did not cover**, 52/05 having scoped the suites rather than made the keyspace discriminate. 72/04 may not edit that file (ADR-006 §5). It is also why FF-7207 pins the worktrees ROOT itself as **not** a worktree — a census cut at the root would be red on arrival at `:635`. |
| **F-72-AC** | developer (72/02) | **Blocker** | **`--only` still pays the whole tree's static import, so the tool loses to the chore it was built to replace — in that chore's most common case.** Measured at HEAD: importing `scripts/test.mjs` **without running it** costs **6,520 ms**, resolves **8,920 specifiers** (1,589 under `test/`) and assembles **8,401 entries**, because the imports at `:1-3203` are static and ADR-004 §1 forbids restructuring them. The chore this story deletes — an agent importing one array — costs **139 ms**. So `aof test --scope file <one suite>` is ~0.6 s of aof boot and spawn **plus 6.5 s before the first assertion**: a **47× fixed-cost regression** on the single most common inner-loop action. 72/02's own premise is that a tool replacing a chore must beat the chore at the chore's own job. | **Closed — ADR-004 §6. The framework stands; the cost is routed, not ruled away.** The architect re-measured independently (8,401 entries, **3,376 ms warm / 6,520 ms cold**; one suite **18 ms warm / 139 ms cold**) and recorded both figures, since neither alone is the truth. **Both proposed fixes are refused, and it is the same refusal twice:** dynamic imports in the runner destroy the registration model outright — `runnerImportedSuites` (`census.mjs:348`) and `REGISTRATION_IMPORT` (`acd-loop-suite-registration.test.mjs:270`) both key on the static `import { … } from "../test/…"` form, so **59/FF-5903's whole authority evaporates**; and a thin second entry needs the loop outside `scripts/test.mjs`, which is §5's measured mis-cut, or a second copy of the isolation loop, which §2 exists to forbid. **The 47× is measured against the wrong denominator:** the chore costs 139 ms *plus the model turn that writes it*, which is why the corpus holds 805/4,950 scratchpad writes and the same command re-run 33×, 31×, 30×. This milestone's own research puts model generation at **84.1%** of agent-active time against 15.9% all-tool wait, so trading ~5 s on the 15.9% side to remove a turn from the 84.1% side is the trade 72 was scheduled to make. The cost is routed to **TECH_DEBT 86**, and ADR-001 §2 is already the escape hatch — the runner is project-declared, so this repo may later declare a cheaper selection entry with no framework change. |
| **F-72-AD** | developer (72/02) | **Blocker** | **ADR-004 §5's "re-derive the pins and nothing else" is unsatisfiable under a literal extraction, and the failure is silent.** Built and run against the real validator: extracting the loop yields `NOT FOUND — the suite loop … owns no braced body`; **re-anchoring the cut at the extracted function cuts three regions correctly and mis-cuts the fourth**, returning `dir } = await import("node:os");` — 30 characters of an unrelated import — because `runTests`'s body has no `if (previousInProcess === undefined)`, so `indexOf` returns `-1` and `-1 + 36` lands mid-line. A developer who "re-derives the pin" there ships a **green control whose integration-lane leg measures nothing**, invisible in a diff. | **Closed in the contract, routed to the architect for §5's wording.** The sanctioned arrangement is to **parameterise `runSuite` in place** — `(tests, { lanes = true } = {})` with the parameter named `tests` so the loop header stays byte-identical, and an early `if (!lanes) return failures;` before the integration lane. Measured: **all four region pins identical**, lane count 1 → 1, specifiers 948 → 948, `RUNNER_RESIDUE` the single value that changes (70 → 113 lines). **The review's discriminator, now in 72/02's Notes: the control's diff must be exactly ONE changed line, `:269`.** Anything else demands a printed cut-body census. |
| **F-72-AE** | developer (72/02) | Important | **FF-7204's door census is red on arrival and its token does not match the command.** The census token is `test:` while the command id is `test`; and a raw-token census fails today — `test:` appears at `src/work-audit/census.mjs:104` (prose, `npm run test:smoke:cli`) and the bare `"test"` literal at `:119`, `:159`, `:326`, `:458` (the suite root path). All five are legitimate. | **Closed 2026-09-02 in the architect's amendment rounds.** FF-7204's own wording saves it — *"the token `test:` **as a command id**"* — so it must match **invocation shapes** (`invoke("…")`, `route: ["test"…]`, a ladder `command === "…"`), never a raw substring, and be a pure function over supplied sources so the planted-invocation row drives without editing a real door. Free consequence recorded: the id must **not** be `work:test`, since both `work:*` controls filter on `id.startsWith("work:")` and a `work:` id would demand a served `/api/work/test` route. |
| **F-72-AF** | developer (72/02) | Important | **Two ADR sentences do not survive contact.** (a) ADR-004 §1 says to reuse *"the same runner-shaped detection `src/work-audit-probe.mjs:36-50` already uses"* — but that module **exports nothing and ends in `await main()`** (`:78`), so importing it *runs the probe*; and as written the detection admits an entry with **no callable `run`** (`:38-40` tests only that `name` is a string). (b) **ADR-001 §2's `work.test` declares no output grammar** while its own sibling does (`work.rubric.report.format: "tap"`, read at `src/work-doctor.mjs:488`), so ADR-003's failures-only face either hard-codes TAP — the "works in one repo and lies in every other" shape ADR-001 opens by refusing — or needs a `format` key in 72/00's declaration. | **Closed 2026-09-02 in the architect's amendment rounds.** (a) becomes "the same SHAPE, re-implemented", with a callable `run` required. (b) needs a ruling; if it is a key, ADR-001 §2 must say so while 72/00 is still open to contract it. |
| **F-72-AG** | developer (72/02) | Important | **The failures-only filter must read stderr, or it prints "no failures" over a red run.** `ok -` goes to stdout; `not ok -` and the stack go to **stderr** (`scripts/test.mjs:4539` vs `:4542-4543`). `src/work-grade.mjs:459-465` already records that a stdout-only capture of a failing run reads all-green. Two related costs: **a TAP parser already exists** — `normaliseTap`/`normaliseReport` (`src/work-grade.mjs:137`, `:262`) parse `ok -`/`not ok -` and attach a failure's diagnostic verbatim, which is precisely ADR-003's contract, so a second one is the duplication species FF-7203 indicts one file over; and **there is no streaming** — `runBounded` accumulates into strings and resolves on `close`, so `--scope all` prints nothing for the whole run then prints everything. | **Closed in 72/02's Notes, ADR-003 wording routed to the architect.** Also recorded there: the `--only` argv reader must **dispatch inside the `invokedDirectly` guard** (`scripts/test.mjs:4607-4613`) — `assembledSuite()` spawns the probe, which imports the runner with the runner's own path as `argv[2]`, so a top-level dispatch would run all 8,401 cases inside a census. |
| **F-72-AH** | developer (72/02) | Nit | **`tasks/02`'s array-mutation scenario would red on its own siblings.** Built as a live pin over this repo's real assembled array, it fails on every sibling story's labelled block — 72/00, /01, /03 and /04 each append spreads inside those brackets, and none of the five is that array's sole writer. | **Closed by the PO** — the scenario now compares a runner **fixture**, with the last row applying this story's own transform to that fixture, and an added step requiring that nothing in the comparison read this repository's own runner. |
| **F-72-U** | developer (72/01) | **Blocker** | **Three shipped, registered, currently-green controls red on arrival** against the module 72/01's `files:` declares (verified 9/9 `ok`, 0 failures at HEAD before the finding was written). `src/work-test-select.mjs` must import `computeImpact`, which lives at `src/commands/graph-impact.mjs:38`. (1) `test/arch/acd-command-layer-imports-downward.test.mjs:63-80` forbids any `src/*.mjs` statically importing `./commands/*`, exempting only `cli.mjs` and `command-core.mjs` — that import IS the forbidden edge, and the gate's sanctioned `await import()` escape is shut by FF-7204. (2) `acd-codebase-grounding-no-parse.test.mjs:206-238` and (3) `acd-codebase-grounding-via-commands.test.mjs:168-181` fail any module reaching `graph-normalize.mjs` outside their frozen allowlists. | **Closed 2026-09-02 in the architect's amendment rounds.** The remedy is the one the layer gate's own message prescribes (`:78-80`): *"Move the shared thing below `commands/` — the command keeps its verb, the core moves down."* Measured blast radius: `computeImpact` has exactly **two** importers (the command, and `test/graph-impact.test.mjs:7`). Re-export from the command for behaviour preservation and add the new home plus `src/work-test-select.mjs` to both frozen allowlists. Four files join 72/01's write set. `matchFile` (`src/commands/graph-impact.mjs:47-48`) is declared and never used — not carried through. |
| **F-72-V** | developer (72/01) | **Blocker** | **`∩ suites` is undefined, and the registered reading reintroduces the exact lie the story exists to prevent.** If `suites` means census-**registered** suites, the set comes from `assembledSuite()` (`src/work-audit/census.mjs:394-430`), a child importing 948 test modules — **measured 3.5 s** — and when that child fails the census answers `audit-runtime-membership-unavailable` (`:478-486`), which is **not one of the four widening reasons**. So *"no registered suite among the dependents"* and *"I could not learn what is registered"* render identically. | **Closed 2026-09-02 in the architect's amendment rounds** — define `suites` as a **path predicate** (a suite file under a declared test root). Registration stays a per-selected-file REPORT (feature 01), never an input to selection. Drops selection from ~3.6 s to ~100 ms and removes the un-nameable fifth failure. Consequence: `roots` is a **parameter**, never a config read, or the selector reds FF-7201's one-reader census from a parallel lane. |
| **F-72-W** | developer (72/01) | **Blocker** | **The registration decider has no honest input under this milestone's own rules.** `registrationDecision` requires `suiteNames`, a `Map<file, exported test names>`, whose only shipped producer is `await import()` of each suite file in the runner's own process (`test/arch/acd-test-suite-registration.test.mjs:206-217`) — forbidden by ADR-001 §4 and FF-7204. `assembledSuite`'s probe returns names with **no file provenance** (ADR-004's own opening says so), and `runCensus` avoids the decider entirely, re-deriving text-level at `census.mjs:493-518` — **which is the second derivation FF-7203 forbids**. | **Closed 2026-09-02 in the architect's amendment rounds** — `src/work-test-select.mjs` **accepts `assembled` and `suiteNames` as injected inputs and produces neither**; provenance is 72/02's. FF-7203's reuse claim becomes "imports the decider and calls it with what it was given", which the story can clear. Without it, 72/02 inherits a problem whose only honest runtime answer is `audit-runtime-membership-unavailable` on every selected file. **Also: FF-7203 names the wrong helper** — the classification input is `importedBy`, from `runnerImportedSuites` (`census.mjs:348-355`), not `runnerBindings`/`runnerSpreadNames`. |
| **F-72-X** | developer (72/01) | Important | **FF-7202's no-spawn clause reds on arrival if walked as a closure.** `census.mjs:49` imports `runBounded`, and `spawn.mjs:34` imports `node:child_process`, so the selector's static closure holds it. It would be wrong regardless: ADR-002 §5 requires a real git child through `runBounded`. Related gaps: **presence must outrank the union** (a changed file that IS a suite but is `present: false` — a test file created this turn — is selected by union-first and widened by §3, and an implementation checking the suite predicate first passes every existing row); **an empty changed set** (clean tree, just committed) selects nothing, which the changed-set outline forbids and no widening reason covers; and **ADR-008 §3 calls 72/01 "a pure function over a graph and a changed-file list"**, which is wrong — no changed-set reader exists in `src/` to reuse, so 72/01 authors a bounded git reader from scratch with a real two-commit fixture. | **Closed 2026-09-02 in the architect's amendment rounds.** Scope the clause to a **text census over the story's own files** (FF-7201's vocabulary). I added the presence-outranks-union row to `tasks/00`. The empty-changed-set answer and the re-pricing of ADR-008 §3 are the architect's. Measured ceiling to record: `computeImpact` is O(paths × (nodes + edges)) — ~80 ms for one changed file, ~400 ms for fifty — and the obvious fix is what the purity clause forbids. |
| **F-72-Y** | developer (72/04) | **Blocker** | **The root-vs-worktree boundary FF-7207 relies on does not hold today — measured by executing the predicates.** All three `isUnder…Root` functions return **`true` for the root itself**, because `src/mesh-worktree.mjs:440-442` compares `path.resolve(root) + path.sep` against the same for the candidate and the two strings are equal. So FF-7207's *"through their own exported `isUnder…` predicates"* cannot express *"the worktrees root itself is not a worktree"* — the row QA pinned precisely so the census would not red on `src/mesh-worker-execution.mjs:635`, a correct recursive delete under the root in a file 72/04 may not edit. | **Closed 2026-09-02 in the architect's amendment rounds.** 72/04 exports a **new composed classifier** (`under(root, p) && resolve(p) !== resolve(root)`), leaving all three shipped predicates byte-unchanged — they have 8 `src/` callers plus an arch control pinning the import shape (`test/arch/acd-observation-census-filtered.test.mjs:113`). FF-7207 should read *"a derived classifier composed over the exported predicates"*, and **the delete census must classify by derivation from the keyed seam, not by resolving paths.** Recorded in 72/04's Notes. |
| **F-72-Z** | developer (72/04) | **Blocker** | **A failed prepare is silently swallowed on two of the three lanes.** `src/mesh-session-spawn-handler.mjs:149` and `src/work-dispatch.mjs:212` both return the worktree when `existsSync(worktreePath)` — the deliberate lost-the-concurrent-add-race reader. A prepare that throws **after** `git worktree add` created the directory therefore hands back a **half-installed tree reported as ready**: exactly the guard-if-present-green species ADR-007 §4 forbids, in two files 72/04 cannot edit. | **Closed 2026-09-02 in the architect's amendment rounds for the §4 clause; the fix is inside the write set** — on prepare failure, `git worktree remove --force` the tree it just created, then throw. The module already owns that verb (`:719-728`), `existsSync` is then false, and the throw stands in all three lanes. It trades away the retain-for-inspection artifact, so the prepare's stdout/stderr must ride the thrown message. Recorded in 72/04's Notes. |
| **F-72-AA** | developer (72/04) | Important | **A cross-story gap: 72/04 tests a compiler nobody is contracted to build.** ADR-007 §5 puts the `work.worktree.prepare` compiler in `src/work-toolchain.mjs` and ADR-008 §2 makes 72/00 its sole writer — but 72/00's contract compiles and refuses `work.test.*` only, with the prepare key appearing in exactly one row (its one-reader census). | **Closed by the PO.** Added to `00/tasks/00`: an Examples block over `work.worktree.prepare.{command,args,deadlineMs}` and a scenario pinning that the two declarations **differ in ABSENCE** (test runner absent = refusal; prepare absent = silent no-op) **and agree in FAULT**. 72/00 is `not-started`, so its contract was still open. Also recorded: declare the key in this repo's own config, or 72/04 ships a feature this repo never exercises. |
| **F-72-AB** | developer (72/04) | Important | **ADR-007's "one install per worktree" is really one install per ASSIGNMENT, forever, on the dominant path.** `reuseWorktreeOnBranch:579-581` force-removes any worktree holding the branch before adding, so the previous assignment's prepared tree is **destroyed** to make the new empty one. The door named "reuse" reuses the **branch**, never the install. In a milestone whose subject is loop cost, the stated consequence understates the frequency. | **Closed 2026-09-02 in the architect's amendment rounds** to restate the trade at its real frequency. The trade is still worth making — worker wall-clock for agent tokens — but it recurs on every dispatch rather than once per worktree. Recorded in 72/04's Notes. |
| **F-72-Q** | developer (72/00) | Important | **ADR-001 §5's premise about the spawn seam is factually wrong at HEAD**, and it was repeated in `STORY.md` and the task narrative. The ADR says the door *"refuses a `command` that is not an existing file"*. `argumentVectorProblem` (`src/work-audit/spawn.mjs:98-113`) conjoins two conditions at `:102` — `SHELL_SHAPED.test(command) && !exists(command)` — and the file documents the consequence at `:79-81`: *"A bare `node` resolved through PATH carries no control character and never reaches condition 2."* Driven through the real seam: bare `node` → `exited`/0/`v22.22.2`; bare `npm` → `not-started`, `spawn npm ENOENT`. | **Closed** — the story and the feature narrative now state it correctly: the door refuses a shell string and lets a bare name through to the OS, so a missing program becomes a failure far from its cause; the resolver goes in front so it becomes a coded refusal naming the declaration, and the door is left exactly as strict as it was. **The decision is unchanged; only its stated reason was wrong.** No scenario depended on the false claim. ADR-001 §5 is routed to the architect. |
| **F-72-R** | developer (72/00) | Important | **On Windows, four of the five program names FF-7201 freezes are undeclarable.** Node 22, post-CVE-2024-27980, throws `EINVAL` on a batch file spawned without `shell: true`; `runBounded` catches it (`spawn.mjs:169-178`) and reports `not-started`. `npm`, `npx`, `yarn`, `pnpm` and `vitest` all ship as `.cmd` shims on Windows, so ADR-001's Consequences claim — *"works in any repo that declares a runner"* — is Windows-false. | **Closed in the contract, routed to the architect for the ADR.** The unresolvable/not-started refusal must name the remedy: declare an executable and its script (`node` + `path/to/runner.mjs`), not a `.cmd` shim — which is exactly what this repo's own declaration will be. Recorded in 72/00's Notes. The failure is honest either way (no scenario breaks), so this is a message-quality and documentation fix, not a design change. |
| **F-72-S** | developer (72/00) | Nit | **FF-7201's census scope is stated two ways.** The feature says *"the modules this story adds"* (one) while the ARCHITECTURE row says *"no module 72 adds"* (three, across three stories). Under the narrow reading, 72/01 and 72/02 must each reach back into 72/00's control file to add themselves — a cross-story write the partition forbids. | **Closed** — assert over ADR-008 §1's declared module set, over those that exist on disk, with a non-vacuity floor of ≥1. Recorded in 72/00's Notes. Also recorded there: do **not** widen the census tree-wide (23 `src/` modules import `node:child_process`, four carry `shell:`), and do **not** widen the five frozen names to "package-manager names" — `src/frameworks.mjs:66` spawns `["npx", …]` and would red on arrival on milestone 12's installer. |
| **F-72-T** | developer (72/00) | Nit | **This story adds a FOURTH PATH resolver to `src/`.** Three exist — `src/terminal-providers.mjs:33` (the only one handling `PATHEXT` correctly), `src/tool-store.mjs:143`, `src/config-inspect.mjs:567` — and **all three are module-private and unexported**, so reuse is impossible without writing outside the story's set, and extraction to one home would touch three files. | **Accepted with the debt ledgered.** Re-deriving ~18 lines inside `src/work-toolchain.mjs` is right for this story; the one-home debt is real and belongs in `TECH_DEBT.md` rather than absorbed silently. Routed to the architect. Two build notes recorded in Notes: add an `isFile` check (a bare `npm` on Windows resolves to an extensionless POSIX shell script that exists and cannot be executed), and compare casing case-insensitively because `PATHEXT` returns `node.EXE`. |
| **F-72-O** | developer (72/03) | Important | **Deleting the unmarked settings blocks costs a delivered acceptance criterion its subject.** `test/bundle-claude-session-hooks.test.mjs:655-660` builds its drift-detection reference by filtering the UNMARKED `SessionStart` entries out of this repo's own `.claude/settings.json`; measured, HEAD yields `"aof session start"` and post-deletion yields `undefined`, so the assertion fails. The suite is imported (`scripts/test.mjs:101`) and spread (`:3944`) — a live CI red. Its delivered scenario (`49/07/00 tasks/00…feature:241-246`) says *"the bare `aof session start` invocation **this repo's hand-authored settings file declares**"*, and the test's own comment at `:648-652` pre-emptively refuses the obvious repair: *"a lane that silently became bundled-vs-bundled would assert a string against itself and detect nothing."* | **Closed by PO decision — pin the literal.** The delivered `.feature` is **not edited** (immutable); the test is code and may change. Of the three repairs, pointing at the marked entry is exactly the bundled-vs-bundled tautology the comment forbids, and guard-if-present is indicted by ADR-005 §2 and TECH_DEBT 36(c). Pinning the literal `"aof session start"` keeps a real assertion — the bundled member's command must be exactly that string, so a bundle changed to `aof session begin` still reds. The comment's objection was to a second COPY as the reference, not to a pinned expectation. The supersession of 49/07's premise is recorded in `ARCHITECTURE.md#ADR-005` per 53/ADR-014's precedent, and `test/bundle-claude-session-hooks.test.mjs` is on 72/03's `files:`. |
| **F-72-P** | developer (72/03) | Nit | ADR-005 §1 names two registry consumers in `src/cli.mjs`; there is a **third**. `helpText()` (`:584`) calls `listCommands()` at `:586` and must become `async`, with its two call sites (`:53`, `:149`) awaited. A missed `await` at `:149` renders `[object Promise]` into an error message, and `test/arch/acd-cli-entry-executes.test.mjs:57` only asserts `/Unknown command/` — it would stay green. | **Closed** — recorded in 72/03's Notes as a priced cost with its silent-failure mode named. `helpText` is not exported and has exactly two call sites, so the ripple stays inside `src/cli.mjs` and none of its five dependents is affected. |
| **F-72-N** | product owner | Nit | ADR-007 §5 enumerates the malformed prepare forms as *no `command`, a non-array `args`, a `command` that resolves nowhere* — not a bad deadline — while ADR-001 §2a makes an absent, zero or negative `deadlineMs` a **refusal** for `work.test`. One module (`src/work-toolchain.mjs`) compiles both declarations, so the two would have behaved differently in the same compiler. | **Closed by PO decision at refine.** Aligned to a refusal, and rows for a non-positive deadline, a non-list `args` and an unresolvable `command` added to `04/tasks/00`. §2a's stated reasoning — *a guessed bound is the same species as a guessed program* — applies identically to a prepare step. Flagged here rather than folded in silently because it is a departure from what §5 enumerates. |
| **F-72-AI** | verify (72/00) | Non-blocker | **This repo's own `work.worktree.prepare` was not declared, and 72/00 is the only story that may declare it.** `.aof/aof.config.json` carries `work.test` and no `work.worktree` block; 72/00's Notes asked for both, and ADR-008 §2 makes 72/00 the sole writer of that file — so the omission cannot be repaired by 72/04, which consumes the key. The reason is a partition gap rather than an oversight and the developer recorded it at source (`STATE.md` `## Feedback (for retro)`): under ADR-001's no-shell rule a `.cmd` shim is undeclarable, so a portable declaration must be an interpreter plus a script (`node scripts/prepare-worktree.mjs`), and **no story in ADR-008 §1's partition owns a new file under `scripts/`**. Declaring the key against a script nobody creates would arm every local dispatch worktree with a prepare step that fails and, per ADR-007 §4, force-removes the tree. Omission was taken as the safe half. Not a blocker for 72/00: no scenario in either task feature asserts the declaration's presence, the compiler itself is built and driven over all six prepare field faults plus the absent-is-silent case, and nothing consumes the key until 72/04. The cost is real and is stated rather than hidden — until the chore lands, 72/04 ships a prepare step this repository never exercises. | **Deferred — routed to `m90` (`90_chore_this-repo-cannot-declare-work-worktree-prepare-without-a-prepare-script-no-story-owns`, `not-started`)**, which owns the `scripts/` file and the declaration together. Open. |

| **F-72-AJ** | verify (72/01) | Non-blocker | **`STORY.md`'s `files:` under-declares the story by one module.** `src/work-test-changed.mjs` is on disk, is imported by `test/work-test-select.test.mjs`, and both of this story's controls census it by name (`STORY_MODULES` in each) — its own header calls it *"the story's SECOND deliverable, not a helper"*. It appears in no `files:` entry, so the record says the story wrote eight files where it wrote nine. Nothing gates on `files:` today (validate PASS, doctor silent), which is why it survived the build. | **Closed at this gate** — `files:` corrected to name `src/work-test-changed.mjs`, `updated:` bumped. The module's PLACEMENT is not in question: FF-7202's own comment fixes it, because the censused subject must start no child and ADR-002 §5 requires a real git one. |
| **F-72-AK** | verify (72/01) | Non-blocker | **FF-7201's declared module set does not reach the module 72/01 added that actually starts a child.** `MILESTONE_MODULES` (`test/arch/acd-declared-program-single-speller.test.mjs:64-68`) is the fixed three `src/work-toolchain.mjs`, `src/work-test-select.mjs`, `src/commands/test.mjs`; `src/work-test-changed.mjs` is absent from it. The one-seam and no-shell clauses are not lost — FF-7202's own self-check asserts `runBounded`, no direct process-module import, no `shell:` and no second spawn API over exactly that module — but the FROZEN-PROGRAM-NAME clause reaches it nowhere, so a future `"npm"` in an executable position in this family would be censused by neither control. No violation today: the module spells `git`, which the five frozen names deliberately exclude. | **Deferred to the milestone gate (`aof:verify 72`)**, not routed into a story. ADR-008 §2 makes 72/00 the sole writer of FF-7201's control file, so 72/01 may not repair it, and `src/commands/test.mjs` — already in the set — does not land until 72/02; the set is re-derivable once, at the close, when every module 72 adds exists. **Closed 2026-09-03 at the milestone gate:** `MILESTONE_MODULES` re-derived to four members (`src/work-test-changed.mjs` added, `test/arch/acd-declared-program-single-speller.test.mjs:64-69`), FF-7201 re-run **9/9 green** with all four on disk — the module spells `git` only, so the frozen-name clause now reaches it and finds nothing. |
| **F-72-AL** | verify (72/01) | Nit | **FF-7202's build-route detector reports the wrong route name for an artifact-path plant.** `GRAPH_ROUTES[0]` fires on the quoted id `graph:build` OR on the bare word `graphify` — and the artifact directory is named `graphify-out/`, so a module that merely spells the artifact path is reported as *"holds an invocation of the graph build"* on top of *"holds a second read of the graph artifact file"*. Observed live in probe (a) above. The control reds correctly in both cases and no false GREEN is reachable; the cost is a finding message that sends the reader after a build nobody wrote. The self-check does not catch it, because it asserts `problems.length >= 1` rather than a route-to-message bijection. | **Deferred to backlog.** Not routed to a story: the fix is one token in a control file this story owns but has already shipped green, and re-cutting it to sharpen a message trades a real regression risk for a wording. |

| **F-72-AM** | verify (72/01) | Non-blocker | **A story-scoped `aof work doctor` reads the parent milestone's status as `not-started` when it is `in-progress`, and reports a false `error`.** Measured at this gate: `wiki/work/72_milestone_inner-loop/SPEC.md:6` reads `status: in-progress`; `aof work doctor 72` reports no such finding; `aof work doctor 72/01` reports `error: story-done-under-not-started — story 72/01 is done but its milestone 72 is not-started`. It is not this story's: `aof work doctor 72/00` reports the identical error against a story accepted the day before, so every scoped story accept in an open milestone hits it. The check is `src/work-doctor-coherence.mjs:211-224`, which resolves the parent through the scoped snapshot. It did not gate the accept — `aof work status 72/01 done` transitioned cleanly — but it drops the scoped Loop-Ready score from 80% to 70% and blocks `stream-coherent` on a fact that is not true. | **Deferred to backlog**, not routed into a story. Outside 72's scope (the milestone owns the inner loop, not the doctor's snapshot scoping) and the repair is in a shared coherence module no story in this partition may write. The accept rule this gate applies — read `control-unresolved` at BOTH severities — is unaffected: neither of this story's controls appears at either severity. |

| **F-72-AN** | verify (72/02) | Important | **A widened `impacted` is a silent whole-suite run.** `aof test --scope impacted` on a working tree holding an untracked file — the commonest inner-loop state — widens to `all` and launches the whole suite with no output until it finishes (measured by the developer at build: >10 minutes here before the first character; `STATE.md` feedback). Both halves are ADR'd — ADR-002 §3's unknown-widens and ADR-003 §3's no-streaming — and their COMPOSITION was not priced: the tool that replaces a focused run becomes a whole-suite run that says so only at the end. | **Deferred to backlog** as a non-blocker: the contract as written is satisfied. The honest fix is not a suppressing flag (ADR-002 forbids one, correctly) but announcing the selection — the widening, the count and that this is now a whole-suite run — BEFORE the child starts; everything it would print is known before the spawn and it needs no new seam. |
| **F-72-AO** | verify (72/02) | Non-blocker | **ADR-004 §4's registration report is undischarged, and nothing is red.** "A selected file that is NOT REGISTERED is reported" is this command's by ADR-004 §4 and 72/02's "where a spawn is available" by ADR-002 §1c — but `registrationDecision` needs `Map<file, exported names>`, and the only shipped producer of assembled names is `src/work-audit-probe.mjs` (milestone 59, outside the write set), which returns names with no file of origin at a ~3.5 s child cost ADR-004 §6 rules out. No task feature of the story states the clause. The command names the gap in its own header rather than half-building it. | **Deferred to backlog** — a story of its own: the probe reporting per-file provenance. Recorded in 72/02's `OUTCOME.md` Gaps. |
| **F-72-AP** | verify (72/04) | Non-blocker | **A malformed config reads as an absent one.** `loadWorkspace` answers `{ config: {} }` for a `.aof/aof.config.json` that is present and does not parse, identically to one that is absent, so a JSON typo silently disables every optional declared step — the worktree prepare among them — with no warning anywhere: the invisible-non-install gap ADR-007 §5 closes lives one layer below where it was closed. Pre-existing; `src/work.mjs` is outside 72's write set. | **Routed to chore `m94`** (`94_chore_a-malformed-aof-config-reads-as-an-empty-one…`, `not-started`), which carries a decision — what should a malformed config do? — and may want to be a story. Open. |
| **F-72-AQ** | verify (72/04) | Non-blocker | **Trees that exist before a door is reached are never prepared.** The session and dispatch handlers' first two doors (`src/mesh-session-spawn-handler.mjs:149`, `src/work-dispatch.mjs:212`) return an EXISTING tree without touching any materialisation door, so a tree materialised before this story is never prepared and still reads as ready; and a genuine TOCTOU remains where the loser of a concurrent add receives the winner's still-preparing tree. Going forward the ladder is closed; the exposure is pre-existing trees plus the race. | **Deferred to backlog.** Recorded in 72/04's `OUTCOME.md` Gaps. |
| **F-72-AR** | verify (gate) | Nit | **`aof work find` served a stale cross-node cache over the local record.** At this gate `aof work find 72 --json` answered `status: not-started`, `answeredFrom: cache`, `reportedBy: umamis-msi-wsl`, while `SPEC.md:6` read `in-progress`. It gated nothing — `validate`, `doctor` and the status verb read the files — but a verify that trusted `find` for status would have refused a milestone that was in progress. | **Deferred to backlog** — mesh cache freshness, outside 72's scope. The same species 71's `STATE.md` archive carries ("the work-stream READ path serves a stale mesh projection while the WRITE path reads the local record", observed four times there); the two records together are the case for the read path preferring the local record when its `updated:` is newer than the projection's `syncedAt`. |
| **F-72-AS** | verify (gate) | Important | **Three of 70/05's regression lanes over the real stream are red on 72's records, and the defect is the brief compiler's.** `test/brief-pinned-to-the-stream.test.mjs` names `72/00`, `72/01`, `72/02` and `72/04` — *"sacrificed objective, tasks, fitness, dependencies with 2,329–3,838 chars of budget still unspent"* — `72/02` and `72/04` as declaring `depends:` with no DEPENDENCIES section, and `72/refine`'s ARCHITECTURE section as *"present but names 0 of its 4 entries"*. Reproduced at this gate through `compileBriefForItem`: 72/02's refine brief is 5,671 of 8,000 chars holding `item` and a `story` block condensed to 4,278 with everything below it sacrificed; 72/03's keeps `objective` and loses the same three at 7,838 (inside the lane's tenth-of-ceiling tolerance, so it passes by luck); the milestone's brief is 5,178 chars with a 227-char architecture husk. `src/phase-brief.mjs`'s `condenseStory` accepts and ignores `budget`, so a long story body is a one-form section taken whole; the sections below are squeezed and sacrificed bottom-up, and the re-plan that would hand the freed room back engages only while the brief is OVER the ceiling. The husk: no ADR decision passage of 72's fits the room whole, and the condenser has no partial form. Trigger: 72's record sizes (`STORY.md` 8–11k chars against 71's 6k; long decision passages). Cause: the compiler, outside every 72 story's write set. Already red at 71's gate and attributed there to "72's refine". Consequence: every 72 story's refine brief carries no contract index, no register and no dependency edge — unfelt here only because 72 was driven interactively (`RETROSPECTIVE.md` R10). | **Routed to chore `m95`** (`95_chore_the-brief-compiler-sacrifices-with-budget-unspent-and-condenses-a-long-architecture-to-a-husk`, `not-started`), scaffolded at this gate with the reproduction and the mechanism in its Notes; the husk half carries a decision (what bounded form to carry when no passage fits). **Non-blocker for 72**: no 72 deliverable is affected, the code under test is 70/05's, and 72's records are valid (`validate` PASS, doctor clean, `ARCHITECTURE.md` within its 1,400-line budget). Open. |

## Accept decision

**72/00 — ACCEPTED, 2026-09-03.** `in-review → done`. Both task features green (12/12), FF-7201 green
(9/9) with all three of its census clauses separately probed red and the tree restored byte-identical,
the tree-wide registration and one-seam guards green (23/23), `aof work validate 72/00` **PASS**, and
`aof work doctor 72/00` reporting **no `control-unresolved` finding at either severity** — FF-7201's
file is on disk, so the accept does not stand on a `pending` marker. One finding landed at this gate,
**F-72-AI**, triaged **non-blocker** and routed to chore `m90`: no scenario in either feature asserts
the missing declaration, the compiler for the key ships and is driven over every fault shape, and
nothing consumes it until 72/04. No `@manual` and no `@uat` scenario exists in this story, so no human
lane was opened. Outcome authored at `stories/00_story_the-declared-toolchain/OUTCOME.md`.

**The milestone is NOT accepted.** Four stories remain (`01`, `02`, `03`, `04`) and four of the seven
declared controls — FF-7204, FF-7205, FF-7206, FF-7207 — resolve to no file yet. The whole-repo
regression sweep is the milestone gate's and has not been run; this story's lane was scoped to its own
scenarios plus the guards its new files fall under. `aof:verify 72` closes it.

**72/01 — ACCEPTED, 2026-09-03.** `in-review → done`. Both task features green (16/16), FF-7202 green
(6/6) with five clauses separately probed red, FF-7203 green (4/4) with three, the tree restored
byte-identical after every one of the eight (`src/work-test-select.mjs` sha256 `74872e5c…34688f`
before and after each). The guards this story's changes fall under are green as one lane (40/40) —
the layer gate the `computeImpact` move exists to keep green, both frozen graph-reader allowlists it
edits, the three registration guards, 59/FF-5904, and `test/graph-impact.test.mjs` proving the
re-export left the shipped `graph:impact` behaviour unchanged. 72/00's FF-7201 is still 9/9 with this
story's modules on disk, which is the parallel-lane risk the partition names and the one it could
have failed on. `aof work validate 72/01` **PASS**; `aof work doctor 72/01` reports **no
`control-unresolved` at either severity**, so neither control is being accepted on a `pending` marker.

Four findings landed at this gate, none a blocker: **F-72-AJ** (the `files:` under-declaration)
closed here by correcting the record; **F-72-AK** (FF-7201's module set does not reach
`src/work-test-changed.mjs`) deferred to the milestone gate, where the set is re-derivable once every
module 72 adds exists; **F-72-AL** (a route-detector message names the wrong route) and **F-72-AM**
(a story-scoped doctor misreads an `in-progress` parent as `not-started`, raised AFTER the transition
and pre-existing on the already-accepted 72/00) both deferred to backlog. No `@manual` and no `@uat` scenario exists in this story, and it ships no UI surface, so no
human lane and no design-conformance lane was opened. Outcome authored at
`stories/01_story_the-selection/OUTCOME.md`.

**72/02 — ACCEPTED, 2026-09-03.** `in-review → done`. All three task features green (57/57 across the
lane, run through the `--only` path the story itself ships), FF-7204 green (7/7) with five clauses
separately probed red and the tree restored byte-identical after each (`src/commands/test.mjs` sha256
`189220cf…f6f3b9`, `src/work-loop.mjs` `76b5a107…02873d`), REG-MUT-11 green over the runner as left
with the control's diff exactly one line (`:269`), the live face exercised from this tree on both faces
and both refusals, `aof work validate 72/02` **PASS**, `aof work doctor 72/02` **no
`control-unresolved` at either severity**. Two findings landed, neither a blocker: **F-72-AN** (a
widened `impacted` is a silent whole-suite run) and **F-72-AO** (the registration report is
undischarged), both deferred to backlog. No `@manual`, no `@uat` and no UI surface, so no human lane
and no design-conformance lane was opened. Outcome authored at
`stories/02_story_the-test-commands-face/OUTCOME.md`.

**72/03 — ACCEPTED, 2026-09-03.** `in-review → done`. Both task features green (23/23, the repaired
delivered lane `test/bundle-claude-session-hooks.test.mjs` included), FF-7205 green (4/4) with five
clauses probed red — the session-half ratchet's planted registry import among them, observed reddening
both closures because `src/cli.mjs` statically imports the session module — and FF-7206 green (4/4)
with four, the tree restored byte-identical after each (`src/cli.mjs` `d2edd492…1bbaec`,
`src/commands/mesh-session.mjs` `9f49fdb7…69f5e0`, `.claude/settings.json` `56294620…ab27fd`). The
settings file verified at source: six `aof session …` commands at HEAD, three now, each survivor
managed, the operator's guard untouched. Validate **PASS**; doctor **no `control-unresolved`**. No
finding landed at this gate. No `@manual`, no `@uat`, no UI. Outcome authored at
`stories/03_story_the-cold-boot/OUTCOME.md`.

**72/04 — ACCEPTED, 2026-09-03.** `in-review → done`. Task 00 and the five `@executable` scenarios of
task 01 green (10/10); task 01's `@manual` scenario run inline by verify — the ratchet seen red on a
planted junction into a keyed worktree, the message recorded against FF-7207, the control green again
once the plant was removed; FF-7207 green (5/5) with five clauses probed red and `src/mesh-worktree.mjs`
restored byte-identical (`b98c4345…8d357c`); the two frozen counters the review found unpriced green in
the gate's guard lane. Validate **PASS**; doctor **no `control-unresolved`**. Two findings, neither a
blocker: **F-72-AP** (a malformed config reads as absent) routed to chore `m94`, **F-72-AQ**
(pre-existing trees are never prepared; the race loser's tree) deferred to backlog. **F-72-AI** stays
open on `m90`. No `@uat`, no UI. Outcome authored at
`stories/04_story_the-prepared-worktree/OUTCOME.md`.

**72 — ACCEPTED, 2026-09-03.** `in-progress → done`. All five stories `done`, each with its outcome.
Every one of the seven declared controls resolves on disk and carries a recorded red probe — FF-7201
with three (and re-derived at this gate over all four modules 72 adds, F-72-AK closed), FF-7202 five,
FF-7203 three, FF-7204 five, FF-7205 five, FF-7206 four, FF-7207 five — **30 probes in all**, each
observed red on its own clause and each restore confirmed byte-identical. `aof work validate 72`
**PASS**; `aof work doctor 72` **no `control-unresolved` at either severity** (its
`control-runner-unchecked` warn is the repo-wide standing condition `STATE.md` records, not a control
that fails to resolve). The guards every 72 change falls under are green as one lane (95/95); the whole
registered suite minus the `:4182` binder ran once, **8,489/8,496**, with all seven failures named and
attributed to chores 88, 91, 92, 93 and 95 — none to 72's code. No blocker finding is open: the open
findings are F-72-AI (`m90`), F-72-AN, F-72-AO and F-72-AQ (backlog), F-72-AP (`m94`), F-72-AR
(backlog) and F-72-AS (`m95`), every one a non-blocker. No `@uat` scenario exists anywhere in the
milestone and it ships no UI surface, so no human lane and no design-conformance lane was opened; the
milestone's one `@manual` scenario (72/04 `tasks/01`) was run by verify. `RETROSPECTIVE.md` written
(R1–R10), `OUTCOME.md` authored at the milestone level (citing the stories' outcomes rather than
restating them) and 72/01's two gate-discharged gaps closed, `aof work memory ingest` run, and
`STATE.md` compacted with its `## Feedback (for retro)` section archived.
