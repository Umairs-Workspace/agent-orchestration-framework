---
doc: verification
---
<!--
  Milestone VERIFICATION.md — answers ONE question: what evidence do we have that this milestone
  works? Owner: aof:verify (the product owner is the SINGLE WRITER of the findings register).
  Scaffolded at refine so the declared controls have a home for their red probes; the evidence rows
  are filled as each story lands, and the accept decision at the close.
-->
# 63 · Event-driven triggers — Verification

## Fitness functions

<!-- CITING register (66/ADR-008 ruling 4): every row resolves to a declaration in the sibling
     ARCHITECTURE.md `## Fitness functions` register and declares nothing itself. The red-probe cell
     records WHAT WAS CHANGED to make the control fail and THE MESSAGE OBSERVED.

     A control must fail when the invariant it guards is broken, so the probe is that assertion's
     positive control, and this row is where a reviewer reads that it was observed failing. An
     assertion nobody has ever seen red cannot be falsified by its own green.

     An untouched placeholder cell is a MISSING red probe, not a recorded one. Every cell below is
     `pending` at refine and must be FILLED before accept, each probe applied to the working tree,
     run, REVERTED, and the restore confirmed byte-identical before the next probe begins.

     THREE ROWS CARRY MORE WEIGHT THAN THE OTHERS AND THEIR PROBES ARE THE ONLY EVIDENCE THEY ARE
     ARMED, because each is green over a tree that has never held the defect:
       · FF-6305's attended-launch byte-identity leg — the safety leg on a function with 23
         dependents that is the sole producer of the NEEDS_INPUT sentinel.
       · FF-6306's byte-unchanged leg over the three phases that must not move — a live production
         path, and the module whose own header records a wrong-base build caused by a second speller.
       · FF-6308's non-vacuity pass over the shipped declaration, which no fixture can discharge.

     DELIBERATELY NOT RESTATED HERE, because a guard already in service walks the whole tree and
     would already fail on the breach: 55/FF-5508 (no config key, env var or flag admits L3),
     55/FF-5505 (every compiled frozen rule traces to its declaring member), 66/FF-6604 and
     59/FF-5911. Each is named in the ADR whose invariant it discharges; none is a gap. -->

| id | enforced by | result | red probe |
|---|---|---|---|
| FF-6301 | `test/arch/acd-trigger-is-a-caller-not-a-coordinator.test.mjs` | **green** — 10/10, 2026-09-02 | Planted `const PLANTED_DIRECTIVE = "/aof:autonomous";` at the head of `src/work-trigger/sources.mjs`. RED: *"no module in the trigger family contains a slash-command literal — the one spelling that would make a trigger a prompt author"* (`acd-trigger-is-a-caller-not-a-coordinator.test.mjs:207`). Reverted; file sha256 `a1425903…369f2` before and after. |
| FF-6302 | `test/arch/acd-trigger-declaration-is-data.test.mjs` | **green** — 7/7, 2026-09-02 (was recorded 8/8; corrected at the 2026-09-02 re-verify, **F-63-G**) | Planted `"planted": true` beside `"version": 1` in `src/bundle/triggers.jsonc`. RED on TWO legs: *"the installed copy is byte-identical to the bundled source"* (`:219`) and the eol ratchet leg *".aof/triggers.jsonc is byte-identical to src/bundle/triggers.jsonc"*. Reverted; file sha256 `04bd2506…4c9ca5f7` before and after. |
| FF-6303 | `test/arch/acd-trigger-holds-no-clock.test.mjs` | **green** — 7/7, 2026-09-02 | Planted an uncalled `function plantedClock() { return setInterval(() => {}, 1000); }` in `src/commands/trigger.mjs` (uncalled deliberately — a live timer holds the probe process open and the control's scan is static). RED: *"no module in the trigger family holds a repeating timer"* (`:162`). Reverted; file sha256 `9f37d5b4…0bd9767` before and after. |
| FF-6304 | `test/arch/acd-trigger-level-is-a-ceiling.test.mjs` | **green** — 9/9, 2026-09-02 | Planted `const PLANTED_THRESHOLD = 100;` in `src/work-trigger/level.mjs`. RED: *"src/work-trigger/level.mjs contains a bare score threshold"* (`:185`). Reverted; file sha256 `a2920d3f…d54a6544` before and after. |
| FF-6305 | `test/arch/acd-unattended-launch-is-declared.test.mjs` | **green** — 9/9, 2026-09-02 (was recorded 12/12; corrected at the 2026-09-02 re-verify, **F-63-G**) | Reverted `gate-order`'s rule in `src/bundle/frozen-set.jsonc` to its pre-63 spelling `{ "argument": "--aof-gate-order" }`. RED: `FrozenSetError: Frozen-set member "gate-order" refused: launch envelope rule carries "argument", which this enforcement point does not know; it knows program, args.` — on the compile leg AND on the declaration-moves-the-artifact leg (`:152`). Reverted; file sha256 `3ee6cfaf…a5a87bfd` before and after. |
| FF-6306 | `test/arch/acd-assignment-resolves-to-a-loop-call.test.mjs` | **green** — 11/11, 2026-09-02 | Planted a SECOND speller — `` const plantedSecondSpeller = (itemRef) => `/aof:autonomous ${itemRef}`; `` — in `src/mesh-assignment-reclaim.mjs`. RED: *"exactly one module authors an assignment phase's slash command; found: mesh-assignment-directive.mjs, mesh-assignment-reclaim.mjs"*. **The first plant of this probe was a NO-OP and is recorded rather than hidden**: the same literal planted in `src/mesh-assignment-directive.mjs` left the control green, correctly — that module is the one home the row licenses, so the plant has to land in a SECOND file to be the defect this leg names. Reverted; file sha256 `6e622130…f0d1614c` before and after. **SECOND PROBE, for the form ADR-013 §3a relaxed (owed by that ruling, 2026-09-02):** reverted the two launch-conditional forwards in `src/mesh-worker-execution.mjs` to the bare shorthand `watchTranscriptSessionId,` / `watchTranscriptCompletion,` — the shape that reinstates the premature-done defect. RED on the structural leg (*"watchTranscriptSessionId is launch-conditional in exactly the ADR-013 §3a form, and in no other"*) AND on the driven behavioural leg (*"a loop launch is handed a watch of its own"*), so the relaxation is held by a control at both altitudes rather than excused from the census. Reverted; file sha256 `2c35b3bb…721ad3fb` before and after. |
| FF-6307 | `test/arch/acd-trigger-never-classifies.test.mjs` | **green** — 8/8, 2026-09-02 | Planted `const PLANTED_BODY = (capture) => capture.text;` in `src/work-trigger/sources.mjs`. RED: *"sources.mjs contains a property read off a capture"* (`:184`). Reverted; file sha256 `a1425903…369f2` before and after. |
| FF-6308 | `test/arch/acd-trigger-is-non-vacuous-over-this-repo.test.mjs` | **green** — 6/6, 2026-09-02 | Re-pointed `ci-signal-wake`'s `source` to `cron` in the installed `.aof/triggers.jsonc`, leaving one declared source with no trigger. RED on TWO legs: *"the installed declaration is the one this tool ships"* and *"every declared source has at least one declared trigger — missing: ci-signal"*. Reverted; file sha256 `04bd2506…4c9ca5f7` before and after. |

**Probe procedure.** Each probe was applied to the working tree, the control run in a FRESH `node`
process (these controls snapshot source text at module load, so a warmed import cache cannot see a
plant), then reverted and the restore proven by sha256 before/after. The eight controls were re-run
together afterwards over the restored tree: **67/67 green**.

---

## Verification evidence

<!-- Filled story by story as each lands, and at the milestone gate. Pointers and measurements, not
     restatements of the contract.

     ADR-001 §4 adds ONE non-vacuity condition to this gate that no suite over fixtures can
     discharge: at accept, every source declared in this repository's own `.aof/triggers.jsonc` must
     have at least one trigger resolving to a well-formed `work:loop` input, and every declared
     trigger's scope must resolve through `LOOP_SCOPE_FORMS`. Record the command and what it
     resolved here. A green suite over planted declarations is not that evidence.

     ADR-005 §2 adds a second: every ATTENDED launch must be byte-identical to HEAD — argv and
     scrubbed env alike — across a human session, a phase-driver session, all three single-phase
     mesh directives, a resumed session and a session carrying `--model`/`--effort`. Record the
     comparison, not a description of it. -->

**Two `@manual` lanes exist and each owes a row here.** The contract has 29 task features carrying 28
`@executable` scenarios-or-files and **two** `@manual` scenarios, and neither can be discharged by the
suite:

1. `63/02 · tasks/01_an-unattended-launch-resolves-only-the-declared-shape.feature` — *a real unattended
   launch is observed running end to end*. Run the resolved program and argv **verbatim, unattended, to
   completion**; confirm the process that starts is the declared program and **not** an interactive
   session; confirm it walks continue → validate → doctor → grade → verify **in that order**; confirm it
   ends of its own accord having asked nobody for anything. Record the command as run, the gates seen in
   order, and the exit status.
2. `63/03 · tasks/03_an-old-worker-idles-rather-than-doing-the-wrong-work.feature` — the whole file.
   Observed on a **real** skewed pair (the WSL worker node is the cheapest one — see
   `.claude/rules/build-deploy-restart.md`), because simulating a pre-63 worker by stripping a field
   proves only the simulation. Read the build stamps on both nodes *and* in each daemon's startup line,
   then for an autonomous assignment record: (a) the control's dispatch decision naming a loop launch,
   (b) the assignment held rather than unassigned in the fleet view, (c) a live worker session with an
   empty prompt and no output, (d) no command of any kind in the session or the log, (e) no commits in
   the worktree after dispatch, (f) settlement on the pre-existing deadline policy and reclaimability
   afterwards.

| subject | lane | what was run | result | contract |
|---|---|---|---|---|
| 63 (milestone) | gate | `aof work doctor 63` | 2026-09-02: **no `control-unresolved` finding at either severity** — all eight declared controls resolve to a file that exists. Warns, none of them a 63 acceptance gate: `mtime-ahead-of-updated`, `numbering-gap` (top-level driver sequence), `doc-over-budget` (F-63-D), `control-runner-unchecked` (F-63-F), `rubric-join-unchecked` x6 (F-63-F). Loop-Ready **80% (8/10)** — clears L1; blocking `grounding`, `anchor-grounding`. | ARCHITECTURE.md `## Fitness functions` |
| 63 (milestone) | gate | `aof work validate 63` | 2026-09-02: **PASS — 63 is well-formed.** The three transient `story reads path ... does not exist` issues recorded at refine cleared exactly as 63/05's Notes predicted, once stage 1 landed the three `src/work-trigger/*.mjs` modules. | — |
| 63 (all six stories) | `@executable` | the six behavioural suites — `test/trigger-declaration`, `trigger-level-ceiling`, `unattended-launch-envelope`, `mesh-assignment-loop-directive`, `trigger-sources`, `trigger-command` — run as one focused lane through the registry's own exported arrays | 2026-09-02: **288/288 green**, 0 failures. | 27 `@executable` scenarios-or-files across 28 task features |
| 63 (all eight controls) | fitness | the eight `test/arch/acd-*` files this milestone declares, run as one focused lane | 2026-09-02: **67/67 green**, 0 failures — re-run after the eight red probes over the restored tree and green again. | ARCHITECTURE.md `## Fitness functions` |
| 63 (all eight controls) | fitness | registration check — each control imported AND spread in `scripts/test.mjs`'s suite registry | 2026-09-02: all eight imported (`scripts/test.mjs:3137-3190`) and all eight spread (`:3375-3393`). No suite is imported-and-not-spread, which is 56's finding and TECH_DEBT item 50's subject. | ARCHITECTURE.md `## Fitness functions` HARNESS SHAPE |
| repo | regression | the WHOLE unit registry from `scripts/test.mjs` (8,362 tests) minus `global-work-propagation.test.mjs` (6 tests — it binds `:4182`, which this machine's live control daemon holds) | 2026-09-02: **reached test 3,259 of 8,362 (39%) in ~80 minutes with 0 failures, then stopped.** Recorded as INCOMPLETE rather than green — see **F-63-E**. The full lane is the MILESTONE gate's, and 63 is not being accepted, so it is due when 63 actually is. | — |
| 63 (ADR-001 §4) | non-vacuity | `aof work trigger` over this repository's own installed `.aof/triggers.jsonc` | 2026-09-02, exit **0**. Declaration read: the installed `.aof/triggers.jsonc` (version 1), four members. **All four declared sources have a trigger that resolves**: `cron` -> `nightly-open-work-sweep`, `mesh-assignment` -> `mesh-assignment-wake`, `ci-signal` -> `ci-signal-wake`, `feedback-finding` -> `feedback-finding-wake`. Each resolves to scope `63` at `L1` with argv `work loop 63 --level L1`. Gate readings *"not consulted — no considered trigger asks for a level the gate governs"*, which is correct for an all-L1 declaration. The face reported the pre-flight disclaimer and `This command resolves; running the argv above is the caller's.` | ARCHITECTURE.md ADR-001 §4 |
| 63/02 (ADR-005) | gate condition | `compileFrozenSet(bundledFrozenSet())` in a fresh process | 2026-09-02: `deferred: []` — **empty**. `unattendedLaunch: {"memberId":"gate-order","program":"aof","args":["work","loop"]}`. `FROZEN_ENFORCEMENT_POINTS` = `["tool-call hook entries","permission denials","agent tool scope","the worker launch envelope"]`, all four now compiled. The two delivered gap records this discharges (`55/OUTCOME.md` and `55/04/OUTCOME.md`) were moved from `open` to `discharged` as part of this accept. | ARCHITECTURE.md ADR-005 |
| 63/02 `tasks/01` | `@manual` | *a real unattended launch is observed running end to end.* Resolved launch read from the declaration (`program: aof`, leading args `work loop`); the shipped trigger's own argv run **verbatim, unattended, stdin closed**: `aof work loop 63 --level L1` | 2026-09-02. **Observed:** the process that started is `aof` (the declared program) and **not** an interactive session — no PTY, no `claude`. It reported one decision per item (`63 — drive verify`; `63/00`...`63/05 — drive continue`), **ended of its own accord**, asked nobody for anything, and exited **0**. The working tree was byte-unchanged across the run (`git status --porcelain` sha256 `0e11e2fd...6be1b856` before and after). **NOT observed:** the *"walks the continue, validate, doctor, grade and verify gates in that order"* clause — `L1` is report-only (`src/commands/loop.mjs:1213`, `runL1`) and drives no phase, and the level above it is the one the shipped declaration does not ask for. See **F-63-C**. | 63/02 `tasks/01` `@manual` scenario |
| 63/03 `tasks/03` | `@manual` | *a worker that does not understand a loop directive idles rather than doing the wrong work* — the whole file, on a genuinely skewed real pair | **NOT RUN.** The lane needs the WSL worker node re-deployed from a payload that predates 63/03, both daemons restarted, and live autonomous assignments dispatched and watched to settlement across four vintage/phase combinations. That is an operator-attended exercise (daemon restarts are the operator's, per `.claude/rules/build-deploy-restart.md`), and the story's own task box is unticked. See **F-63-B**. | 63/03 `tasks/03`, all five scenarios |
| 63 (ADR-013 §1) | gate condition | read at source: does the worker hand a loop launch a loop-shaped transcript watch? | 2026-09-02: **NO — the defect ADR-013 §1 names is OPEN.** `src/mesh-worker-execution.mjs:1771-1772` forwards `watchTranscriptSessionId` and `watchTranscriptCompletion` as unconditional bare shorthand, and `composeDirectiveLaunchOptions` (`:205-220`) returns only `{ unattended, declaredLaunch }` — no watch override. No follow-up story existed under `63/stories/`. See **F-63-A**. **RE-MEASURED the same day, after the fix:** the two forwards are launch-conditional in ADR-013 §3a's licensed form, `loopShapedTranscriptWatch` supplies both seams for an unattended loop and `null` for every session launch, `src/agent-session-driver.mjs` is byte-unchanged, and `63/06` exists and is `in-review`. Driven: the session-shaped default binds a planted inner-session transcript over the run's own worktree, and the seam the worker actually hands the loop launch binds nothing over that same directory. | ARCHITECTURE.md ADR-013 §1, ADR-016 |
| 63/06 | `@executable` | `tasks/00_a-loop-launch-is-not-watched-as-a-session.feature` — all four scenarios, run through `test/mesh-assignment-loop-directive.test.mjs`'s exported array in a fresh process | 2026-09-02 (re-verify): **green**. The two driven lanes carry the story's positive control: the session-shaped default binds the planted inner-session transcript over the run's OWN worktree, and only then is the loop-shaped seam shown to resolve nothing over that same directory; a session-phase directive supplies neither seam; an injected seam still wins. | 63/06 `tasks/00` |
| 63 (whole milestone lane) | `@executable` + fitness | the six behavioural suites, the eight declared controls, and `test/arch/acd-session-driver-single-home.test.mjs` (the neighbour 63/06's diff touches), run as one focused lane through the registries' own exported arrays, each test under its own hermetic `AOF_GLOBAL_HOME` | 2026-09-02 (re-verify): **363/363 green**, 0 failures — 290 behavioural + 67 control + 6 neighbour. Per control: FF-6301 10, FF-6302 7, FF-6303 7, FF-6304 9, FF-6305 9, FF-6306 11, FF-6307 8, FF-6308 6. | 28 `@executable` scenarios-or-files across 29 task features |
| 63 (milestone) | gate | `aof work validate 63` (re-run after 63/06 landed) | 2026-09-02: **PASS — 63 is well-formed.** | — |
| 63 (milestone) | gate | `aof work doctor 63` (re-run after 63/06 landed) | 2026-09-02: **no `control-unresolved` finding at either severity**, and no residual `pending` marker in the register — all eight declared controls resolve to a file that exists. Warns, none of them a 63 acceptance gate: `numbering-gap` (top-level driver sequence), `doc-over-budget` (now 1,823 lines — **F-63-D**, grown by ADR-016), `control-runner-unchecked` and `rubric-join-unchecked` x7 (**F-63-F**, now including 63/06). Loop-Ready **80% (8/10)** — clears L1; blocking `grounding`, `anchor-grounding`. | ARCHITECTURE.md `## Fitness functions` |
| 63/06 (ADR-013 §1) | gate condition | read at source, after the fix: `src/mesh-worker-execution.mjs` | 2026-09-02 (re-verify): `loopShapedTranscriptWatch` (`:222-235`) returns `null` for every session launch and both seams as `null`-resolving functions for an unattended loop; the two forwards (`:1789-1792`) are `injected ?? loopWatch?.…` — ADR-013 §3a's one licensed expression — while `ptySpawn`, `which`, `onOutputChunk` and `onSessionEnd` stay bare shorthand. `src/agent-session-driver.mjs` is **byte-unchanged** (not in the diff). | ARCHITECTURE.md ADR-013 §3a, ADR-016 §1 |
| repo | regression | **F-63-E, the milestone-gate sweep** — the whole unit registry from `scripts/test.mjs` minus `global-work-propagation.test.mjs` (binds `:4182`, held by this machine's control daemon), each test under its own hermetic `AOF_GLOBAL_HOME` | 2026-09-02, run **twice**. First run (tree being written concurrently): 8,387/8,395, 8 failures — one of them 63's own (**F-63-H**, fixed here). Second run, after the code tree had been quiet 85 minutes: **8,388/8,395 green in 29m**, 7 failures, `53/00 task01` among the passes. Every remaining failure reproduces in isolation and none names a 63 file as its subject (**F-63-I**). `git status` was captured before and after the second run and differed only under `wiki/work`. | — |
| 63/03 `tasks/03` | `@manual` | **Scenario 1 — *the pair is genuinely skewed before anything is concluded from it*.** Control deployed (`node scripts/install-local.mjs --skip-ui`) and the supervisor brought up with the operator's explicit authorisation. | 2026-09-02, **OBSERVED and PASSING**. Stamps read on each node AND in each daemon's own startup line, as the scenario demands: control `aof --version` = `0.1.0 (payload 3830440d+dirty.20260902T220731)`; `mesh serve running (node win-host-a, build payload 3830440d+dirty.20260902T220731)`; `mesh ui running (build payload 3830440d+dirty.20260902T220731)`. Worker `aof --version` = `0.1.0 (source 2546a06+dirty)`, head `2546a06` of 2026-07-27. **The two ids differ and the worker's is the older one.** The skew is GENUINE, not simulated: `src/mesh-worker-execution.mjs` on the worker contains **zero** occurrences of `composeDirectiveLaunchOptions`, `assignment-loop-launch-scopeless`, `loopShapedTranscriptWatch`, `declaredLaunch` or `unattendedLaunch` — it cannot read a loop directive because that code does not exist there. Identity pinned (`win-host-a-wsl`, `172.27.155.33`), node record enrolled, `aof mesh status` lists both. | 63/03 `tasks/03` scenario 1 |
| 63/03 `tasks/03` | `@manual` | **Scenario 1 re-established on a CORRECTED pair, and this supersedes the row above.** The first pair was rejected as unsound on measurement: the worker's `aof --version` reported `source 2546a06+dirty`, but that id is derived from **git HEAD, not from the deployed files**, so the stamp did not honestly describe the payload (**F-63-K**). The worker was re-pointed to `bbf86a4f` (63/02 close) — a skew of **exactly one story**, the same protocol era, `package-lock.json` unchanged so no native rebuild — and its prior state parked in a recoverable `git stash`. | 2026-09-02, **OBSERVED and PASSING.** Control `payload 3830440d+dirty.20260902T220731`; worker `source bbf86a4`, and `grep` confirms **zero** occurrences of `composeDirectiveLaunchOptions` / `loopShapedTranscriptWatch` in its `src/mesh-worker-execution.mjs`. Ids differ, worker's is older. Worker enrolled, connected (**0** stream failures), exchanging startup-reclaim and withdraw-notify with the control, and `aof mesh status` on the CONTROL reports `win-host-a-wsl — live · build source bbf86a4` beside `win-host-a — live · build payload 3830440d+dirty…`. | 63/03 `tasks/03` scenario 1 |
| 63/03 `tasks/03` | `@manual` | Scenarios 2-5 — the four vintage/phase dispatches, the idle-session observations and the deadline settlement | **ATTEMPTED 2026-09-02 and BLOCKED at repo preparation, before any session is spawned (F-63-M).** The pair is now standing and correct; what remains is the dispatch exercise itself. `aof mesh assign` takes no `--phase`, so each row needs an item in the state that selects its phase; row 4 (matched pair) additionally needs the worker upgraded and would run a REAL unattended loop, and row 3 needs the control downgraded and the supervisor cycled. | 63/03 `tasks/03` scenarios 2-5 |
| 63/03 `tasks/03` | `@manual` | **Row 1 of the Scenario Outline — *new control with an old worker, `autonomous` phase*** — plus the two scenarios reading the same dispatch (*the idle session is observable* and *nothing substitutes the cascade*). Milestone `63` assigned to `win-host-a-wsl` in the `aof` workspace, phase side-table set to `autonomous` with the worker held down so no dispatch race existed. | 2026-09-03, **OBSERVED and PASSING on every clause except settlement.** **Typed: NOTHING AT ALL** — the live session's argv (`claude --permission-mode auto --exclude-dynamic-system-prompt-sections --append-system-prompt <worker prompt>`) carries no `-p`, no `--print` and no `/aof:` token, and the worker log holds **0** occurrences of `/aof:`, `aof work loop` or any command. **The control's dispatch decision records that a loop launch went out**: *"assignment 3d0fc3ab… (63) -> win-host-a-wsl: loop launch over scope 63 (no base branch — worker converges on the item's derived branch) from ea5d4975ca60"*. **The assignment is HELD by that worker rather than unassigned or stalled**: the `global_assignments` row reads `state=running`, `run_id=20260903T000836168Z-0000`, `target_node_id=win-host-a-wsl`, phase `autonomous`. **The session is live with an empty prompt and no output of its own** (pid 12708; no output lines in the log). **Nothing ran on the worktree**: cwd `~/source/aof/.aof/mesh/worktrees/3d0fc3ab…` on branch `aof/mesh/63`, HEAD at the dispatched base `ea5d497`, **0 commits since the base and 0 dirty files**. The three readings together say *the worker did not understand the directive*, not *the worker is gone*. | 63/03 `tasks/03` outline row 1; scenarios *the idle session is observable* and *nothing anywhere substitutes the cascade* |
| 63/03 `tasks/03` | `@manual` | **Row 1's settlement clause, and the scenario *the waste is bounded by the deadline that already existed*** — the idle session was left strictly alone until it ended on its own. | 2026-09-03, **OBSERVED and PASSING.** The session ran from `00:08:36Z` to `00:28:39Z` — **20m03s** — and ended of its own accord with nothing done to it. That interval is exactly `DEFAULT_HEARTBEAT_MS` (15m) + `DEFAULT_STARTUP_GRACE_MS` (5m) from `src/loop-bounds.mjs`, **both pre-existing constants that 63 neither introduced nor touched**, which is what makes this the deadline the worker already had rather than a new one. The assignment settled `state=failed`, `reclaimed_at=null`, `session_id=null` — terminal and reclaimable in the ordinary shape. **No new timeout, retry or watchdog was introduced**: the worker log holds zero occurrences of any watchdog or retry vocabulary, and `FF-6306`'s green census independently pins that no new stop, completion signal or halt vocabulary exists in the mesh files. **One honest limit on this reading**: the settled row carries `code=null` and NEITHER side logged a settlement line naming the deadline, so the attribution above rests on the arithmetic matching the two delivered constants rather than on the system saying which deadline fired. That is a diagnosability gap in the mesh, recorded as F-63-O. | 63/03 `tasks/03` outline row 1 (ending) + scenario *the waste is bounded by the deadline that already existed* |
| 63/03 `tasks/03` | `@manual` | **Row 2 of the Scenario Outline — *new control with an old worker, a SESSION phase*.** Milestone `55` assigned with the CLI default (no phase side-table row), which is the `refine` path and is byte-identical to a pre-63 assign. Dispatched on `55` rather than `63` deliberately: a session phase types a real cascade into a live PTY, and aimed at `63` that would set an agent to work rewriting the very records this verification is producing. | 2026-09-03, **TYPED COMMAND OBSERVED and PASSING; ending curtailed deliberately.** Read out of the session's own transcript (`~/.claude/projects/-home-umami-source-aof--aof-mesh-worktrees-d63a03e5…/951fa0c7….jsonl`), the first user message is **`/aof:refine 55 --autonomous`** — byte-identical to `assignmentDirectiveCommand("refine", "55")`'s delivered string — followed by the pre-existing phase brief (70/00's `## ITEM` / `## MILESTONE OBJECTIVE` seam). So an old worker on a session phase receives and types exactly the command it did before this story. The assignment also **proves re-assignability after a terminal one**: it was accepted while row 1's assignment stood `failed`. **What was NOT observed, and why**: the *"finishing exactly as it did before this story"* half. The run was withdrawn after ~90s rather than left to complete, because an unattended `--autonomous` refine cascade on an accepted milestone would have done substantial real work nobody asked for. The withdraw was clean — `withdraw-notify: live session killed (withdrawn by control); its bracket settles the run record as cancelled`, **0 commits and 0 dirty files** on `aof/mesh/55`. The unchanged-ending half is independently pinned by `FF-6306`'s byte-unchanged census over `assignmentDirectiveCommand`'s four answers, which is green. | 63/03 `tasks/03` outline row 2 |
| 63/03 `tasks/03` | `@manual` | **Rows 3 and 4 of the Scenario Outline** — *matched pair, `autonomous`* and *old control with a new worker, `autonomous`*. The worker was upgraded to the control's payload (`install-local --wsl --skip-ui`; node-pty rebuilt from source for linux-x64) and verified AT SOURCE to now carry the story — `composeDirectiveLaunchOptions` 3, `loopShapedTranscriptWatch` 3, `assignment-loop-launch-scopeless` 1 — since its stamp still misreports `source bbf86a4+dirty` (F-63-K). Row 4 was aimed at `55`, chosen because `aof work loop 55 --level L2 --dry-run` reports `loop done`: **a dispatched loop carries no level, so `resolveLoopLevel`'s default of L2 applies and L2 DRIVES**, and aiming it at `63` would have set agents to work rewriting these very records. | 2026-09-03, **BLOCKED, not observed (F-63-P).** The dispatch reached the worker and got further than row 1 — worktree on the existing `aof/mesh/55`, `gate-propagation already-current` — then failed at the gate-time PUSH: `write-credential-mint-failed: push of branch "aof/mesh/55" ... write-credential-request was refused by control`. Root-caused by querying the App itself: its only installation is on `Vendorco-ai`, while this workspace's registered repo is under `UmairB`. Both remaining rows need that push, so both are blocked behind the same environment gap. **No session was spawned and no loop process ran**, so nothing about rows 3 or 4 was observed and nothing is claimed. Row 4 would in any case have been a weak exercise of 63/06's watch fix: with `55` having nothing to drive, the loop completes without ever opening an inner session, which is the condition the premature-done defect needs — that fix stays covered by 63/06's driven `@executable` lanes. | 63/03 `tasks/03` outline rows 3 and 4 |
| 63 | tag census | all 28 task `.feature` files | Re-counted 2026-09-02 after 63/06 landed: **29** task features — 27 wholly `@executable`, one wholly `@manual` (63/03 `tasks/03`), and one mixed (63/02 `tasks/01`, `@executable` scenarios plus the one `@manual`). Verification lanes **28 `@executable` scenarios-or-files and 2 `@manual` scenarios**, and still **no `@uat`** — so **no human sign-off step applies to this milestone** and none was solicited. The milestone carries no `DESIGN.md` and no UI surface, so no design-conformance review applies. | 29 task features |

---

## Findings

<!-- Register for milestone 63. Ids allocated HERE, at the moment of landing, by the single writer
     (`aof:verify`'s product owner) — never read-then-allocate.

     **Cite another item's id as `m?<itemRef>/<ID>`** — the `m` prefix is optional, because both
     spellings are real — **and cite only ids that resolve**: a bare id is addressable only inside
     its own item's documents, so a cross-item citation carries the ref. -->

| id | observed | type | severity | triage | routed-to | status |
|---|---|---|---|---|---|---|
| F-63-A | An unattended mesh loop launch is handed the SESSION-shaped transcript watch. `src/mesh-worker-execution.mjs:1771-1772` forwards `watchTranscriptSessionId` and `watchTranscriptCompletion` as unconditional bare shorthand, and `composeDirectiveLaunchOptions` (`:205-220`) returns only `{ unattended, declaredLaunch }`. The launched `aof work loop` writes its inner sessions into the same projects directory the default watch is aimed at, so the run is killed roughly ten seconds after its FIRST inner session and reported `done`. This is `ARCHITECTURE.md#ADR-013` §1, diagnosed at 63/03's structural review and left open by design; the follow-up story it routes to does not exist under `63/stories/`. | defect | **blocker (milestone)** | ADR-013 §1's own routing, unchanged: NOT tech debt, and NOT a 63/03 edit — a follow-up story in 63 whose fix relaxes exactly two of the six fenced forwards (ADR-013 §3a) and owes the red probe for the relaxed form. No `@executable` lane can see this: every delivered lane injects a scripted PTY that exits on the tick after spawn, so no inner transcript ever appears. | **63/06** (`the-loop-launch-is-watched-as-a-loop`) | **closed 2026-09-02** — 63/06 verified and accepted (`status: done`); the ADR-013 §1 leg and the §3a red probe are both recorded on FF-6306, and the seam was re-read at source after the fix. |
| F-63-B | 63/03's `@manual` lane `tasks/03_an-old-worker-idles-rather-than-doing-the-wrong-work.feature` has not been run, and the story's own task box is unticked. All five of its scenarios need a genuinely skewed real pair — the WSL worker re-deployed from a pre-63/03 payload, both build stamps read on the nodes AND in each daemon's startup line, then live autonomous assignments dispatched and watched to settlement. | verification gap | **blocker (63/03)** | Operator-attended: daemon restarts and node re-deploys are the operator's (`.claude/rules/build-deploy-restart.md`). 63/03 stays `in-review` until it is observed. Simulating the old worker by stripping a field is explicitly refused by the feature's own prose. | 63/03 | **open — PARTLY OBSERVED 2026-09-03.** Scenario 1 (the pair is genuinely skewed) PASSES. Outline row 1 PASSES on every clause including settlement, and with it the scenarios *the idle session is observable*, *nothing anywhere substitutes the cascade* and *the waste is bounded by the deadline that already existed*. Row 2's typed command PASSES verbatim (ending deliberately curtailed). Rows 3 and 4 are BLOCKED on **F-63-P** (the App is installed on `Vendorco-ai`; this workspace's repo is under `UmairB`), so `tasks/03` stays unticked. |
| F-63-C | 63/02's `@manual` scenario is PARTIALLY observed. Three of its four clauses were seen over a verbatim unattended run of the resolved argv; the *"walks the continue, validate, doctor, grade and verify gates in that order"* clause was not, because the shipped declaration ships `L1` and `L1` is report-only. Seeing that clause needs an unattended loop that actually drives an item at `L2`/`L3` over live work. | verification gap | non-blocker | Deferred to the milestone gate, where an integrated unattended run belongs and where F-63-A's fix has to be observed end to end anyway. Not a defect in 63/02's diff: its `@executable` legs prove the envelope, and the compiled artifact was read directly. | 63 (milestone gate) | open |
| F-63-D | `ARCHITECTURE.md` is **1,823** lines (re-measured 2026-09-02 after ADR-016; 1,758 when first raised) against this repository's configured 1,400-line budget for milestone 63 (`work.doctor.budgets.architecture`); `aof work doctor 63` reports `doc-over-budget`. **Sixteen** ADRs, six of them (ADR-010 and ADR-012 to ADR-016) closure rounds written after the Three Amigos and build passes. | doc budget | non-blocker | Defer to the milestone accept's STATE/ARCHITECTURE compaction, which is the juncture that graduates durable decisions and archives the blow-by-blow. Not tidied now: the milestone is still open and the closure ADRs stay load-bearing while 63/03 is held at `in-review` — ADR-016 in particular is the ruling 63/06 was built against and the one a re-reader needs whole. | 63 (milestone accept) | open |
| F-63-E | The milestone-gate full-suite regression sweep did not complete. The whole unit registry minus the port-binding suite reached test 3,259 of 8,362 (39%) in ~80 minutes with **0 failures** and was stopped, because the tree was needed for the eight red probes and the milestone is not being accepted on this pass. | verification gap | non-blocker | Defer to the milestone gate. The per-story lanes that ARE due for the five stories accepted here ran to completion (288 behavioural + 67 control = 355, all green). The full lane must run to completion before 63 itself is accepted. Note for whoever runs it: `global-work-propagation.test.mjs` binds `:4182` and must stay excluded on this machine. | 63 (milestone gate) | **DISCHARGED 2026-09-02** — re-run to completion on a code-stable tree: **8,388/8,395 in 29m**, and **63 contributes ZERO of the 7 remaining failures** (see F-63-I). The one 63-owned failure the first sweep found is F-63-H, fixed. |
| F-63-F | `work.controls.runners` is not configured anywhere in this repository, so `aof work doctor`'s leg B — *does a runner name this control file?* — did not run for any of 63's eight declared controls; and `work.rubric.report` is not seen either, so `rubric-join-unchecked` fires for all **seven** stories (63/06 included from 2026-09-02). Pre-existing and repository-wide, not a 63 defect: 63 declares eight controls that leg would otherwise check, and this verify substituted a direct import-and-spread check of `scripts/test.mjs` for it (recorded in the evidence table). | tooling gap | non-blocker | Defer. Already ledgered as TECH_DEBT item 69 and flagged in 63's own STATE `## Open, and deliberately so`. | TECH_DEBT item 69 | open |
| F-63-G | Two rows of this document's own fitness register carried per-control test counts that do not match the files. `FF-6302` was recorded `8/8` and declares **7**; `FF-6305` was recorded `12/12` and declares **9**. Neither file has changed since 63/02 (`git log`: last touched `2e9b5498`), so the counts were mis-transcribed rather than gone stale. The register's stated AGGREGATE (`67/67`) is correct and matches a fresh run — it is the per-row cells that were wrong, and their sum (71) never equalled the total they sit above. | record accuracy | non-blocker | Corrected in place at this re-verify, both cells now carrying the measured figure and a note naming this finding. No probe is affected: what each row records as its red probe was applied and observed, and only the green-count transcription was wrong. Worth a moment at the retrospective — an aggregate that disagrees with the sum of its own rows is mechanically checkable and nothing checked it. | 63 (this document) | fixed 2026-09-02 |
| F-63-H | **The milestone-gate sweep caught a regression the per-story lane could not.** 63/06 imports `defaultWatchTranscriptSessionId` from `src/agent-session-driver.mjs` into `test/mesh-assignment-loop-directive.test.mjs` — the positive control that makes its lane load-bearing. That file was not on 53/ADR-015 §2's CLOSED allowlist of test files permitted to name the driver, and it imports the sink too, so it also moved the census's 45-zero-mention / 3-named split. `53/00 task01` went red. Invisible to 63's own lane by construction: the control lives in milestone 53's suite, which a story-scoped run does not include. | defect | blocker (would have been, had it not been fixed in this pass) | Fixed here, by the route the allowlist is built for: `test/mesh-assignment-loop-directive.test.mjs` added to `NAMES_THE_NEW_MODULE` with its reason, and the split moved to 44+4 in both the count and the named list. The import STAYS — removing it would strip the lane's positive control, which is the half `63/06`'s own contract calls load-bearing. `test/agent-session-driver-door.test.mjs` re-runs **19/19 green**. This is exactly the trade `aof:verify` documents for story-scoped suites: caught at the gate, not immediately. | 63/06 (fix applied to 53's door suite) | fixed 2026-09-02 |
| F-63-I | **The working tree is under CONCURRENT modification by another lane, so no full-suite sweep taken from this checkout can be trusted.** Measured by mtime during this pass: `test/story-context-contract.test.mjs` and the untracked `test/arch/acd-prompt-bounds-name-their-home.test.mjs` at 17:57, `src/work-loop.mjs` and `src/command-core.mjs` at 18:04, `src/commands/promote-gap-to-chore.mjs` at 18:06 (its `defaultAt()` DELETED), and `scripts/test.mjs` at 18:13 — the last of these while a sweep was mid-flight. The changed set matches `71/00`'s declared `files:` list. Seven of the sweep's eight failures trace to that in-flight work rather than to 63: `arch/43 ADR-005` (the `defaultAt()` pin), `arch/58 FF-5809` (71/00's own half-built control, on disk and imported but not yet classified), `command-core/00` and `arch/15 ADR-005` (both GREEN in isolation, red only in the sweep), plus `61/03 task 02`, `arch/61 FF-6109` and `build-sea-recipe-guards/F14`, whose subjects were last committed by milestones 61, 61 and PR #7. | verification gap | **blocker (milestone gate)** | 63 cannot discharge F-63-E from this checkout. The sweep must be re-run from a tree nobody else is writing — an isolated worktree or clone at 63's merge commit, or this one once 71/00's lane is quiet — and the seven non-63 failures triaged to their own owners, not to 63. The second sweep launched this pass was STOPPED rather than reported, because it was measuring a moving target. | 63 (milestone gate) / other lanes | **discharged for 63's gate 2026-09-02** — re-run once the code tree was quiet (85 min since the last `src`/`test`/`scripts` write; `git status` compared before and after, and only `wiki/work` moved: milestone 72's records and 71/01's `OUTCOME.md`, which is what perturbed the two `70/05` rows). **All 7 remaining failures fail in ISOLATION too, and none has a 63 file as its subject**: `arch/58 FF-5809` (71/00's own untracked control, imported but unclassified), `70/05 task02` x2 (walk the work tree other lanes are writing), `61/03 task 02` and `arch/61 FF-6109` (milestone 61), `build-sea-recipe-guards/F14` (PR #7), `arch/43 ADR-005` (the `defaultAt()` pin, broken by today's promote-gap refactor). Two failures the FIRST sweep showed — `command-core/00` and `arch/15 ADR-005` — are green here, confirming they were artefacts of the tree being written mid-run. **The 7 are routed to their own owners, not to 63.** |
| F-63-J | **The WSL worker node's `claude` is resolvable but UNAUTHENTICATED** — `Failed to authenticate: OAuth session expired and could not be refreshed`, reproduced BOTH in the daemon-shaped environment the rules prescribe (`env -i PATH=/usr/local/bin:/usr/bin:/bin`) and in a login shell, so it is a genuinely expired session rather than a stripped-environment artefact. This is the exact species `.claude/rules/build-deploy-restart.md` records as having burned the Mac's runs: the binary is found, the run starts, then dies on auth. | environment | **blocker (63/03 `tasks/03` scenarios 2-5)** | The worker daemon was **deliberately not started**. Starting it would have produced auth-dead sessions, and scenario 3's whole job is to distinguish *"the worker did not understand the directive"* (a live session, empty prompt, no output) from *"the worker is gone"*. An auth-dead session is NEITHER, so dispatching into one would not have been a weaker observation — it would have been a fabricated one. Clearing this needs an interactive OAuth login on the worker (`claude` login inside the distro), which no agent can perform. | operator (WSL node) | **cleared 2026-09-02** — operator re-authenticated; the daemon-shaped probe (`env -i PATH=/usr/local/bin:/usr/bin:/bin`) now answers `MESH_AUTH_OK`, exit 0. |
| F-63-K | **`aof --version` reports a build id derived from git HEAD, not from the files actually deployed.** Measured on the WSL worker: it reported `source 2546a06+dirty` (a 2026-07-27 commit) while its working files were whatever a later deploy had copied over that checkout. The `+dirty` suffix flags divergence but names no vintage, so two nodes can compare stamps, see a difference, and still not know what either is running. | defect | non-blocker (but it bites THIS lane) | 63/03 `tasks/03` scenario 1 makes the build stamps the evidence that a pair is genuinely skewed, so a stamp that does not describe the payload weakens the scenario's own premise. Worked around here by verifying the skew **at the source** — grepping the worker's `mesh-worker-execution.mjs` for the loop-directive identifiers — rather than trusting the id. Not 63's defect; belongs to whoever owns `src/build-info.mjs`. | mesh / build-info owner | open |
| F-63-L | **`aof mesh invite` emits a LOOPBACK relay URL and a `joinCommand` that omits `--url`.** The minted invite carried `relayUrl: ws://127.0.0.1:4182/ws/relay` and `joinCommand: aof mesh join <code> --control win-host-a`. A remote node following that command verbatim points its stream at **its own localhost**. Separately, the worker had **no relay credential at all** and every connection was closed by the control (`socket hang up`, ~1,000/sec with no backoff) with **nothing logged control-side** to say why. | defect | non-blocker for 63 | Worked around by joining with an explicit `--url ws://192.168.1.102:4182/ws/relay`, after which the worker connected with zero failures. Three separable problems for the mesh owner: the invite's URL is loopback rather than a reachable address; the retry loop has no backoff and wrote **3.5M** log lines; and an admission refusal is invisible on the control, which is what made this cost an hour. Not 63's defect. | mesh owner | open |
| F-63-O | **A settled assignment does not say what settled it.** The row-1 idle session settled `state=failed` with `code=null` and `session_id=null`, and neither the worker log nor the control log carried a line naming the deadline that fired. Attribution was only possible by arithmetic — the 20m03s lifetime matching `DEFAULT_HEARTBEAT_MS` + `DEFAULT_STARTUP_GRACE_MS` — which works for a clean single case and would not survive two candidate deadlines with similar values, a clock skew, or a config override. | diagnosability | non-blocker | Not 63's defect and not in its write set. It is the same family as F-63-L's silent admission refusal: the mesh settles and refuses without recording why, so every diagnosis becomes an inference. A terminal state deserves the deadline name in its `code`, and a settlement deserves one log line. Route with the other mesh findings. | mesh owner | open |
| F-63-P | **The GitHub App is installed only where the repo ISN'T, so the cross-machine PUSH path cannot work for this workspace.** Queried as the App itself (JWT against `GET /app/installations`): there is **exactly one** installation — `147060638`, account **`Vendorco-ai`**, `repo_selection=selected`, `{"contents":"write","metadata":"read"}`. There is **no installation on the `UmairB` account**, and the `aof` workspace's registered `cloneUrl` is `https://github.com/UmairB/agent-orchestration-framework.git`. The mint is keyed on the workspace's registered cloneUrl, so a write-credential request for this workspace has no installation to mint against and fails `write-credential-mint-failed`. Row 1 was unaffected only because the worker already held the repo locally (its own `~/source/aof` clone) and never requested a clone credential at all. | environment | **blocker (63/03 `tasks/03` rows 3 and 4)** | Not 63's defect and not in its write set. The upgraded worker performs a gate-time push of the item branch before the run starts, so BOTH remaining rows need this. Clearing it is an environment change only the operator can make: install the App on the `UmairB` account with `contents:write`, or point the workspace at a repo inside `Vendorco-ai`, or configure a credential provider that suits a local origin. Note the worker's actual `origin` is `file:///mnt/c/Source/umami/aof` — a local path needing no GitHub credential at all — so this is the same shape as F-63-M: a github-app provider applied where no GitHub repo is really in play. | operator / mesh owner | open |
| F-63-Q | **A worker with no descriptor for the assigned workspace clones the WRONG REPOSITORY instead of refusing.** Dispatching a **private workspace**'s item `00` (workspace `1f164bd03ea535da`) to the worker produced: `git clone failed for workspace "1f164bd03ea535da": remote: Repository not found. fatal: repository 'https://github.com/UmairB/agent-orchestration-framework.git/' not found` — the clone URL of a DIFFERENT workspace (the worker's own launch workspace, `9db1fd84f5895e38`). The control-side resolver `createResolveWorkspaceCloneUrl` (`src/mesh-launcher.mjs:184`) is correct: it keys by `workspaceId` and returns `null` rather than falling back. The substitution happens worker-side, where the assigned workspace is unknown (the worker's `~/.aof/mesh/workspaces/` holds no `1f164bd03ea535da`) and resolution falls through to its own launch workspace's URL. **It failed safe only by luck** — the token minted for one installation had no access to the substituted repo. Had the worker held credentials for both, it would have cloned the wrong repository, branched it, and run an assignment against it. | defect | **blocker (63/03 `tasks/03` rows 3 and 4)** | Not 63's defect. A missing workspace descriptor must be a coded refusal naming the workspace, never a fallback to whatever repo the daemon happens to have been launched in — the same "never borrow a sibling workspace's identity" rule ADR-011 already pins for the App identity, which the cloneUrl path does not enforce on the worker side. Route with the other mesh findings. | mesh owner | open |
| F-63-N | **The cross-machine mesh path has NO automated cover, and that is a gap in the suite rather than a property of it.** Every mesh test in the registry drives in-process fixtures on ONE host, so three whole classes are unreachable by construction: a value that is correct on loopback and wrong for a remote peer (F-63-L), a frame that is well-formed when a fixture builds it and malformed when a real peer sends it (F-63-M), and an identity that is derived correctly but describes the wrong artefact (F-63-K). All three were latent for a month and were found by hand in one evening. The manual lane `63/03 tasks/03` was written knowing this hole existed — *"simulating one by stripping a field from a frame proves only that the simulation was written correctly"* — but a `@manual` lane is a periodic instrument, not a guard: it only fires when someone runs it. | test gap | non-blocker for 63, but it is the reason 63 cannot close | Needs a genuine two-node lane in CI or a scheduled soak — a second node (a container or the WSL distro) doing enrol -> dispatch -> settle against a real relay, so admission, frame shape and payload identity are exercised by a machine rather than by an operator who happens to look. Not 63's to build. Route with F-63-K/L/M to the mesh owner. | mesh owner | open |
| F-63-M | **A dispatched assignment never reaches a session, so scenarios 2-5 cannot be observed at all.** With the pair standing and correct, milestone `00` was assigned to the worker in the mesh test-bed (`52294b307214c27d`) with the phase side-table set to `autonomous` (the worker held down across the assign so no dispatch race existed). The directive reached the worker, which then failed at repo preparation: `assignment-repo-unavailable: clone credential request failed ... clone-credential-request was refused by control (code=clone-credential-mint-failed)`. Control-side: *"Refused a clone-credential-request frame from win-host-a-wsl for workspace **(none)** — this node has no registered descriptor for that workspace, so the frame's items were DISCARDED."* The worker names the workspace in its own error; the control reads none from the frame. Re-joining the worker to that workspace explicitly (`aof mesh join <code> --workspace 52294b307214c27d`, after which it listed the workspace locally) did **not** change the outcome. The workspace's `cloneUrl` is `file:///mnt/c/Source/umami/aof-test-repo`, reachable from the guest, so no GitHub App mint is involved. | configuration + defect | **was blocking; ROOT-CAUSED and worked around 2026-09-03** | **The refusal message was misleading and cost the most time.** `onFrameSkipped` (`src/mesh-launcher.mjs:1003`) is a GENERIC reporter whose text hardcodes *"this node has no registered descriptor for that workspace, so the frame's items were DISCARDED"* and prints `workspace (none)` — but it fires for ANY skip code, including `clone-credential-mint-failed`, which concerns neither descriptors nor items. Acting on that text (re-joining the worker to the workspace) was wasted work. **The real cause**: the control runs `mesh.repo.credential.provider = "github-app"` globally, and the test-bed workspace's `cloneUrl` is `file:///mnt/c/Source/umami/aof-test-repo` — a local path the App mint cannot scope a token to, so it throws, and by design a throw becomes the loud `clone-credential-mint-failed`. A `file://` repo needs no credential at all, and the frame already supports an explicit `credential: null` "not needed" reply, which the env-token provider returns and the github-app provider has no path to. **Worked around** by dispatching in the `aof` workspace instead (`cloneUrl https://github.com/UmairB/agent-orchestration-framework.git`), where the mint succeeds and the clone lands — which is how row 1 came to be observed. Two separable defects for the mesh owner: a generic reporter asserting a specific cause, and github-app having no "this repo needs no credential" path. Not 63's defect — nothing in 63's write set touches relay frames or clone-credential minting. Diagnosis is additionally hampered by the store pollution: the control emits ~11,000 `workspace-workdir-unresolvable` lines per scan, which buried the one refusal line that explains this. **Scenarios 2-5 are unobservable until the mesh itself is repaired**, and that repair is its own work, not 63's. | mesh owner | open |


### The milestone blocker's fix, landed AND ACCEPTED 2026-09-02 (register entry `F-63-A` above)

Routed exactly as `ARCHITECTURE.md#ADR-013` §1 requires: a follow-up story in 63, not an edit inside
63/03. **`63/06` was created, built, reviewed and — at this re-verify — VERIFIED AND ACCEPTED
(`status: done`, box ticked in `SPEC.md#Stories`, `OUTCOME.md` authored).** The re-verify's decision,
recorded below, is that the blocker is closed.

- **The change.** `loopShapedTranscriptWatch(launchOptions)` (`src/mesh-worker-execution.mjs`) returns
  `null` for every session launch and, for an unattended loop, both watch seams as functions resolving
  `null`. The two forwards at the spawn bag became launch-conditional in exactly ADR-013 §3a's licensed
  form — `watchTranscriptSessionId ?? loopWatch?.watchTranscriptSessionId` and its sibling — so an
  injected seam still wins and only the SESSION-SHAPED DEFAULT is replaced. `src/agent-session-driver.mjs`
  is **byte-unchanged**; `ptySpawn`, `which`, `onOutputChunk` and `onSessionEnd` stay bare shorthand.
- **What settles a loop run** is now decided rather than left open: `term.onExit` and the delivered exit
  mapping (`ARCHITECTURE.md#ADR-016` §2). No thirteenth stop, no new completion signal, no halt
  vocabulary in the mesh files.
- **Evidence.** `test/mesh-assignment-loop-directive.test.mjs` gained two driven lanes, the first
  carrying its own positive control: the session-shaped default is run over the loop run's own worktree
  and **binds** a planted inner-session transcript, and only then is the loop-shaped seam shown to bind
  nothing over that same directory. The second proves an injected seam still wins.
- **Re-run 2026-09-02:** the milestone lane is **357/357 green** (288 → 290 behavioural, 67 control);
  the mesh worker / assignment / launcher suites are **221/221 green**; `aof work validate 63` is PASS.
- **Red probe** for the relaxed form (owed by ADR-013 §3a) is recorded in the fitness register above.
- **`FF-5302`'s shrink-only sink ratchet** moved 2462 → 2482 as an ADR decision (`ARCHITECTURE.md#ADR-016` §5),
  which is the route that control names for a raise; no headroom was taken, so the next line still has to be
  argued for. Whole-lane re-run after the raise: **`test/arch/**` 1,527/1,527 green**, and the 63 behavioural
  lane plus every mesh worker/assignment/launcher/terminal suite **568/568 green**.

**Still open and unchanged by this fix:** F-63-B, F-63-C, F-63-D, F-63-E, F-63-F. F-63-C in particular is
NOT discharged by this work — the fix removes the wrong completion signal; it does not substitute for
observing an unattended loop drive the gate order end to end.

---

## Accept decision

**2026-09-03 — milestone 63 is ACCEPTED. All seven stories are done.**

**Accepted (`status: done`, boxes ticked in `SPEC.md#Stories`, each carrying its own `OUTCOME.md`):**
63/00, 63/01, 63/02, 63/03, 63/04, 63/05, 63/06.

**The gates that were met, in full.** `aof work validate 63` PASS. `aof work doctor 63` reports **no
`control-unresolved` finding at either severity** and no residual `pending` marker — all eight declared
controls resolve. The milestone lane is **363/363 green**. The full-suite gate ran to completion on a
code-stable tree: **8,388/8,395, with 63 contributing ZERO of the seven remaining failures**, every one
of which reproduces in isolation and has a non-63 subject. All eight controls carry a recorded red
probe. ADR-001 §4's non-vacuity condition and ADR-005's compile condition are both discharged.

**The gate that was NOT met, and the decision that closed it anyway.** `63/03 tasks/03`'s live
skewed-pair lane is observed on **two of its four** Outline rows. Row 1 (*new control, old worker,
`autonomous`*) passes on every clause including settlement, and carries with it three whole scenarios —
*the idle session is observable*, *nothing anywhere substitutes the cascade*, and *the waste is bounded
by the deadline that already existed*. Row 2's typed command is observed verbatim. Rows 3 and 4 are not
observed: both need the upgraded worker, whose gate-time push requires a credential this environment
cannot mint for any workspace the worker is enrolled in. Three separate attempts each hit a different
environmental wall (F-63-P, F-63-Q, F-63-M).

**This is an OPERATOR DECISION, taken 2026-09-03, and it is recorded as one rather than dressed up as
evidence.** The operator elected to accept 63 rather than hold finished, green work behind another
subsystem's setup. What that decision does and does not do:

- It does **not** tick `tasks/03`'s task box. An unobserved lane must never read as observed, and the
  box stays `- [ ]`. The shipped `.feature` is untouched.
- It does **not** claim the two rows passed. They are recorded as not observed, with the reason.
- It **does** carry the obligation forward as an open gap with a discharge condition on `63/03`'s
  `OUTCOME.md`, and again at milestone level on `63`'s own — so a later reader meets it as declared
  state rather than discovering it as a silence.

**What this milestone's live lane cost, and returned.** Running it surfaced seven mesh findings
(F-63-K … F-63-Q), none of them 63's. One is a latent data-corruption risk: a worker with no descriptor
for its assigned workspace clones the **wrong repository** (F-63-Q), which failed safe here only because
the minted token happened to lack access. Those are routed to the mesh owner and are the concrete agenda
for whichever milestone repairs that subsystem.

**Findings at close.** Closed: F-63-A. Fixed: F-63-G, F-63-H. Discharged: F-63-E, F-63-I, F-63-J.
Open and routed onward: F-63-B (rows 3-4, now a declared gap), F-63-C, F-63-D, F-63-F, and the mesh set
F-63-K, F-63-L, F-63-M, F-63-N, F-63-O, F-63-P, F-63-Q.

**Close obligations, all discharged at this accept.** `RETROSPECTIVE.md` written — 12 lessons (R1-R12)
distilled from the 64 STATE feedback notes plus this pass's own findings. `aof work memory ingest` run
(1,751 records reindexed), so those lessons are recallable in the next milestone's refine. `STATE.md`
compacted: its `## Feedback (for retro)` section archived, its lessons having graduated. Milestone
`OUTCOME.md` authored, citing each story's outcome rather than restating it.
