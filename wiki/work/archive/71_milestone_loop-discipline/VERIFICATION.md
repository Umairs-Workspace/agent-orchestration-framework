---
doc: verification
---
<!--
  Milestone VERIFICATION.md — answers ONE question: what evidence do we have that this milestone
  works? Owner: aof:verify (the product owner is the SINGLE WRITER of the findings register).
  Opened at 71/00's accept (2026-09-02); the evidence rows are filled as each story lands, and the
  accept decision at the close.
-->
# 71 · Loop discipline — Verification

## Fitness functions

<!-- CITING register (66/ADR-008 ruling 4): every row resolves to a declaration in the sibling
     ARCHITECTURE.md `## Fitness functions` register and declares nothing itself. The red-probe cell
     records WHAT WAS CHANGED to make the control fail and THE MESSAGE OBSERVED.

     A control must fail when the invariant it guards is broken, so the probe is that assertion's
     positive control, and this row is where a reviewer reads that it was observed failing. An
     assertion nobody has ever seen red cannot be falsified by its own green.

     An untouched placeholder cell is a MISSING red probe, not a recorded one. A row whose story has
     not landed reads `not yet landed` and owes its probe at that story's accept, not here. -->

| id | enforced by | result | red probe |
|---|---|---|---|
| FF-7101 | `test/arch/acd-prompt-bounds-name-their-home.test.mjs` | **green** — 6/6, 2026-09-02 (71/00) | **Two probes, both on `src/bundle/commands/continue.md`.** *Leg (c)* — deleted the citation `— work.loop.buildNoProgressRounds, the bound's one home in src/loop-bounds.mjs —` from the build terminator. RED: *"commands/continue.md: the bound fact \"the build no-progress stop\" states no home — `work.loop.buildNoProgressRounds` is absent from the sentence that states it"*, and the leg-(b) plant test reddened with it. *Leg (b)* — changed `Reaching **2 consecutive no-progress rounds**` to `**3**`. RED: *"commands/continue.md: states 3 for `work.loop.buildNoProgressRounds`, but work.loop.buildNoProgressRounds resolves to 2"*, on the census test AND on the out-of-reach-by-construction test. Each probe run in a FRESH `node` process (the control snapshots source text at module load), then reverted; file sha256 `9256575e…475433e3` before and after both. |
| FF-7105 | `test/arch/acd-prompt-gate-ladder-parity.test.mjs` | **green** — 3/3, 2026-09-02 (71/00) | Deleted the second rung — the `aof work doctor <ref>` bullet — from `continue.md`'s `<gate_ladder>` block, leaving a one-rung prompt ladder against the shell's two. RED: *"src/bundle/commands/continue.md: the shell walks `work:doctor` and the prompt's ladder does not name it"*, and the both-directions/order test reddened with it — which is the leg that proves the parity is derived from `invokeGateLadder` rather than from a literal pair. Fresh process, then reverted; file sha256 `9256575e…475433e3` before and after. |
| FF-7102 | `test/arch/acd-render-lane-is-gated.test.mjs` | **green** — 3/3, 2026-09-03 (71/03) | **Four probes across the three legs.** *Leg (a), the plant the control's own comment names* — deleted ` --window-size=<W>,<H>` from `verify.md`'s render invocation. RED: *"src/bundle/commands/verify.md: the render INVOCATION itself carries --window-size="* — and the file-wide breakpoint prose one paragraph below did NOT rescue it, which is the assertion that proves the flags are read inside the invocation rather than anywhere in the file. *Leg (a), second* — changed `continue.md`'s invocation to `npx playwright screenshot --headless=new`. RED: *"no file under src/bundle/ names the superseded `npx playwright` render"*. *Leg (b)* — planted a `--headless=new` sentence ABOVE the precondition bullet in `verify.md`'s design step, leaving both present and only the ORDER inverted. RED: *"src/bundle/commands/verify.md: the precondition is stated before the render, not after it"* — the leg is genuinely an order assertion, not a presence one. (Leg (a) reddened with it, because the invocation regex takes the FIRST backticked `--headless=new` span and the plant's carried no `--screenshot=`.) *Leg (c)* — re-added a positive `npx playwright` render assertion to `acd-conformance-verdict-contract.test.mjs`. RED: *"test/arch/acd-conformance-verdict-contract.test.mjs: no surviving assertion REQUIRES the superseded npx playwright render"*. Each probe run in a FRESH `node` process, then restored from a pre-probe copy; sha256 `9dd8d5f5…c1a64174` (`verify.md`), `4856309e…be420690` (`continue.md`) and `09ed258d…333e96af` (the 07 control) proven equal before and after every probe. Control **3/3 green before the first plant and 3/3 green over the restored tree**. |
| FF-7103 | `test/arch/acd-promotion-creates-one-type.test.mjs` | **green** — 4/4, 2026-09-02 (71/01) | **Three probes, one per leg.** *Leg (a)* — in `src/commands/promote-finding-to-chore.mjs` changed `type: PROMOTED_TYPE` to `type: "milestone"`. RED: *"src/commands/promote-finding-to-chore.mjs: names the insert type "milestone" — the loop creates only "chore""*, and the leg-(a) plant test reddened with it (2/4). *Leg (b)* — inside `continue.md`'s `<finding_triage>` region planted the sentence *If it needs criteria, run aof work insert-story --under 71 and refine it.* RED: *"the review close names `insert-story` — the loop creates no story and no milestone"* AND *"the review close names `aof work insert-story` — its only creating verb is promote-finding"* (3/4). *Leg (c)* — added `at: { type: "number" }` to the finding face's declared input properties. RED: *"src/commands/promote-finding-to-chore.mjs: declares an `at` input — the loop chooses no position and no type"*, on the shipped-tree test AND on the seam-separation test (2/4). Each probe run in a FRESH `node` process, then restored from a pre-probe copy — two of the three probed files are untracked, so `git checkout` was not the restore — with sha256 `95a863e3…044d470f` (the face) and `9256575e…475433e3` (`continue.md`) proven equal before and after every probe. |
| FF-7104 | `test/arch/acd-one-promotion-engine.test.mjs` | **green** — 3/3, 2026-09-02 (71/01) | **Three probes, one per leg group.** *Legs 1+2* — appended a second Definition-of-Done heading regex (the one `chore-seed.mjs` defines) to the finding face. RED: *"the Definition-of-Done seed: expected exactly one home at src/work-promote/chore-seed.mjs, found src/commands/promote-finding-to-chore.mjs, src/work-promote/chore-seed.mjs"* (2/3). *Leg 3* — wrote a third module `src/zz-probe-rival.mjs` holding the string `## Definition of Done` and a write to `CHORE.md`. RED: *"src/zz-probe-rival.mjs: matches the promotion signature (seeds a Definition of Done AND writes a chore record doc) outside src/work-promote"* (2/3); the file was removed afterwards. *Leg 4* — prepended `import { runInsertTopLevel } from "../commands/insert-shared.mjs";` to `src/work-promote/promotion.mjs`. RED: *"src/work-promote/promotion.mjs: imports from ../commands/ — the promotion engine must stay a leaf"* (2/3). Fresh process each, restored from a pre-probe copy; sha256 `95a863e3…044d470f` (the face) and `5560732a…703cb6f4` (the engine) equal before and after. |
| FF-7106 | `test/arch/acd-declared-writes-include-generated-siblings.test.mjs` | **green** — 6/6, 2026-09-03 (**chore 89**, the item that owns it — see **F-71-A**) | **Three probes, each on a different load-bearing claim.** *The SUBJECT* — added `src/bundle/commands/continue.md` to a real OPEN story's `files:` (85, `not-started`) without the manifest and without its renders. RED, naming the story and all four missing siblings: *"85_story_records-follow-the-story/STORY.md: declares bundle member src/bundle/commands/continue.md and must also declare its tracked render .claude/commands/aof/continue.md"*, the same for `.codex/skills/aof-continue/SKILL.md` and `.opencode/commands/aof/continue.md`, and *"…must also declare src/bundle/manifest.json"*. *The DERIVATION* — reverted the per-member render to keying on the OUTPUT's own resource id (the defect measured while writing the control: the codex render presents as `skill:aof-continue`, not `command:continue`). RED on two legs: the tracked-render count drops to two, and *"every rendered output is attributable to a member"* fails. *The HORIZON READ* — replaced the status read with the obvious `line.slice("status: ".length)` shape, which keeps the `\r` of a CRLF checkout. RED with the live leg reporting **33 violations across four `done` stories** (59/04, 61/01, 83, 84) — the horizon silently widened to the whole stream. Each probe in a FRESH `node` process, restored from a pre-probe copy, sha256 proven equal before and after (`a7f48b91…b26bcd8f` for the story, `dd183bd5…d3128c0e` / `fe57b178…129c833e` for the control across its two revisions). **6/6 green before the first plant and over the restored tree.** |

**Probe procedure.** Each probe was applied to the working tree, the control run in a FRESH `node`
process, then reverted and the restore proven by sha256 before/after. The story's whole lane was
re-run over the restored tree afterwards: **41/41 green**.

**71/01's probes (2026-09-02) followed the same procedure**, with one difference recorded because it
matters: two of the three probed files are untracked in git, so every restore was from a pre-probe
copy rather than `git checkout`, and the sha256 before/after is the proof of restore. Both controls
were re-run green over the restored tree afterwards (FF-7103 **4/4**, FF-7104 **3/3**), and the whole
71/01 lane was green before the probes began: **94/94**.

---

## Verification evidence

<!-- Filled story by story as each lands, and at the milestone gate. Pointers and measurements, not
     restatements of the contract. -->

| subject | lane | what was run | result | contract |
|---|---|---|---|---|
| 71/00 | `@executable` | the story's own lane through the registries' own exported arrays, each test under its own hermetic `AOF_GLOBAL_HOME`: FF-7101 (6), FF-7105 (3), `test/story-context-contract.test.mjs` (12), and the four neighbours this story's diff touches — `acd-loop-cap-single-home` (10), `acd-bundle-manifest-hashes` (3), `acd-bundle-membership` (3), `acd-test-suite-registration` (4) | 2026-09-02: **41/41 green**, 0 failures. The two 71/00-specific behavioural assertions are named in the runner output: *"the build step states BOTH terminators — success in all three legs, and the failure-to-progress stop with its round accounting"* and *"the gate ladder step says what each rung's answer decides, and a red gate is its own outcome"*. | 16 `@executable` scenarios across 3 task features |
| 71/00 `tasks/01` | `@executable` | the sole-writer leg, read at source: `test/story-context-contract.test.mjs:325` asserts the build step matches neither `hard cap` nor `review round`; `:328` asserts the review-rounds region matches neither `buildNoProgressRounds` nor `failing-scenario count` | 2026-09-02: green. The Notes' hard requirement holds — `Three rounds is the hard cap` is present exactly once in each of `src/bundle/commands/continue.md` and `code-review.md`, and is asserted at `:202` and `:207`. | 71/00 STORY.md `## Notes` |
| 71/00 | registration | both new controls imported AND spread in `scripts/test.mjs`'s suite registry | 2026-09-02: imported at `scripts/test.mjs:1076-1077`; `acd-test-suite-registration` (4/4 green) is the check that a suite is not imported-and-not-spread, and it reports no unregistered suite. | ARCHITECTURE.md HARNESS SHAPE |
| 71/00 | generated siblings | `renderBundleOutputs(await loadBundle(), { runtimes: ["claude","codex","opencode"] })` compared byte-for-byte against the six git-tracked runtime copies this story declares | 2026-09-02: **6/6 IN SYNC**, 0 stale — `.claude/commands/aof/{continue,code-review}.md`, `.codex/skills/aof-{continue,code-review}/SKILL.md`, `.opencode/commands/aof/{continue,code-review}.md`. The shipped runtime prompts an agent actually reads carry `<gate_ladder>`, `<build_terminator>`, the `work.loop.buildNoProgressRounds` citation and the `MAX_REVIEW_ROUNDS` clamp. `src/bundle/manifest.json` is current — `acd-bundle-manifest-hashes` green on all three legs. **This is the claim FF-7106 would ratchet; it was measured by hand here because that control does not exist (F-71-A).** | 71/00 STORY.md `files:`, ADR-008 |
| 71/00 | gate | `aof work validate 71/00` | 2026-09-02: **PASS — 71/00 is well-formed.** | — |
| 71/00 | gate | `aof work doctor 71/00` | 2026-09-02: **no `control-unresolved` finding at either severity.** Two warns, neither a 71/00 acceptance gate: `numbering-gap` (top-level driver sequence) and `rubric-join-unchecked` (no `work.rubric.report` configured). Loop-Ready **80% (8/10)** — clears L1. | ARCHITECTURE.md `## Fitness functions` |
| 71 (milestone) | gate | `aof work validate 71`, `aof work doctor 71` | 2026-09-02: validate **PASS — 71 is well-formed.** Doctor: one **error**, `verification-register-missing` — *"6 fitness function(s) are declared … and VERIFICATION.md carries no `## Fitness functions` register"* — **cleared by this document**. Two `control-unresolved` warns remain, both `pending`-marked: FF-7102 (71/03, unbuilt) and FF-7106 (**F-71-A**). Loop-Ready 70% (7/10). Recorded here because it is the milestone's gate, not 71/00's. | — |
| 71/00 `tasks/00` | `@manual` | *a real story's continue pays the gate once and skips the reviewers on red* | **PARTIALLY RUN — see F-71-B.** *Observed:* the ladder's cost, measured against a genuinely red item — `aof work doctor 71` returns an error-severity finding in **~1s**, as does `aof work validate 71/00`, so the two rungs together cost seconds against the tens of minutes three review lanes cost. *Not observed:* the clauses that need a live `aof:continue` run against a story with a known validate finding — that the run records the rung that stopped it, and that no `aof-architect`/`aof-qa`/`aof-designer` session was spawned for that attempt. No story in the stream is red at validate today, so the lane needs one constructed and a real build driven. | 71/00 `tasks/00` `@manual` scenario |
| 71/00 `tasks/01` | `@manual` | *a real build that cannot converge stops itself and says so* | **NOT RUN — see F-71-B.** The precondition is a build whose failing-scenario count stops falling, which is not a state that can be induced on demand without fabricating it; fabricating it would prove the fabrication. Due when a real build hits the bound. | 71/00 `tasks/01` `@manual` scenario |
| 71/00 | tag census | the story's 3 task `.feature` files | 2026-09-02: **16 `@executable` scenarios, 2 `@manual`, and no `@uat`** — so **no human sign-off step applies to this story** and none was solicited. Milestone 71 carries no `DESIGN.md` and no UI surface, so no design-conformance review applies and no render/designer/QA hand-off was run. | 3 task features |
| 71/01 | `@executable` | the story's own lane through the registries' own exported arrays, each test under its own hermetic `AOF_GLOBAL_HOME`: `test/promote-finding-to-chore.test.mjs` (13 — all three task features; the triage rule driven directly, `work:promote-finding` invoked through the command core against a temp stream and read back from `CHORE.md`), FF-7103 (4), FF-7104 (3), 39/03's untouched `test/promote-gap-to-chore.test.mjs` (9 — the control on the extraction), the three closed-list guards a new `work:*` verb reds (`command-core-contract` 26, `acd-work-command-route-coverage` 4, `acd-work-command-cli-bijection` 4), the bundle pair (`acd-bundle-manifest-hashes` 3, `acd-bundle-membership` 3), `acd-test-suite-registration` (4), and the three `continue.md` neighbours (`story-context-contract` 12, FF-7101 6, FF-7105 3) | 2026-09-02: **94/94 green**, 0 failures. | 18 `@executable` scenarios across 3 task features |
| 71/01 | registration | the story suite and both new controls imported AND spread in `scripts/test.mjs`'s suite registry | 2026-09-02: imported at `scripts/test.mjs:1081-1083`, spread at `:3680-3682`; `acd-test-suite-registration` (4/4) reports no unregistered suite. | ARCHITECTURE.md HARNESS SHAPE |
| 71/01 | generated siblings | `renderBundleOutputs(await loadBundle(), { runtimes: ["claude","codex","opencode"] })` intersected with `git ls-files` — 143 tracked outputs compared, the three `continue.md` renders this story declares read byte-for-byte | 2026-09-02: **`.claude/commands/aof/continue.md`, `.codex/skills/aof-continue/SKILL.md` and `.opencode/commands/aof/continue.md` all byte-exact**; no tracked render output is stale (31 differ by CRLF alone). `src/bundle/manifest.json` is current — `acd-bundle-manifest-hashes` green on all three legs. Measured by hand again because FF-7106 does not exist (F-71-A). | 71/01 STORY.md `files:`, ADR-008 |
| 71/01 `tasks/01` | append position | `appendPosition()` against 39/03's count-derived `defaultAt`, measured read-only over THIS repository's stream | 2026-09-02: 87 top-level items, highest number 87 (`42_structural-overhaul` is not an item). Count-derived answer **87 — would have renumbered one item**; the engine answers **88 and shifts nothing**. The story's Notes measured the gap as "ten" against the driver sequence; against the full top-level item set it is one. The defect is real either way and the fix is strictly safer. | ADR-003 "a promotion appends and shifts nothing" |
| 71/01 | gate | `aof work validate 71/01` | 2026-09-02: **PASS — 71/01 is well-formed**, before and after the record edits at this accept. | — |
| 71/01 | gate | `aof work doctor 71/01`, `aof work doctor 71` | 2026-09-02: **no `control-unresolved` for FF-7103 or FF-7104 at either severity** — both files resolve, and their stale `pending` markers were struck (F-71-E). At the milestone the two `control-unresolved` warns are unchanged: FF-7102 (71/03, unbuilt) and FF-7106 (F-71-A). Story-level warns: `numbering-gap`, `rubric-join-unchecked` — neither an acceptance gate. Loop-Ready 80% (8/10). | ARCHITECTURE.md `## Fitness functions` |
| 71/01 `tasks/02` | `@manual` | *a real capped review pass leaves a legible queue* | **NOT RUN — see F-71-D.** The precondition is a story whose review reaches its cap with surviving Important findings, under the shipped `<finding_triage>` region. 71/01's own review pass raised two Important findings (`STATE.md` `## Feedback (for retro)`, "Raised by: aof-architect (71/01 review)") and recorded them rather than promoting them — it ran under the prompt it was replacing. `grep -rl "Promoted from review finding" wiki/work/*/CHORE.md` answers nothing: no chore in the stream carries a finding back-reference. | 71/01 `tasks/02` `@manual` scenario |
| 71/01 | tag census | the story's 3 task `.feature` files | 2026-09-02: **18 `@executable` scenarios, 1 `@manual`, and no `@uat`** — so no human sign-off step applies to this story and none was solicited. No `DESIGN.md` and no UI surface, so no design-conformance review applies. | 3 task features |
| 71/02 | `@executable` | the story's own lane through the registries' own exported arrays, each test under its own hermetic `AOF_GLOBAL_HOME`: `test/story-context-contract.test.mjs` (17 — the story's five own assertions plus the twelve neighbours it shares the file with), `test/work-next-ready-set.test.mjs` (18 — the wave the derivation reads), and the four neighbours this story's diff touches: FF-7101 (6), FF-7105 (3), `acd-bundle-manifest-hashes` (3), `acd-bundle-membership` (3), `acd-test-suite-registration` (4) | 2026-09-03: **54/54 green**, 0 failures. The story's five assertions are named in the runner output: *"the review lanes are spawned together, staggered, bounded — and no lane waits on another's output"*, *"execution mode is derived from the wave, before anything is dispatched — and never widens a solo setting"*, *"decideExecutionMode resolves (configured mode × --solo × wave size) to one mode, and refuses a wave it cannot count"*, *"a granted second round re-reviews the delta — only the raising lens, only over the fix"*, *"refine's cascade closes the ADR set before the contract fan-out, and routes a later delta instead of re-authoring"*. | 26 `@executable` scenarios across 4 task features |
| 71/02 | generated siblings | `renderBundleOutputs(await loadBundle(), { runtimes: ["claude","codex","opencode"] })` intersected with `git ls-files`, compared byte-for-byte | 2026-09-03: **143 tracked outputs compared, 0 stale** (115 byte-exact, 28 differing by CRLF alone). All nine renders this story declares — `.claude/commands/aof/{continue,refine,code-review}.md`, `.codex/skills/aof-{continue,refine,code-review}/SKILL.md`, `.opencode/commands/aof/{continue,refine,code-review}.md` — are **byte-exact**. `src/bundle/manifest.json` is current (`acd-bundle-manifest-hashes` green on all three legs). Measured by hand again because FF-7106 does not exist (F-71-A, now chore 89). | 71/02 STORY.md `files:`, ADR-008 |
| 71/02 | gate | `aof work validate 71/02` | 2026-09-03: **PASS — 71/02 is well-formed.** | — |
| 71/02 `tasks/00-03` | `@manual` | *lanes overlap in time · a wave of one pays no fan-out · a second round costs one lens · refine spends no run re-applying an ADR delta* | **NOT RUN from a run record — see F-71-G.** All four read their answer off the run record, and `aof work observe 71` and `aof work observe 72` both report **0 agent runs across 0 sessions, 408 unattributed** — both milestones were driven interactively rather than through the loop door. One is discharged at the ARTEFACT level instead: `tasks/03` — milestone 72's refine authored **eleven task contracts and `ARCHITECTURE.md` in one commit each** (`git log` per file: 1 commit, all `ea5d4975`) across the **three amendment rounds** 72's `STATE.md` records, so each contract had exactly one authoring beat and no run existed to re-apply a delta. Contrast the defect this closes: milestone 52's thirteen re-application runs, 41.8% of its output tokens. | 71/02's 4 `@manual` scenarios |
| 71/02 | tag census | the story's 4 task `.feature` files | 2026-09-03: **26 `@executable` scenarios, 4 `@manual`, and no `@uat`** — so no human sign-off step applies and none was solicited. | 4 task features |
| 71/03 | `@executable` | the story's own lane through the registries' own exported arrays, each under its own hermetic `AOF_GLOBAL_HOME`: FF-7102 (3) plus the three milestone-07 controls it amends in place — `acd-conformance-verdict-contract` (4), `acd-design-conformance-bundled` (3), `acd-design-role-split` (5) | 2026-09-03: **15/15 green**, 0 failures. The three superseded controls are green **as amended**, including the legs 07 still owns: *"Playwright is on-demand via npx, NOT a dependency in root package.json"*, *"the browser-execution ownership tokens … the designer never runs the browser"*, and *"verify.md + continue.md render via the cached Chromium"*. | 13 `@executable` scenarios across 2 task features |
| 71/03 | schema | `schemas/aof.schema.json` read at source | 2026-09-03: `work.ui.renderer` is declared as an optional string inside the **closed** (`additionalProperties: false`) `work.ui` block — the edit ADR-009 found necessary when it corrected ADR-005 §3's claim that `work.ui` was open. One line added; `work.ui.baseUrl` and `work.ui.a11y` untouched. | ADR-005 §3 as corrected by ADR-009 |
| 71/03 | generated siblings | the same render-vs-`git ls-files` comparison | 2026-09-03: all six renders this story declares — `.claude/commands/aof/{continue,verify}.md`, `.codex/skills/aof-{continue,verify}/SKILL.md`, `.opencode/commands/aof/{continue,verify}.md` — are **byte-exact**. | 71/03 STORY.md `files:`, ADR-008 |
| 71/03 | gate | `aof work validate 71/03` | 2026-09-03: **PASS — 71/03 is well-formed.** | — |
| 71/03 `tasks/01` | `@manual` | *a real surface renders on this machine* | **RUN, 2026-09-03 — DISCHARGED, with a finding (F-71-F).** The renderer resolved by GLOBBING `~/AppData/Local/ms-playwright/`: ten Chromium revisions present, the highest (`chromium-1234`) resolving under **`chrome-win64`** — the layout instability the glob-never-template rule exists for. The shipped invocation was run verbatim against a real served surface (`http://127.0.0.1:4181/?mode=fleet`), once per breakpoint: **exit 0 at all three widths, three PNGs written at the named absolute forward-slash paths — 10,815 / 11,251 / 16,184 bytes, none zero-byte.** `aof-designer` was then spawned with the three paths and the conformance baseline question, and returned a **region-by-region verdict of `INCONCLUSIVE`** naming the missing baseline (every committed mock under `47_*/mocks/` is of the *filtered* fleet; 47/DESIGN's own conformance table declines to re-baseline the unfiltered page; 25/DESIGN's checklist is superseded by 47/ADR-006). **That verdict is the mechanism succeeding** — the designer refused to read component code and manufacture a `CONFORMS`. Both halves of the scenario hold: a screenshot file was produced at the named path, and the designer returned a region-by-region verdict from it. | 71/03 `tasks/01` `@manual` scenario |
| 71/03 `tasks/00` | `@manual` | *an unconfigurable repository pays one line for the lane* | **NOT RUN.** This repository IS the unconfigurable one — `.aof/aof.config.json` carries no `work.ui` key at all (read at source) — but it holds no UI story to drive, so the precondition's cheap exit could not be observed end to end. Same shape as F-71-B: a natural discharge point exists (the first UI story driven anywhere), and inventing one would prove the invention. | 71/03 `tasks/00` `@manual` scenario |
| 71/03 | tag census | the story's 2 task `.feature` files | 2026-09-03: **13 `@executable` scenarios, 2 `@manual`, and no `@uat`** — so no human sign-off step applies and none was solicited. | 2 task features |
| 71 (milestone) | `@executable` | **the whole suite, run once at the gate** — `scripts/test.mjs`'s own exported `tests` array driven through the runner's per-test hermetic `AOF_GLOBAL_HOME`, MINUS `test/global-work-propagation.test.mjs` (6 tests): that lane binds `:4182`, which the live control daemon holds on this machine, so it is excluded by the repo's standing rule rather than skipped silently | **First pass**, 2026-09-03, before chore 89: **8,395 / 8,403, 8 fail**. **Second pass**, after chore 89 landed FF-7106's control: **8,419 / 8,430, 11 fail** (~40 min each). **None of the eleven is 71/02's, 71/03's or chore 89's**, and every one is named and routed: **four** are 71/00's and 71/01's, promoted to **chore 88** (`acd-cache-read-surface-boundary`'s moved `defaultAt()` pin, FF-5809's unclassified file, FF-6109's lifted `harnessRefusal`, and `61/03 task 02` which shares FF-6109's cause — **F-71-H**); **three** belong to milestone 72's refine (**F-71-I**); **three** appeared BETWEEN the two passes, from milestone 72's build running concurrently in this same working tree (**F-71-K**); **one** belongs to milestone 63's uncommitted `VERIFICATION.md` (**F-71-J**). The exclusion is the honest cost of running the gate on the control node: the propagation lane is unrun here. | ADR-008, the milestone gate |
| chore 89 | `@executable` | FF-7106's own control, plus the three registration guards a new suite file reds if it is imported-and-not-spread: `acd-test-suite-registration`, `acd-registry-fixture-closed` (FF-5809), `acd-loop-suite-registration` (FF-5311) | 2026-09-03: FF-7106 **6/6 green**. The registration guards report **only milestone 72's in-flight files** — `acd-test-suite-registration` names `acd-suite-registration-single-decider`, `acd-test-selection-widens-never-narrows` and `work-test-select` (all FF-7202/FF-7203/72/01), FF-5809 names `acd-prompt-bounds-name-their-home` (chore 88's item b), FF-5311 names `work-test-select`. **This control's own file appears in none of them**, which is the measurement that it is imported AND spread rather than merely mentioned. | chore 89 `## Definition of Done` |
| chore 89 | gate | `aof work validate` (whole stream), `aof work validate 89`, `aof work doctor 71` | 2026-09-03: **PASS — work stream is well-formed**; **PASS — 89 is well-formed**; and `aof work doctor 71` reports **no `control-unresolved` at either severity**, which is the chore's own stated close criterion. | chore 89 `## Definition of Done` |
| 71 (milestone) | tag census | all 12 task `.feature` files across the four stories | 2026-09-03: **73 `@executable` scenarios, 9 `@manual`, and NO `@uat` anywhere in the milestone** — so **step 2 (human acceptance) does not apply and no human was pestered**, which is the intended outcome for a technical/prompt-layer milestone. Of the nine `@manual`: **1 discharged** (71/03 `tasks/01`, run at this gate), **1 discharged at the artefact level** (71/02 `tasks/03`, from 72's one-commit contracts), **1 partially run** (71/00 `tasks/00`), **6 open** against F-71-B / F-71-G / F-71-I's shape — all six waiting on a run record or a real UI story. | 12 task features |
| 71 (milestone) | design conformance | the milestone's own folder, read at source | 2026-09-03: **NOT APPLICABLE — milestone 71 carries no `DESIGN.md` and no frontend surface**, so no design-conformance verdict gates it and no render/designer/QA hand-off was run *as a gate*. The render and designer hand-off recorded above are 71/03's `@manual` discharge over an unrelated live surface, and produced a finding (F-71-F) rather than a verdict on 71. | ADR-001/002/003 |
| 71 (milestone) | gate | `aof work validate 71`, `aof work doctor 71` | 2026-09-03, **twice**. Before chore 89: validate **PASS**, doctor **one `control-unresolved` — FF-7106**, which was the accept blocker (read at BOTH severities per the accept rule, because a `pending` marker changes what doctor prints and never whether the control exists). After chore 89: validate **PASS — 71 is well-formed**, doctor **NO `control-unresolved` at either severity — all six declared controls resolve**. Loop-Ready **80% (8/10)**, up from 70% at 71/00's accept. Remaining warns, none an acceptance gate: `cache-incomplete`, `mtime-ahead-of-updated`, `numbering-gap`, `control-runner-unchecked`, and `rubric-join-unchecked` per story. | the accept rule |

---

## Findings

<!-- Register for milestone 71. Ids allocated HERE, at the moment of landing, by the single writer
     (`aof:verify`'s product owner) — never read-then-allocate. Cite another item's id as
     `m?<itemRef>/<ID>`. -->

| id | observed | type | severity | triage | routed-to | status |
|---|---|---|---|---|---|---|
| F-71-A | `ARCHITECTURE.md` declares **FF-7106** (ADR-008 — a story's `files:` must name every generated sibling its change lands) and cites `test/arch/acd-declared-writes-include-generated-siblings.test.mjs`, which is not a file on disk. The story map assigns FF-7101/FF-7105 to 71/00, FF-7103/FF-7104 to 71/01 and FF-7102 to 71/03; **no story's `files:` names FF-7106's control**, so the declaration has no owner and no landing beat. `aof work doctor 71` reports it as `control-unresolved`, downgraded to `warn` by the standing `pending` marker — which is exactly the shape the accept rule says a marker must not be allowed to hide. | gap | important | **non-blocker for 71/00** — the control guards a claim about *other* stories' write sets, not this story's behaviour, and 71/00's own write set was verified by hand against the render engine (evidence row above, 6/6 in sync). **It is a blocker for the milestone**: 71 cannot be accepted with a declared control that does not resolve. **Unchanged at the milestone gate, 2026-09-03** — `aof work doctor 71` still reports it, and it is the ONLY `control-unresolved` finding left at either severity (FF-7102's control landed). | **routed by the shipped triage rule during 71/03's review: `chore 89` (`89_chore_ff-7106-is-declared-in-milestone-71-s-fitness-register-but-owned-by-no-story`)**, whose `## Definition of Done` carries the register's full specification and a `**Promotion key:**` back-reference to this finding. The declaration deliberately STAYED in 71's register — chore 89's DoD cites it as the specification, so dropping it would have destroyed the chore's contract; the rule's other exit was the wrong one here. **CLOSED 2026-09-03: chore 89 ran to `done`** — the control is on disk, registered in `scripts/test.mjs`, 6/6 green with three red probes recorded above, and `aof work doctor 71` now reports **no `control-unresolved` at either severity**. All six declared controls resolve. | closed |
| F-71-B | 71/00's two `@manual` scenarios are not discharged. `tasks/00`'s is partially run (the ladder's cost measured on a real red item); the clauses proving a live `aof:continue` refuses to spawn reviewers on a red gate are not, because no story in the stream is red at validate today. `tasks/01`'s is not run at all — its precondition is a build that has stopped converging, which cannot be induced without fabricating it. | gap | important | **non-blocker** — every `@executable` claim in both features is green, and both lanes have a natural discharge point rather than needing invented work: driving 71/01, 71/02 and 71/03 through `aof:continue` runs the shipped ladder and the shipped terminator live, three times, before the milestone gate. | **Still open at the milestone gate, 2026-09-03, and the reason has changed** — the three runs happened, but they were driven interactively rather than through the loop door, so `aof work observe 71` attributes **0 agent runs across 0 sessions (408 unattributed)** and there is no run record to read the ladder's refusal-to-spawn or the terminator's stop out of. This is the same missing-run-record cause as **F-71-G** and discharges with it. | open |
| F-71-C | After FF-7101's and FF-7105's controls landed, the `enforced by` cells in `ARCHITECTURE.md`'s register still read `— **pending**`. The marker was accurate at refine and stale at accept; left standing it downgrades a future genuine `control-unresolved` on those rows to a warn. | record drift | nit | **fixed at this accept** — the two markers were struck, so both rows now cite a control that resolves with nothing softening the report. The four rows whose stories have not landed keep their markers, correctly. | `wiki/work/71_milestone_loop-discipline/ARCHITECTURE.md` | closed |
| F-71-D | 71/01's one `@manual` scenario (`tasks/02`: *a real capped review pass leaves a legible queue*) is not discharged. Its precondition — a review that reaches its cap with surviving Important findings, run under the shipped `<finding_triage>` region — has not occurred: the pass that built 71/01 ran under the prompt it was replacing, recorded its two Important findings in `STATE.md` feedback rather than promoting them, and no chore in the stream carries a finding back-reference. | gap | important | **non-blocker** — every `@executable` claim behind the scenario is green (the verb through the command core, the routing, the append, the idempotence, the refusals), and the lane has a natural discharge point rather than needing invented work: 71/02's and 71/03's `aof:continue` runs execute the shipped region live. | **DISCHARGED at the milestone gate, 2026-09-03.** Both runs did exactly that. 71/02's review round one raised *"Three arch tests are red from 71/00 and 71/01, both already done"* and 71/03's raised *"FF-7106 is declared in milestone 71's fitness register but owned by no story"*, and each was promoted rather than recorded: **`88_chore_three-arch-tests-are-red-from-71-00-and-71-01-both-already-done`** and **`89_chore_ff-7106-is-declared-in-milestone-71-s-fitness-register-but-owned-by-no-story`**. Both are top-level chores born `not-started`, both appended after the highest existing number shifting nothing, both `aof work validate` **PASS**, and both carry the full `## Notes` back-reference — `**Promoted from review finding:**` with the finding's title and `file:line`, `**Raised reviewing:** 71/02` / `71/03, review round 1`, and a visible `**Promotion key:**`. `grep -rl "Promoted from review finding" wiki/work/*/CHORE.md` now answers two files where it answered none. The queue the cap leaves behind is legible. | closed |
| F-71-E | After FF-7103's and FF-7104's controls landed, both `enforced by` cells in `ARCHITECTURE.md`'s register still read `— **pending**` — the same drift F-71-C found on 71/00's two rows. | record drift | nit | **fixed at this accept** — both markers struck, so `aof work doctor 71` now reports `control-unresolved` only for FF-7102 and FF-7106, which are genuinely unlanded. | `wiki/work/71_milestone_loop-discipline/ARCHITECTURE.md` | closed |
| F-71-F | **The render's soundness does not travel with the frame, and `--window-size` cannot establish it.** ADR-009 §C added `--window-size=<W>,<H>` to the shipped invocation so a mechanism swap could not silently drop 07's breakpoints — but milestone **47**'s delivered design contract had already ruled on that flag: `47/DESIGN.md` **F-47-V-21** (added 2026-08-13, verify pass 2) states *"a frame whose page overflows its viewport, or whose layout viewport is not the width it claims, may not be used to judge anything … soundness is the renderer's obligation and travels with the frame: record `document.documentElement.scrollWidth == innerWidth`, that the layout viewport equals the stated width (Chromium's `--window-size` does not guarantee this; CDP `Emulation.setDeviceMetricsOverride` does), and a PNG `IEND` terminator"* — a clause added after **three HIGH design findings were raised and withdrawn** for want of it. Demonstrated live at this gate, in both of its failure modes. **(a) The 390 frame is unsound**: the nav, the surface-bar slot and all four content boxes are cut at the *same* x — three unrelated subtrees, the signature of a layout viewport wider than 390, not five independent overflows. The read-only designer correctly refused to judge it. **(b) The step names no settle**: the 768 frame was captured mid-load and its surface bar rendered as an empty 40px band, which the designer escalated as a suspected Blocker against `acd-mesh-ui-scope-visible` (34/ADR-006) while flagging capture timing as the likelier cause. A re-render with `--virtual-time-budget=8000` showed the bar fully populated (`Global\|Local`, `All repos ▾`, `legend`, `refreshed just now`) — **so the suspected Blocker is an artifact and is closed**, but it took a second render nobody was instructed to take. The shipped step names four render-FAILURE modes (`exits non-zero`, `writes no file`, `zero-byte`, `does not return`) and no render-SOUNDNESS obligation at all. | gap | important | **non-blocker for 71/03** — the story's claim is that the lane is GATED on renderability, and that is delivered and controlled: the precondition resolves before any spawn, the skip is recorded, the supersession is real, and FF-7102 holds all three legs under four plants. Soundness is a property of the frame the render produces, which is the next layer down and which 47 owns the vocabulary for. It is nonetheless the difference between a judgeable frame and an unjudgeable one, and it cost a designer session a near-miss Blocker at this very gate. | **deferred to the backlog as a chore-shaped item** — amend the render step in `src/bundle/commands/{continue,verify}.md` (and the six rendered runtime copies) to drive CDP `Emulation.setDeviceMetricsOverride` rather than `--window-size`, to wait for settle, and to record the three soundness facts alongside each screenshot handed to the designer; widen FF-7102 leg (a) to assert the soundness record rather than the flag. Cite `m47/F-47-V-21` as the specification. | open |
| F-71-G | **71/02's four `@manual` scenarios all read their answer off a run record, and this milestone produced none.** Each of the four — *lanes overlap in time*, *a wave of one pays no fan-out*, *a second round costs one lens over one diff*, *refine spends no run re-applying an ADR delta* — is phrased against "the run record". `aof work observe 71` and `aof work observe 72` each report **0 agent runs across 0 session(s)** with **408 unattributed agent runs**: both milestones were driven interactively rather than through the loop door, so nothing attributes a session to an item. The consequence is specific and it is the milestone's own headline: SPEC quantifies the defect as **1h03m of pure serialisation in one milestone** (`aof-architect` 30m46s at 1.00× concurrency, `aof-qa` 32m01s), and the saving that fix produces is now **stated and unmeasured**. F-71-B is the same cause one story earlier. | gap | important | **non-blocker** — every `@executable` claim behind all four scenarios is green (54/54 on 71/02's lane), the four claims are prompt-layer instructions that ADR-008 deliberately gave no fitness function because a grep for "together" proves nothing, and the discharge point is natural rather than invented: the first milestone driven through the loop door produces the record all four read. Blocking 71 on it would block a prompt change on telemetry attribution that is milestone 68's subject, not this one's. **One of the four is discharged at the artefact level** (72's eleven contracts, one commit each, three amendment rounds — evidence row above). | **deferred to the backlog**, and it is a real gap in the milestone's own evidence rather than a formality: until one milestone is driven through the loop door, 71's headline claim is unmeasured. Recorded in 71/02's `OUTCOME.md` `## Gaps` with its discharge condition. | open |
| F-71-H | **Chore 88's `## Definition of Done` is one red short of the tree.** It names three reds to clear — `acd-cache-read-surface-boundary`'s moved `defaultAt()` pin, `acd-registry-fixture-closed` (FF-5809)'s unclassified new file, and `acd-progress-ledger-consumed` (FF-6109)'s lifted `harnessRefusal` — and closes with *"re-run the full test/arch tree to confirm 1543/1543"*. Measured at this gate, the FF-6109 cause reddens a **fourth** test that is not in `test/arch/` and not named: `test/acceptor-admissibility.test.mjs:589`, *"61/03 task 02 — outline row: a key the registry declares tunable is considered, and judged on the other grounds"*, which expects `["not-admissible", "harness-not-introspectable"]` and now gets `["not-admissible"]` alone — the same lift, one layer up in the behavioural suite. A chore whose DoD stops at `test/arch/` would tick every box and leave the tree red. | gap | important | **non-blocker for 71** — the chore is `not-started` and every one of its reds pre-dates this gate; none is 71/02's or 71/03's. But it is a blocker for **chore 88's own** accept, which is where it lands. | **chore 88** — the missing item was added to its `## Definition of Done` at this gate (routing, not building: the chore is `not-started` and its DoD is its whole contract, so a known red left out of it guarantees the chore closes over a red tree). | open |
| F-71-I | **Three reds in the tree at this gate belong to milestone 72's refine, not to 71.** `test/brief-pinned-to-the-stream.test.mjs` (70/05 task 02) fails three ways and every named subject is a 72 item: *"72/00, 72/01, 72/02, 72/04 /refine: sacrificed objective, tasks, fitness, dependencies with 2,329–3,838 chars of budget still unspent"*; *"72/02 and 72/04 declare `depends:` in frontmatter, yet their refine brief carries no DEPENDENCIES section"*; *"72/refine: its ARCHITECTURE section is present but names 0 of its 4 entries — the section reached the brief and its content did not"*. All three landed with commit `ea5d4975` (72's refine), after 70/05 shipped. | gap | important | **non-blocker for 71** — no 71 item appears in any of the three failure lists, and 71 wrote nothing under `wiki/work/72_*`. The phase-brief condenser is milestone 70's delivered surface and 72's items are its new subjects. | **deferred to the backlog** as a chore against the phase-brief condenser: a brief that drops `objective`/`tasks`/`fitness`/`dependencies` with thousands of characters of budget unspent is a condenser bug, not a budget one, and the husk case (a section present that names none of its own entries) is the shape 70/05's own control was written to catch. | open |
| F-71-L | **The stale mesh projection fabricates an error-severity gate failure against a correctly accepted milestone.** Immediately after `aof work status 71 done`, `aof work doctor 71` reports `error: lying-parent — milestone 71 is done but child stories 71/00, 71/01 are not done`, and Loop-Ready falls 80% → 70% (`blocking: stream-coherent`). **Read at the source, all four `STORY.md` records carry `status: done`.** `aof work find 71/00 --json` and `71/01 --json` both answer `not-started` with `answeredFrom: "cache"`, `reportedBy: "win-host-a-wsl"`, `syncedAt: 2026-09-03T01:03:19Z` — a projection published by another node before those accepts. The WRITE path was never confused: every `aof work status` printed its real transition and exited 0 with no propagation warning, because it validated against the local record. This is `STATE.md`'s standing feedback note about the read path, with a consequence worse than the one that note recorded: it does not merely re-offer finished stories to the walk, it **manufactures a red gate for an item whose records are correct**. | gap | important | **non-blocker, and it does not disturb this accept** — the accept rule's three inputs all read local records and all held: `aof work validate 71` **PASS** (before and after), `aof work doctor 71` reported **no `control-unresolved`** at the moment of accept, and no blocker finding was open. A finding produced by a stale projection is evidence about the projection, not about the item — the same reading the lifecycle verb's refusals get. Recorded because a future reader running `doctor 71` will see this error and must not "fix" correct records to satisfy it. | **deferred to the backlog**, and it is the sharpest form of the read-path defect this milestone kept meeting: the read path should prefer the local record when its `updated:` is newer than the projection's `syncedAt`, or invalidate on local write. Until then, `lying-parent` on a milestone whose `STORY.md` records all read `done` is a false finding — confirm at the source. | open |
| F-71-K | **The working tree moved under the gate: milestone 72 is being built concurrently in it.** Between the gate's first suite pass (8 failures) and its second (11), three further tests went red, and every file they name is 72's in-flight, uncommitted work: `acd-test-suite-registration` reports `acd-suite-registration-single-decider` (FF-7203), `acd-test-selection-widens-never-narrows` (FF-7202) and `work-test-select` (72/01 task 00) as **imported by a runner and never spread** — "the file looks registered and executes nothing"; `acd-loop-suite-registration` (FF-5311) and `instrument-census/02` report the same files as unregistered. `git status` shows the matching untracked sources (`src/work-test-select.mjs`, `src/work-toolchain.mjs`, `src/graph-impact.mjs`) and a chore **90** promoted from a 72 review during this gate. | gap | important | **non-blocker for 71** — the failures are a snapshot of another milestone's build mid-edit, they name no 71 file, and 71 wrote nothing under `wiki/work/72_*`. It is recorded because it bounds what "the full suite ran green at the gate" can ever mean here: **a whole-suite number taken while a sibling milestone is mid-build measures the tree, not the item**, and the honest reading is the per-item lanes plus an attributed failure list — which is what this document carries. | **no action against 71.** For 72: the three files are its own to register or baseline before its gate. Recorded as the evidence-quality note it is, not as a defect. | open |
| F-71-J | The last red in the tree at this gate belongs to milestone **63**: `test/arch/acd-no-internal-project-names.test.mjs:103` reports *"wiki/work/63_milestone_event-driven-triggers/VERIFICATION.md: 1 reference(s), and this file is NOT on the baseline — a new disclosure"*. The control is shrink-only by design, so the cure it names is to remove the reference or add an EXEMPT entry with a written reason — never to raise the baseline. The file is uncommitted in the working tree. | gap | important | **non-blocker for 71** — 71 wrote nothing under `wiki/work/63_*`, and the disclosure control is a repo-wide hygiene gate rather than a 71 acceptance criterion. It is nonetheless a **private-project name in a tracked-path document**, which is the one class of red that should not sit in a working tree indefinitely. | **deferred to the backlog**, and named here as the highest-priority of the four inherited-red findings: remove the reference from 63's `VERIFICATION.md`, or add the EXEMPT entry with its reason. | open |

---

## Accept decision

**71/00 — ACCEPTED, 2026-09-02.**

- `aof work validate 71/00` → **PASS**.
- `aof work doctor 71/00` → **no `control-unresolved` at either severity**; the two controls this
  story lands both resolve to files on disk, and both are registered in the runner.
- The story's lane is **41/41 green**, and both controls were **observed red** under a planted
  defect and green again over a tree proven byte-identical by sha256.
- **No blocker finding is open.** F-71-A and F-71-B are open at *important*, both routed to the 71
  milestone gate; neither bears on whether 71/00's own behaviour is delivered. F-71-C is closed.
- No `@uat` scenario exists in this story, so no human sign-off was solicited. No UI surface, so no
  design-conformance verdict applies.

**Not run at this accept, deliberately:** `aof:retrospective` and the `STATE.md` compaction are
milestone-close acts and 71 has three stories still open, so the `## Feedback (for retro)` notes stay
where they are until the milestone gate.

**71/01 — ACCEPTED, 2026-09-02.**

- `aof work validate 71/01` → **PASS**.
- `aof work doctor 71/01` → **no `control-unresolved` at either severity**; the two controls this
  story lands resolve to files on disk, are registered in the runner, and their stale `pending`
  markers were struck (F-71-E).
- The story's lane is **94/94 green** — its own 13, the two controls, 39/03's untouched suite as the
  extraction's control, the three closed-list guards, the bundle pair, registration, and the three
  `continue.md` neighbours — and both controls were **observed red** under six planted defects (one
  per leg) and green again over a tree proven byte-identical by sha256.
- The three rendered runtime copies of `continue.md` are byte-identical to the live bundle render.
- **No blocker finding is open.** F-71-D is open at *important*, routed to the 71 milestone gate
  exactly as F-71-B was; it concerns whether a live pass follows the shipped rule, not whether the
  rule, the verb and the bound are delivered. F-71-A and F-71-B remain open from 71/00, unchanged.
- No `@uat` scenario exists, so no human sign-off was solicited. No UI surface, so no
  design-conformance verdict applies.
- `OUTCOME.md` authored in the story's folder; `SPEC.md` `## Stories` ticked.

**Not run at this accept, deliberately:** `aof:retrospective` and the `STATE.md` compaction are
milestone-close acts and 71 has two stories still open (71/02, 71/03); the `## Feedback (for retro)`
notes — including the two this story's review raised — stay where they are until the milestone gate.

**71/02 — ACCEPTED, 2026-09-03.**

- `aof work validate 71/02` → **PASS**. `aof work doctor 71/02` → **no `control-unresolved` at
  either severity**; the story declares no fitness function, by decision (ADR-008). Two warns,
  neither an acceptance gate: `numbering-gap` and `rubric-join-unchecked`. Loop-Ready 80% (8/10).
- The story's lane is **54/54 green** — its five own assertions in `story-context-contract`, the
  twelve neighbours it shares that file with, the 18-test ready-set suite the derivation reads, and
  the four neighbours the diff touches (FF-7101, FF-7105, the bundle pair, registration).
- **No red probe is owed.** ADR-008 declares this story lands no fitness function, and the register
  carries no `FF-` row assigned to it — the four claims are prompt-layer instructions a grep could
  not falsify, and each is checkable only by driving the seam.
- All nine of its rendered runtime copies are byte-identical to the live bundle render.
- **No blocker finding is open.** **F-71-G** is open at *important* — the story's four `@manual`
  scenarios all read a run record this milestone never produced — and is recorded with its discharge
  condition in the story's `OUTCOME.md` `## Gaps`. F-71-A remains open, routed to chore 89.
- No `@uat` scenario exists, so no human sign-off was solicited. No UI surface, so no
  design-conformance verdict applies to this story.
- `OUTCOME.md` authored in the story's folder; `SPEC.md` `## Stories` ticked.

**71/03 — ACCEPTED, 2026-09-03.**

- `aof work validate 71/03` → **PASS**. `aof work doctor 71/03` → **no `control-unresolved` at
  either severity** — FF-7102's control resolves to a file on disk and is registered in the runner,
  and its `pending` marker was struck at this accept. Same two non-gating warns; Loop-Ready 80%.
- The story's lane is **15/15 green** — FF-7102's three legs plus the three milestone-07 controls it
  amends in place, all green *as amended*, with the legs 07 still owns asserted still enforced.
- FF-7102 was **observed red under four planted defects**, one per leg plus the second `npx
  playwright` plant, each in a fresh `node` process, each restored from a pre-probe copy with sha256
  proven equal before and after, and the control green over the restored tree.
- The `@manual` scenario *a real surface renders on this machine* was **RUN and is discharged**: the
  renderer resolved by glob (`chromium-1234 → chrome-win64`), the shipped invocation produced three
  non-empty PNGs at the named paths, and `aof-designer` returned a region-by-region `INCONCLUSIVE`
  naming the missing baseline — the mechanism working, and refusing to manufacture a verdict.
- **No blocker finding is open.** **F-71-F** is open at *important* — the frame the shipped
  invocation produces carries no soundness record, and `--window-size` cannot establish one
  (`m47/F-47-V-21`) — triaged non-blocker because the story's claim is that the lane is *gated*, and
  that is delivered and controlled. Recorded in the story's `OUTCOME.md` `## Gaps`.
- No `@uat` scenario exists, so no human sign-off was solicited. Milestone 71 carries no `DESIGN.md`
  and no UI surface of its own, so no design-conformance verdict gates this story; the render above
  was the `@manual` discharge, not a conformance gate.
- `OUTCOME.md` authored in the story's folder; `SPEC.md` `## Stories` ticked.

---

**71 (the milestone) — ACCEPTED, 2026-09-03.** All four stories `done` and ticked in `SPEC.md`.

**The gate was run twice, because the first pass refused it.** On the first pass
`aof work doctor 71` reported one `control-unresolved` — **FF-7106**, declared by ADR-008 and given
no owning story by the four-story partition (**F-71-A**) — and the accept rule admits no marker:
*do not accept an item whose `ARCHITECTURE.md` register declares a control that does not resolve;
what clears it is landing the file or dropping the declaration, never re-marking it `pending`.*
Dropping it was the wrong exit — `chore 89`'s `## Definition of Done` cited this register as its
specification, so unsaying the declaration would have destroyed the chore's contract. **Chore 89 was
run to `done` instead**, and the second pass cleared: **no `control-unresolved` at either severity,
all six declared controls resolve, and every one carries a recorded red probe.**

- `aof work validate 71` → **PASS**. `aof work validate` (whole stream) → **PASS**.
- `aof work doctor 71` → **no `control-unresolved`**. Loop-Ready **80% (8/10)**, up from 70%.
- The whole suite ran at the gate: **8,419 / 8,430**. All eleven failures are named and attributed
  in the evidence table, and **none is 71's**: four are 71/00's and 71/01's, already queued as
  chore 88 (F-71-H); three are 72's refine (F-71-I); three are 72's build running concurrently in
  this same tree (F-71-K); one is 63's (F-71-J).
- **No `@uat` scenario exists anywhere in the milestone**, so step 2 was skipped entirely and no
  human was asked for anything — the intended outcome for a prompt-layer milestone.
- **No `DESIGN.md` and no UI surface**, so no design-conformance verdict gates this milestone.

**What the six controls now hold, each observed red first:** FF-7101 (a bound stated in a bundled
prompt names its home and equals it, binding to the clamp where one stands) · FF-7102 (no bundled
asset renders through `npx playwright`, and the precondition precedes the render *by order*) ·
FF-7103 (the loop's creation authority is one type, one placement, zero shifts) · FF-7104 (one
promotion engine, two faces, no rival and no upward import) · FF-7105 (the prompt's gate ladder is
the shell's, derived from `invokeGateLadder`) · FF-7106 (a story's declared write set includes every
generated sibling its change lands). Seventeen planted defects across the six.

**The loop's own rules were exercised on this milestone, not just written into it.** 71/02's and
71/03's review closes ran under the shipped `<finding_triage>` region and **promoted rather than
recorded**, producing chores **88** and **89**; 72's review then produced **90** the same way. Chore
89 is the loop closing on itself: a finding this milestone's own gate refused to accept became a
named work item, was executed, and cleared the gate that raised it.

**Open findings at accept — five, no blockers:** F-71-B and F-71-G (six `@manual` scenarios waiting
on a run record; the milestone was driven interactively, so `aof work observe` attributes nothing —
the headline saving is stated and unmeasured) · F-71-F (the render's soundness does not travel with
the frame; `--window-size` cannot establish the layout viewport, per `m47/F-47-V-21`) · F-71-I,
F-71-J and F-71-K (inherited and concurrent reds belonging to milestones 72 and 63) · F-71-H (chore
88's checklist, corrected here). F-71-A, F-71-C, F-71-D and F-71-E are closed.

**Run at this accept:** `aof:retrospective 71` (→ `RETROSPECTIVE.md`, **R1–R9**),
`aof work memory ingest` (1,785 records reindexed), `aof work observe 71 --write --if-enabled`
(snapshot written; it records the zero attribution that F-71-G is about), the `STATE.md` compaction
with `## Feedback (for retro)` archived, and the milestone `OUTCOME.md`.

**One thing observed AFTER the accept, recorded so a later reader does not mistake it for a defect in
these records.** `aof work doctor 71` now reports `error: lying-parent — milestone 71 is done but
child stories 71/00, 71/01 are not done`, and Loop-Ready falls to 70%. **It is false.** All four
`STORY.md` records read `status: done` at the source; `aof work find 71/00 --json` answers
`not-started` from `answeredFrom: "cache"`, published by another node before those accepts. The
accept's own inputs were all read from local records and all held. See **F-71-L** — and do not
"correct" records that are already right in order to satisfy a stale projection.
